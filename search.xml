<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>「译」大规模敏捷</title>
    <url>/blog/2019/12/06/in-the-large/</url>
    <content><![CDATA[<p>自敏捷之开端始，我们就思考大规模敏捷的问题。我们是否能够把轻量级，迭代，增量，快速反馈等软件开发的原理应用于规模巨大的项目呢？</p>
<p>最初我们想到的答案是Scrum of Scrums之类的东西。这个想法是在更高的层次上递归地应用敏捷开发的原理。如果一个项目需要超过5-12个开发人员，那么可以组织两个这样的团队，以及一个更高级别的团队来“监督？”他们。</p>
<p>请注意上面的问号。当我们开始考虑大型项目时，我们不可避免地要谈及层级化的组织。但是敏捷似乎是厌恶组织层级的。毕竟，敏捷就是关于平等主义的。敏捷是拒绝命令和控制的。敏捷是拒绝计划和时间表的，还有…</p>
<p><strong>无稽之谈！并不是这样的！</strong></p>
<p>敏捷其实是一场“风水轮流转”的革命。在软件行业的早期，我们原本就以敏捷的方式编写代码。我们写小块代码片段，对其进行了测试，将它们构建为更大的块，如此循环往复。如果您回到1960年代后期，并观察当时人们编写代码的方式，您会发现敏捷的小荷尖尖角正在显露出来。</p>
<p>当然，当时我们在硬件方面受到了很大的限制。编译要跑好几个小时。代码编辑要用电传打字机。那时，大多数程序员根本不会使用键盘。因此他们让按键操作员替他们输入代码。在这种环境下，很难实现快速反馈机制。</p>
<p>即便如此，我们仍竭尽所能去缩短反馈周期。我们使用汇编器编写程序，坐在控制台前通过用八进制或十六进制打补丁的方式来进行调试。我们可以通过在调试器中执行代码，甚至通过单步执行来测试代码。经年累月，熟能生巧。这是在合理的时间内完成工作的唯一方法。</p>
<p>但是“风水轮流转”了。我们开始使用在控制台上不容易调试的语言。我们开始编写越来越大的程序。为了在反馈周期如此长的环境中工作，我们需要制定计划。</p>
<p>瀑布就在这样的环境中诞生了出来。当编辑,编译,测试的循环周期需要一整天的时候，我们需要进行大量的计划和检查。在24小时的循环周期内做TDD和重构是不现实的！</p>
<p>但是“风水”一直在转动。今天的大多数程序员都没有深切体会到摩尔定律的指数发展。我们从1970年的24小时循环周期，到1980年的60分钟循环周期，到1990年的十分钟循环周期，再到2000年的10秒循环周期。到2005年，大多数程序员的循环周期不到1秒。</p>
<p>敏捷就在这样的环境中诞生了出来。敏捷是对1960年代快速周转，高频反馈开发策略的一种历史回归，不过这次我们有了更强大的机器，更强大的语言和工具以及更大规模的项目。</p>
<p>敏捷也可以说是从历史的灰烬中浴火重生而来的。瀑布虽然在70年代和80年代是很有必要的，但却令人非常痛苦。在那几十年中，我们学会了哪些事情是不该做的。因此，当敏捷在90年代末出现时，它也承载着之前那段黑暗时期所积累的教训。</p>
<p>敏捷却也不仅仅是对短反馈周期的历史回归。敏捷在短反馈周期的基础之上增强了纪律。诸如测试，重构，结对编程和高度自动化之类的实践。敏捷确实让我们从六十年代的策略中前进了。</p>
<p>不过前进的方向是什么？敏捷革命改进了什么？</p>
<p>敏捷革命关注的是相对较<em>小</em>的团队如何开发相对较<em>小</em>的软件项目。请注意，我强调了“<em>小</em>”这个字。</p>
<p>敏捷团队擅长于创建十万行左右的软件系统。十万行已经可以做很多事情了。对于许多公司而言，一两个敏捷团队足以满足他们。</p>
<p>另一方面，如果您需要创建一个千万行代码的系统，那么一个敏捷团队就不够了。您需要大约一百个团队来构建千万代码行级别的系统。</p>
<p>但是，您如何管理一百个敏捷团队？您如何给他们提供用户故事？您如何协调它们之间的接口？您如何在那千万行代码中创造边界而让团队可以彼此独立工作？</p>
<p>以及您如何以“敏捷”的方式做到这一点？ （这才是真正的问题）</p>
<p><strong>答案是：你不能！</strong></p>
<p>我们人类非常擅长于建设大型项目。长久以来我们就知道如何做这件事。</p>
<p><img src="https://blog.cleancoder.com/assets/pyramids.jpg" alt="alt img"></p>
<p>想想我们人类完成过的真正巨大的项目吧。</p>
<ul>
<li>阿波罗登月：我们把人送到月球上！</li>
<li>诺曼底战役：我们在50英里坚固的防线上，以156000名士兵入侵了诺曼底。</li>
<li>我们拥有支持80亿人口的世界经济。</li>
<li>全球各地都有庞大的网络，您可以在树林里徒步时在手机上读这篇文章！</li>
<li>你要买东西吗？按几下手机，明天甚至今天就会有人来送快递。</li>
<li>我们将红色跑车驶入了太阳轨道。</li>
</ul>
<p>应该不用说更多例子了。我们人类确实非常擅长做大事。</p>
<p>那为什么我们对大型软件会有所忧虑呢？我们已经知道如何构建大型软件了。我们已经这样做了50多年或更久了。“大”的部分实际上从来不是问题所在。我们用敏捷解决的问题是关于“<strong>小</strong>”的那部分。我们之前不知道如何搞定的，是做<strong>小</strong>型项目。</p>
<p>我们一直都知道如何做大型项目。那就是分而治之。敏捷解决了其中“<strong>小</strong>”的那一部分。敏捷与“<strong>大</strong>”的部分无关。</p>
<p>但是，但是，但是，但是……平等主义！拒绝计划和命令与控制！敏捷！</p>
<p><strong>无稽之谈！</strong></p>
<p>敏捷不是平等主义。敏捷不是拒绝计划，也不是拒绝命令和控制。事实上，敏捷是命令和控制体系中最小单元的体现：战术小队。</p>
<p>是的，在层级结构的末端，命令和控制不再有效。一小组人可以通过大量反馈和激烈的沟通在较短的周期内工作，以实现目标。这就是一支敏捷的团队。在这个级别上，严格的命令和控制是极为有害的。但是在此级别之上，命令和控制就变得有必要了。在层级上越往上走，这种效应就越明显。没有大量的命令和控制，就无法设计，建造，生产和销售数以亿部的iPhone。</p>
<p>市场上有很多种大规模敏捷的玩法。有关该主题的书籍和博客也有很多。也有人成立了咨询公司，专门为大公司做大规模敏捷的转型。这没有什么不好的。</p>
<p>这些大规模敏捷方法所讲的策略和技术都没有错。只不过，它们不是敏捷的。它们与敏捷无关。它们是在人类几千年来用于完成重大工作的策略和技术的基础上加了点敏捷的“味道”。</p>
<p>这点味道来自敏捷使用的词汇和概念。增添些口味没有错–没问题。如果您喜欢使用敏捷中的词语和概念，那就尽管去用。但是不要过分关注它的“敏捷性”。一旦你要做大规模的事情，就离开了敏捷的领域。希望您的各个开发小组正在使用敏捷；但是整个大规模项目并不是敏捷的。</p>
<p>因为敏捷是专注于做”<strong>小</strong>“事情的。</p>
]]></content>
      <tags>
        <tag>Uncle Bob</tag>
      </tags>
  </entry>
  <entry>
    <title>[译] FP vs OO</title>
    <url>/blog/2019/11/03/fp-vs-oo/</url>
    <content><![CDATA[<p>原文地址：<a href="https://blog.cleancoder.com/uncle-bob/2018/04/13/FPvsOO.html" target="_blank" rel="noopener">https://blog.cleancoder.com/uncle-bob/2018/04/13/FPvsOO.html</a></p>
<p>原作者：Robert C. Martin (Uncle Bob)</p>
<p>在过去的几年中，我通过与人结对来学习函数式编程，他们中的很多人表达了反对OO的偏见。他们经常会说：“啊，这太像对象了。”</p>
<p>他们会这样说是因为他们认为FP和OO在某种程度上是互斥的。许多人似乎认为程序FP的程度等同于其非OO的程度。我认为这种观点是学习新事物的自然结果。</p>
<p>当我们采用一种新技术时，我们通常倾向于避开以前使用的旧技术。这很自然，因为我们认为新技术“更好”，因此旧技术就一定是“更糟”的。</p>
<p>在此博客中，我将说明OO和FP是正交的，但它们并不互斥。一个好的函数式程序可以（并且应该）是面向对象的。而且一个好的面向对象程序可以（并且应该）是函数式的。在此之前，我们必须非常谨慎地给FP和OO这两个词语下个定义。</p>
<h1 id="什么是OO？"><a href="#什么是OO？" class="headerlink" title="什么是OO？"></a>什么是OO？</h1><p>我将在这里采取非常还原主义的立场。OO有许多有效的定义，涵盖了丰富的概念，原理，技术，模式和哲学。在这里，我将忽略所有这些内容，而将重点放在最基础的东西上。我采取这种还原主义的原因是，很多围绕着OO的丰富特性实际上根本不是OO所特有的，而是整体软件开发丰富性的一部分。在这里，我将重点介绍面向对象不可分割的那部分。</p>
<p>看看以下两个表达式：</p>
<figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>：f(o)<span class="comment">;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">2</span>：o.f()<span class="comment">;</span></span></pre></td></tr></table></figure>

<p>有什么区别？</p>
<p>显然，没有实际的语义差异。差异完全在语法上。但是一个看起来是FP的，另一个看起来是OO的。这是因为我们会推断表达式2具有特殊的语义行为，同时我们推断表达式1不具有这种特殊语义行为。这种特殊语义行为就是：多态性。</p>
<p>当我们看到表达式1时，我们看到名为f的函数被应用在了o上。我们推断只有一个名为f的函数，并且它可能不是围绕着o的标准函数中的一员。</p>
<p>另一方面，当我们看到表达式2时，我们看到一个名为f的消息被发送给了一个名为o的对象。我们推测可能还有其他类型的对象可以接受该消息f，因此我们不知道被调用的f具体是哪一个。<strong>其行为取决于o的类型，即f是多态的。</strong></p>
<p>对多态性的这种预期是OO编程的本质。这是还原论的定义；它与OO密不可分。没有多态性的OO不是OO。OO的所有其他属性，例如封装的数据，绑定到该数据的方法，甚至继承，与表达式1的关系要比与表达式2的关系更多。</p>
<p>C和Pascal程序员（甚至在某种程度上甚至包括Fortran和Cobol程序员）都创建了包含封装函数和数据结构的系统。要创建和使用这种封装的结构并不一定非得需要OO语言。封装，甚至简单的继承，在此类语言中都是显而易见且自然的。（在C和Pascal中比其他更自然。）</p>
<p>因此，真正将OO程序与非OO程序区分开的是多态性。</p>
<p>您可能会说可以通过在f内部使用switch语句或if/else来实现多态。的确如此，因此我必须向OO添加一个约束。</p>
<blockquote>
<p>多态机制一定不能创建从调用方到被调用方的源码依赖关系。</p>
</blockquote>
<p>为了解释这一点，请再次看看上文的两个表达式。表达式1：f(o)似乎对f函数的源码有依赖。我们之所以如此推断是因为我们推断只有一个f，所以调用者必须认识被调用者。</p>
<p>但是，当我们看表达式2时，从o.f()我们推断出一些不同的东西。我们知道可能会有很多个f的实现，而且我们不知道真正要被调用到的是其中哪个。因此表达式2对于f函数的源码没有依赖。</p>
<p>具体来说，这意味着包含对函数进行多态调用的模块（源文件）绝对不能以任何方式引用包含这些函数实现的模块（源文件）。不可以有任何include或use或require或任何其它这样的声明使得一个源文件依赖另一个。</p>
<p>因此，我们对OO的简化定义是：</p>
<blockquote>
<p>调用者的源码对于被调用者的源码不产生依赖的一种动态多态技巧。</p>
</blockquote>
<h1 id="什么是FP？"><a href="#什么是FP？" class="headerlink" title="什么是FP？"></a>什么是FP？</h1><p>同样，我将采用还原主义。FP具有悠久的历史和传统，可追溯到软件之外的其他领域。FP范式里存在很多原理，技术，定理，哲学和概念。我将忽略所有这些内容，直接进入将FP与任何其他范式区分开的不可分割的属性。简而言之，就是：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">当 a==b 时 f<span class="comment">(a)</span>==f<span class="comment">(b)</span></span></pre></td></tr></table></figure>

<p>在函数式程序中，每次调用同一个函数并给出同一个参数时，都会得到相同的结果。无论程序执行了多长时间。这叫做引用透明性。</p>
<p>这意味着函数f不可以更改任何影响函数f行为方式的全局状态。而且，如果我们说函数f可以代表系统中的所有函数 – 系统中的所有函数都必须是引用透明的 – 那么系统中的任何函数都无法改变任何全局状态。系统中的任何函数都无法执行任何操作，来导致系统中的另一个函数对相同的输入返回不同的值。</p>
<p>其更深的含义是，任何命名值都无法更改。也就是说，不能有赋值运算符。</p>
<p>现在，如果您仔细地考虑一下，您可能会得出这样的结论：仅由引用透明的函数组成的程序根本无法执行任何操作-因为任何有用的系统行为都会改变某些事物的状态。即使只是打印机或显示器的状态。但是，如果我们从引用透明性约束中排除硬件以及外界的任何元素，那么事实证明我们确实可以创建非常有用的系统。</p>
<p>诀窍当然是递归。考虑一个以state数据结构作为参数的函数。此参数包含函数工作需要的所有状态信息。该函数将创建一个新的state，里面包含更新过的值。该函数做的最后一件事就是调用它自己并把新的state作为参数传递进去。</p>
<p>这是函数式程序可以用来跟踪内部状态的变化而无需真正改变任何内部状态的简单技巧之一。</p>
<p>因此，函数式编程的简化定义为：</p>
<blockquote>
<p>引用透明 – 没有重新赋值。</p>
</blockquote>
<h1 id="FP-vs-OO"><a href="#FP-vs-OO" class="headerlink" title="FP vs OO"></a>FP vs OO</h1><p>现在OO和FP社区都要向我开炮了。还原主义不是赢得朋友的好方法。但这有时很有用。我认为有必要在似乎正在传播的FP vs OO的迷因上说两句。</p>
<p>显然，我选择的两个归约定义是完全正交的。多态和引用透明之间没有任何关系。它们之间没有交集。</p>
<p>但是正交并不意味着相互排斥（问问麦克斯韦就知道了）。建立同时使用动态多态性和引用透明性的系统是完全可能的。不仅可能，而且是可取的！</p>
<p>为什么是可取的？二者各自独立可取，合一仍可取！我们希望系统具有动态多态性，为了解耦。依赖关系可以跨架构边界反转。可以使用Mocks and Fakes和其他类型的Test Doubles进行测试。可以在不强制更改其他模块的情况下修改模块。这使得系统更易于更改和改进。</p>
<p>我们还希望系统具有引用透明性，为了可预测性。无法更改内部状态使系统更易于理解，更改和改进。它大大减少了竞态和其他并发更新问题的机会。</p>
<p>底线是：</p>
<p>没有FP vs OO。</p>
<p>FP和OO可以很好地合作。这两个属性都是现代系统所希望具有的。同时基于OO和FP原理构建的系统将最大限度地提高灵活性，可维护性，可测试性，简单性和健壮性。排斥一个赞成另一个只会削弱系统的结构。</p>
]]></content>
  </entry>
  <entry>
    <title>被诅咒的与被祝福的</title>
    <url>/blog/2019/06/20/zxz-swd/</url>
    <content><![CDATA[<p>身陷南洋囹圄，心系故园风物，无以聊赖之中，撰文权作慰藉。</p>
<h1 id="从Neal-Ford的一段话讲开去"><a href="#从Neal-Ford的一段话讲开去" class="headerlink" title="从Neal Ford的一段话讲开去"></a>从Neal Ford的一段话讲开去</h1><p>前段时间看了Neal Ford的一个演讲，<a href="https://player.oreilly.com/videos/9781491901137" target="_blank" rel="noopener">原视频</a></p>
<p>其中一段讲到了传统工程与软件工程的区别。</p>
<p>於我心甚有戚戚焉。</p>
<p>译如下：</p>
<blockquote>
<p>软件工程和传统工程是截然不同的。</p>
</blockquote>
<blockquote>
<p>在传统工程的世界中，当你进入施工阶段时，你基本上是在给原子塑形，原子结构是很难重构的。一旦你将它们塑造成特定的形状，它们往往会保持这种形状，很难将它们变成其他形状。除非你熔化它们再回炉重造。因此，制造物理物品是一个非常昂贵的过程。这就意味着，我们会希望在施工开始之前正确做出设计。</p>
</blockquote>
<blockquote>
<p>但在软件世界中，我们不是在铸造原子。我们在比特（bit）的世界里，这些比特（bit）都非常具有可塑性。对比特（bit）进行更改非常容易。我们来比较一下，传统工程中有施工，软件世界中与之对应的是什么呢？那就是是编译和部署。编译和部署实际上是我们所设计的东西在现实世界中的表征，电子沿导电线路移动，对现实世界施加影响力。这就表明，对于我们软件世界而言，我们的设计蓝图就是我们的源代码。</p>
</blockquote>
<blockquote>
<p>在传统工程业中，最昂贵的部分是施工阶段。在桥梁和其他种类的物理制造中存在许多预测性数学，这样做的原因并不是为了获得更安全的结构，虽说它在客观上确实会达成这一点。初衷其实是为了节约成本。造完了一座桥梁，然后让重物碾压过去，观察桥梁是否会崩塌，从而来确认桥梁质量达标与否，这样做，万一垮塌了，这成本就实在是太昂贵了。所以你需要可预测性，因为制造这些东西太贵了。</p>
</blockquote>
<blockquote>
<p>但在软件世界中，因为施工过程对应的是编译和部署，所以改变的成本非常低，可以随时进行。我们甚至不必为此操心，每当我们停止打字时，我们的计算机内都会涌现出一小群精灵把我们设计的东西给制造出来，以便我们能够以一种逼真的方式对其进行测试。</p>
</blockquote>
<blockquote>
<p>所以Reeves先生说，“鉴于软件设计相对容易并且基本上可以免费构建，一个不足为奇的启示是，软件设计往往非常庞大和复杂。”</p>
</blockquote>
<blockquote>
<p>我相信软件是现在人类思维创造出来的最复杂的东西，有几个原因导致它们如此复杂，其中一个原因是物理世界中存在许多有用的约束，而软件的世界中这样的约束太少。</p>
</blockquote>
<hr>
<p>蓝图设计对应源码编写，建造施工对应编译部署，这着实有趣。</p>
<p>我们使用建筑这个隐喻来关照软件的时候，通常都是把架构设计对照到蓝图上去，而把源码编写对照到建造施工上去。</p>
<p>而Neal Ford不是这样使用建筑隐喻的。</p>
<p><img src="http://i1.fuimg.com/691355/770eef4285d59e81.png" alt="Markdown"></p>
<p>可以看出来，Neal Ford这个隐喻在落地程度上更进一层。</p>
<h1 id="牛老爵爷的诅咒"><a href="#牛老爵爷的诅咒" class="headerlink" title="牛老爵爷的诅咒"></a>牛老爵爷的诅咒</h1><p>Neal Ford这段话的内容非常丰富。</p>
<p>除了上述隐喻之外，还有桥梁不能像软件一样测试，软件如此复杂的原因是我们缺乏真实世界中的物理限制。</p>
<p>诚哉斯言，与土木工程师比起来，我们软件工程师确实是具有非常奢侈的环境。这个代码怎样？跑跑测试就知道了。</p>
<p>能想象土木工程师说这种话吗？</p>
<p>老板娘: 那栋楼设计搞的怎么样了？有没有仔细的检查过？</p>
<p>工程师: 我觉得没问题，直接施工建造出来看看就好了。</p>
<p>老板娘: 好，我相信你，既然你这么有信心，那就来吧。</p>
<p><img src="http://i2.tiimg.com/691355/b18dcdf9861e98bb.png" alt="Markdown"></p>
<p><img src="http://i2.tiimg.com/691355/809c6a1b7c0c142f.png" alt="Markdown"></p>
<p>工程师: 😳 😳 😳</p>
<p>老板娘: 请问您信心的来源是啥？有依据吗？你这么乱搞，我得陪多少钱？</p>
<p>工程师: 这是个小工程，有点粗心了。另一个，那个大桥的项目，我一定用心！</p>
<p><img src="http://i2.tiimg.com/691355/0373758da912bbbd.png" alt="Markdown"></p>
<p>老板娘: 合龙这部分，你是不是忘了设计喜鹊了？你打算让牛郎织女自己组成一个平行四边形吗？</p>
<p>工程师: 卒</p>
<p>老板娘: 破产</p>
<p>一旦发现失误，拆掉重造太贵，土木工程师没法像我们软件工程师一样动不动就跑测试，动不动就debug，只能事先把设计做好。</p>
<p>不过，也正是由于牛顿老爵爷设下了诅咒，土木工程师被逼练就了严谨的作风，扎实的推算能力。</p>
<p>（此处用牛老爵爷代表包括麦克斯韦在内的诸多先贤）</p>
<h1 id="蛮荒西部-牛顿管不了-图灵没来得及立法"><a href="#蛮荒西部-牛顿管不了-图灵没来得及立法" class="headerlink" title="蛮荒西部 牛顿管不了 图灵没来得及立法"></a>蛮荒西部 牛顿管不了 图灵没来得及立法</h1><p>牛爷的诅咒持久而坚固，不过，还是有它管不到的法外之地。</p>
<p>引力，电磁力，强力，弱力，你们能耐程序员何？程序员就是要任性地玩弄bits。</p>
<p>祖师爷死得早，一口苹果啃完，留待我等程序员飘零纵欲至今。</p>
<p>福兮？祸兮？</p>
<h1 id="Robert-C-Martin：还是要设立并遵守基本法"><a href="#Robert-C-Martin：还是要设立并遵守基本法" class="headerlink" title="Robert C. Martin：还是要设立并遵守基本法"></a>Robert C. Martin：还是要设立并遵守基本法</h1><p>无独有偶。最近看了《架构整洁之道》这本书。作者Robert C. Martin讲述了编程范式的发展历史，字里行间表达了与Neal Ford颇为神似的一些观点。</p>
<p>摘抄如下：</p>
<blockquote>
<p>结构化编程是第一个普遍被采用的编程范式（但是却不是第一个被提出的），由Edsger Wybe Dijkstra于1968年最先提出。与此同时，Dijkstra还论证了使用goto这样的无限制跳转语句将会损害程序的整体结构。接下来的章节我们还会说到，也是这位Dijkstra最先主张用我们现在熟知的if/then/else语句和do/while/until语句来代替跳转语句的。我们可以将结构化编程范式归结为一句话：结构化编程对程序控制权的直接转移进行了限制和规范。</p>
</blockquote>
<blockquote>
<p>面向对象编程对程序控制权的间接转移进行了限制和规范。</p>
</blockquote>
<blockquote>
<p>函数式编程对程序中的赋值进行了限制和规范。</p>
</blockquote>
<blockquote>
<p>它们都从某一方面限制和规范了程序员的能力。没有一个范式是增加新能力的。也就是说，每个编程范式的目的都是设置限制。这些范式主要是为了告诉我们不能做什么，而不是可以做什么。</p>
</blockquote>
<blockquote>
<p>这三个编程范式分别限制了goto语句、函数指针和赋值语句的使用。那么除此之外，还有什么可以去除的吗</p>
</blockquote>
<blockquote>
<p>这些编程范式的历史知识与软件架构有关系吗？当然有，而且关系相当密切。譬如说，多态是我们跨越架构边界的手段，函数式编程是我们规范和限制数据存放位置与访问权限的手段，结构化编程则是各模块的算法实现基础。这和软件架构的三大关注重点不谋而合：功能性、组件独立性以及数据管理。</p>
</blockquote>
<hr>
<p>原文的意思非常明白，Structured programming，OO，FP都是在约束我们作为程序员的行为，给我们立下基本法，免得我们瞎搞。</p>
<p>Neal Ford: 自然界的物理定律是个好东西啊，可以限制土木工程师，让他们无法乱搞。我们软件世界里缺的就是这个。</p>
<p>Robert C. Martin: 老弟所言甚是。缺少自然的馈赠，我们就搞人为的规约。诸多编程范式都已经尝试过做这件事了。天不立法人立法。</p>
<h1 id="张信哲表示同意"><a href="#张信哲表示同意" class="headerlink" title="张信哲表示同意"></a>张信哲表示同意</h1><p>张信哲：你俩说的对，自由是个好东西，但是不能《过火》，免得过犹不及。</p>
<p><img src="http://i2.tiimg.com/691355/7f1e9d0fcf5aeaa9.png" alt="Markdown"></p>
<h1 id="工程师之荣光-演绎-归纳"><a href="#工程师之荣光-演绎-归纳" class="headerlink" title="工程师之荣光 演绎 归纳"></a>工程师之荣光 演绎 归纳</h1><blockquote>
<p>人类99%的机率都在使用归纳法，只有1%的机率使用演绎法因为演绎法需要消耗认知能量，所以默认使用归纳法。</p>
</blockquote>
<p>土木工程师要仰仗演绎推理，这是理工科之荣光。</p>
<p>此乃吾辈偏执之骄傲。</p>
<p>怎奈沦落为软件工程师，我们要重度依赖归纳才能勉强维持生活这样子。</p>
<p>代码有问题？跑一跑tests，改一改。还不行？再改改，再跑跑。仍然不行？那我可要debug了！！！</p>
<p>反复利用run tests和debug的方式来观察现象，收集现象，总结现象，反推原理。</p>
<p>这是软件工程师在脑力不断失败的绝望中不断降低自己身段的慷慨悲歌。</p>
<p>福兮？祸兮？</p>
<h1 id="测试要先写-别着急跑-debug不到万不得已最好别用"><a href="#测试要先写-别着急跑-debug不到万不得已最好别用" class="headerlink" title="测试要先写 别着急跑 debug不到万不得已最好别用"></a>测试要先写 别着急跑 debug不到万不得已最好别用</h1><p>为了保护我们宝贵的智商与荣光，在此作出如下建议。</p>
<p>测试写完，然后去写实现代码的时候要尽力去争取一遍过。</p>
<p>在试图用run test去验证刚刚写完的实现代码之前，别着急，可以先回头把代码捋一遍，用肉脑跑一遍先。</p>
<p>不到万不得已，别用debug。因为一旦你开始debug，就是认输了，认怂了。承认无法通过肉脑来演绎推理明白了。</p>
<p>不要过于骄纵，不要过量服用软件世界给我们的自由，多给自己一些人为设置的限制，向土木工程师看齐。</p>
<p>没想到吧？前面写的神神叨叨，最后来到了如此接地气的建议上。</p>
<h1 id="别误会-不是那个意思"><a href="#别误会-不是那个意思" class="headerlink" title="别误会 不是那个意思"></a>别误会 不是那个意思</h1><p>不是宣扬big upfront design。</p>
<p>不是反对short feedback loop。</p>
<p>不是反对跑测试，只是建议别着急跑，不要太过依赖跑测试，优先用肉脑，然后才跑测试。</p>
<p>不是否认debug的价值，只是希望大家尽力放慢做出要去debug的决定的速度，让子弹飞一会，给肉脑机会，让它跑一会。</p>
<p>不是否认归纳的价值，只是建议演绎与归纳不可偏废，不要过度依赖归纳而冷落演绎。</p>
<hr>
<p>谨以此文与诸君共勉。For the serious hardcore programmers。</p>
]]></content>
  </entry>
  <entry>
    <title>读屠龙术五卷，粗浅总结一下太祖的套路</title>
    <url>/blog/2019/04/11/tu-long-shu-mao/</url>
    <content><![CDATA[<p>最近快速浏览完了太祖的五卷选集。粗浅的尝试着总结一下太祖做事情和分析问题解决问题时都会运用一些什么样的套路。</p>
<h1 id="性质决定形式"><a href="#性质决定形式" class="headerlink" title="性质决定形式"></a>性质决定形式</h1><p><font size="3">革命不是请客吃饭，不是做文章，不是绘画绣花，不能那样雅致，那样从容不迫，文质彬彬，那样温良恭俭让。革命是暴动，是一个阶级推翻一个阶级的暴烈的行动。</font></p>
<hr>
<p><font size="3">大家明白，不论做什么事，不懂得那件事的情形，它的性质，它和它以外的事情的关联，就不知道那件事的规律，就不知道如何去做，就不能做好那件事。</font></p>
<hr>
<p><font size="3">中国是一个经过了一次革命的、政治经济发展不平衡的、半殖民地的大国，这是中国革命战争的第一个特点。这个特点，不但基本地规定了我们政治上的战略和战术，而且也基本地规定了我们军事上的战略和战术。</font></p>
<hr>
<p><font size="3">办法是跟着方针来的。方针是不抵抗主义的时候，一切办法都反映不抵抗主义，这个我们已经有了六年的教训。方针如果是坚决抗战，那就非实行合乎这个方针的一套办法不可，非实行这八大纲领不可。</font></p>
<h1 id="要究根源"><a href="#要究根源" class="headerlink" title="要究根源"></a>要究根源</h1><p><font size="3">开会时要使到会的人尽量发表意见。有争论的问题，要把是非弄明白，不要调和敷衍。一次不能解决的，二次再议（以不妨碍工作为条件），以期得到明晰的结论。</font></p>
<hr>
<p><font size="3">战争问题中的唯心论和机械论的倾向，是一切错误观点的认识论上的根源。他们看问题的方法是主观的和片面的。或者是毫无根据地纯主观地说一顿；或者是只根据问题的一侧面、一时候的表现，也同样主观地把它夸大起来，当作全体看。 … … 反对战争问题中的唯心论和机械论的倾向，采用客观的观点和全面的观点去考察战争，才能使战争问题得出正确的结论。</font></p>
<h1 id="抓大放小"><a href="#抓大放小" class="headerlink" title="抓大放小"></a>抓大放小</h1><p><font size="3">有些同志的批评不注意大的方面，只注意小的方面。他们不明白批评的主要任务，是指出政治上的错误和组织上的错误。至于个人缺点，如果不是与政治的和组织的错误有联系，则不必多所指摘，使同志们无所措手足。而且这种批评一发展，党内精神完全集注到小的缺点方面，人人变成了谨小慎微的君子，就会忘记党的政治任务，这是很大的危险。</font></p>
<hr>
<p><font size="3">据衡山的调查，贫农领袖百人中八十五人都变得很好，很能干，很努力。只有百分之十五，尚有些不良习惯。这只能叫做“少数不良分子”，决不能跟着土豪劣绅的口白，笼统地骂“痞子”。要解决这“少数不良分子”的问题，也只能在农会整顿纪律的口号之下，对群众做宣传，对他们本人进行训练，把农会的纪律整好，决不能随便派兵捉人，损害贫农阶级的威信，助长土豪劣绅的气势。这一点是非常要注意的。</font></p>
<hr>
<p><font size="3">要充分相信青年人，绝大多数是会胜任的。个别人可能不称职，也不用怕，以后可以改选掉。这样做，基本方向是不会错的。</font></p>
<h1 id="注重调查研究而后有解决方案"><a href="#注重调查研究而后有解决方案" class="headerlink" title="注重调查研究而后有解决方案"></a>注重调查研究而后有解决方案</h1><p><font size="3">你对于那个问题不能解决吗？那末，你就去调查那个问题的现状和它的历史吧！你完完全全调查明白了，你对那个问题就有解决的办法了。一切结论产生于调查情况的末尾，而不是在它的先头。只有蠢人，才是他一个人，或者邀集一堆人，不作调查，而只是冥思苦索地“想办法”，“打主意”。须知这是一定不能想出什么好办法，打出什么好主意的。换一句话说，他一定要产生错办法和错主意。</font></p>
<hr>
<p><font size="3">调查就像“十月怀胎”，解决问题就像“一朝分娩”。</font></p>
<hr>
<p><font size="3">对于国内和国际的政治、军事、经济、文化的任何一方面，我们所收集的材料还是零碎的，我们的研究工作还是没有系统的。二十年来，一般地说，我们并没有对于上述各方面作过系统的周密的收集材料加以研究的工作，缺乏调查研究客观实际状况的浓厚空气。“闭塞眼睛捉麻雀”，“瞎子摸鱼”，粗枝大叶，夸夸其谈，满足于一知半解，这种极坏的作风，这种完全违反马克思列宁主义基本精神的作风，还在我党许多同志中继续存在着。</font></p>
<h1 id="不要盲从"><a href="#不要盲从" class="headerlink" title="不要盲从"></a>不要盲从</h1><p><font size="3">我们说上级领导机关的指示是正确的，决不单是因为它出于“上级领导机关”，而是因为它的内容是适合于斗争中客观和主观情势的，是斗争所需要的。</font></p>
<hr>
<p><font size="3">我们说马克思主义是对的，决不是因为马克思这个人是什么“先哲”，而是因为他的理论，在我们的实践中，在我们的斗争中，证明了是对的。</font></p>
<hr>
<p><font size="3">马克思说的武装起义之后一刻也不应该停止进攻，这是说乘敌不备而突然起义的群众，应该不让反动的统治者有保守政权或恢复政权的机会，趁此一瞬间把国内反动的统治势力打个措手不及，而不要满足于已得的胜利，轻视敌人，放松对于敌人的进攻，或者畏缩不前，坐失消灭敌人的时机，招致革命的失败。这是正确的。然而不是说，敌我双方已在军事对抗中，而且敌人是优势，当受敌人压迫时，革命党人也不应该采取防御手段。如果这样想，那就是第一号的傻子。</font></p>
<hr>
<p><font size="3">不要迷信。中国人也好，外国人也好，死人也好，活人也好，对的就是对的，不对的就是不对的，不然就叫做迷信。要破除迷信。不论古代的也好，现代的也好，正确的就信，不正确的就不信，不仅不信而且还要批评。这才是科学的态度。</font></p>
<h1 id="注重工作细节"><a href="#注重工作细节" class="headerlink" title="注重工作细节"></a>注重工作细节</h1><p><font size="3">每到一处，壁上写满了口号。惟缺绘图的技术人材，请中央和两省委送几个来。</font></p>
<hr>
<p><font size="3">大家要努力去发展农业和手工业的生产，多造农具，多产石灰，使明年的收获增多，恢复钨砂、木头、樟脑、纸张、烟叶、夏布、香菇、薄荷油等特产过去的产量，并把它们大批地输出到白区去。</font></p>
<hr>
<p><font size="3">我郑重地向大会提出，我们应该深刻地注意群众生活的问题，从土地、劳动问题，到柴米油盐问题。妇女群众要学习犁耙，找什么人去教她们呢？小孩子要求读书，小学办起了没有呢？对面的木桥太小会跌倒行人，要不要修理一下呢？许多人生疮害病，想个什么办法呢？一切这些群众生活上的问题，都应该把它提到自己的议事日程上。</font></p>
<h1 id="不要用静态的眼光看问题"><a href="#不要用静态的眼光看问题" class="headerlink" title="不要用静态的眼光看问题"></a>不要用静态的眼光看问题</h1><p><font size="3">革命的道路，同世界上一切事物活动的道路一样，总是曲折的，不是笔直的。革命和反革命的阵线可能变动，也同世界上一切事物的可能变动一样。</font></p>
<hr>
<p><font size="3">国民党营垒中，在民族危机到了严重关头的时候，是要发生破裂的。</font></p>
<hr>
<p><font size="3">日本是小国，地小、物少、人少、兵少，中国是大国，地大、物博、人多、兵多这一个条件，于是在强弱对比之外，就还有小国、退步、寡助和大国、进步、多助的对比，这就是中国决不会亡的根据。强弱对比虽然规定了日本能够在中国有一定时期和一定程度的横行，中国不可避免地要走一段艰难的路程，抗日战争是持久战而不是速决战；然而小国、退步、寡助和大国、进步、多助的对比，又规定了日本不能横行到底，必然要遭到最后的失败，中国决不会亡，必然要取得最后的胜利。</font></p>
<h1 id="先行动起来"><a href="#先行动起来" class="headerlink" title="先行动起来"></a>先行动起来</h1><p><font size="3">革命战争是民众的事，常常不是先学好了再干，而是干起来再学习，干就是学习。</font></p>
<h1 id="批判的学习"><a href="#批判的学习" class="headerlink" title="批判的学习"></a>批判的学习</h1><p><font size="3">对于外国文化，排外主义的方针是错误的，应当尽量吸收进步的外国文化，以为发展中国新文化的借镜；盲目搬用的方针也是错误的，应当以中国人民的实际需要为基础，批判地吸收外国文化。苏联所创造的新文化，应当成为我们建设人民文化的范例。对于中国古代文化，同样，既不是一概排斥，也不是盲目搬用，而是批判地接收它，以利于推进中国的新文化。</font></p>
<hr>
<p><font size="3">后起而且发展得很快的帝国主义国家，即德日两国的军事家中，积极地鼓吹战略进攻的利益，反对战略防御。这种思想，是根本不合于中国革命战争的。德日帝国主义的军事家们指出防御的一个重要的弱点是不能振奋人心，反而使人心动摇。这是说的阶级矛盾剧烈，而战争的利益仅仅属于反动的统治阶层乃至反动的当权政派的那种国家。我们的情况不同。在保卫革命根据地和保卫中国的口号下，我们能够团结最大多数人民万众一心地作战</font></p>
<h1 id="不耻下问，兼听则明"><a href="#不耻下问，兼听则明" class="headerlink" title="不耻下问，兼听则明"></a>不耻下问，兼听则明</h1><p><font size="3">其中还有些问题没有弄清楚，需要先征求下级的意见。我们切不可强不知以为知，要“不耻下问”[2]，要善于倾听下面干部的意见。先做学生，然后再做先生；先向下面干部请教，然后再下命令。</font></p>
<hr>
<p><font size="3">你们对上述计划意见如何？这个计划有何缺点？执行有何困难？统望考虑电告。</font></p>
<hr>
<p><font size="3">像卫立煌、翁文灏这样的有爱国心的国民党军政人员，我们应当继续调动他们的积极性。就是那些骂我们的，像龙云、梁漱溟、彭一湖之类，我们也要养起来，让他们骂，骂得无理，我们反驳，骂得有理，我们接受。这对党，对人民，对社会主义比较有利。</font></p>
<h1 id="定量分析"><a href="#定量分析" class="headerlink" title="定量分析"></a>定量分析</h1><p><font size="3">胸中有“数”。这是说，对情况和问题一定要注意到它们的数量方面，要有基本的数量的分析。任何质量都表现为一定的数量，没有数量也就没有质量。我们有许多同志至今不懂得注意事物的数量方面，不懂得注意基本的统计、主要的百分比，不懂得注意决定事物质量的数量界限，一切都是胸中无“数”，结果就不能不犯错误。</font></p>
<h1 id="分清屁股（立场）"><a href="#分清屁股（立场）" class="headerlink" title="分清屁股（立场）"></a>分清屁股（立场）</h1><p><font size="3">十月革命推翻了资产阶级，这在世界上是个新鲜事情。对这个革命，国际资产阶级不管三七二十一，骂的多，总是说不好。俄国资产阶级是个反革命阶级，那个时候，国家资本主义这一套他不干，他怠工，破坏，拿起枪来打。俄国无产阶级没有别的办法，只好干掉他。这就惹火了各国资产阶级，他们就骂人。我们这里对待民族资产阶级比较缓和一点，他就舒服一点，觉得还有些好处。现在艾森豪威尔威尔、杜勒斯不让美国的新闻记者到中国来，实际上就是承认我们的政策有这个好处。如果我们这里是一塌糊涂，他们就会放那些人来，横直是写骂人文章。他们就是怕写出来的文章不专门骂人，还讲一点好话，那个事情就不好办。</font></p>
<hr>
<p><font size="3">江苏作了一个调查，有的地区，县区乡三级干部中间，有百分之三十的人替农民叫苦。后头一查，这些替农民叫苦的人，大多数是家里比较富裕，有余粮出卖的人。这些人的所谓“苦”，就是有余粮。所谓“帮助农民”、“关心农民”，就是有余粮不要卖给国家。这些叫苦的人到底代表谁呢？他们不是代表广大农民群众，而是代表少数富裕农民。</font></p>
<hr>
<p><font size="3">“人性论”。有没有人性这种东西？当然有的。但是只有具体的人性，没有抽象的人性。在阶级社会里就是只有带着阶级性的人性，而没有什么超阶级的人性。我们主张无产阶级的人性，人民大众的人性，而地主阶级资产阶级则主张地主阶级资产阶级的人性，不过他们口头上不这样说，却说成为唯一的人性。有些小资产阶级知识分子所鼓吹的人性，也是脱离人民大众或者反对人民大众的，他们的所谓人性实质上不过是资产阶级的个人主义，因此在他们眼中，无产阶级的人性就不合于人性。现在延安有些人们所主张的作为所谓文艺理论基础的“人性论”，就是这样讲，这是完全错误的。</font></p>
<hr>
<p><font size="3">对于革命的文艺家，暴露的对象，只能是侵略者、剥削者、压迫者及其在人民中所遗留的恶劣影响，而不能是人民大众。人民大众也是有缺点的，这些缺点应当用人民内部的批评和自我批评来克服，而进行这种批评和自我批评也是文艺的最重要任务之一。但这不应该说是什么“暴露人民”。对于人民，基本上是一个教育和提高他们的问题。</font></p>
<h1 id="事物的两面性"><a href="#事物的两面性" class="headerlink" title="事物的两面性"></a>事物的两面性</h1><p><font size="3">把毒草，把非马克思主义和反马克思主义的东西，摆在我们同志面前，摆在人民群众和民主人士面前，让他们受到锻炼。不要封锁起来，封锁起来反而危险。这一条我们跟苏联的做法不同。为什么要种牛痘？就是人为地把一种病毒放到人体里面去，实行“细菌战”，跟你作斗争，使你的身体里头产生一种免疫力。</font></p>
<hr>
<p><font size="3">日本国度比较地小，其人力、军力、财力、物力均感缺乏，经不起长期的战争。日本统治者想从战争中解决这个困难问题，但同样，将达到其所期求的反面，这就是说，它为解决这个困难问题而发动战争，结果将因战争而增加困难，战争将连它原有的东西也消耗掉。</font></p>
<hr>
<p><font size="3">国民党是一个复杂的政党。它虽被这个代表大地主、大银行家、大买办阶层的反动集团所统治，所领导，却并不整个儿等于这个反动集团。它有一部分领袖人物不属于这个集团，而且被这个集团所打击、排斥或轻视。它有不少的干部、党员群众和三民主义青年团的团员群众并不满意这个集团的领导，而且有些甚至是反对它的领导的。</font></p>
<hr>
<p><font size="3">坏事也算一种经验，也有很大的作用。我们就有陈独秀、李立三、王明、张国焘、高岗、饶漱石这些人，他们是我们的教员。此外，我们还有别的教员。在国内来说，最好的教员是蒋介石。我们说不服的人，蒋介石一教，就说得服了。蒋介石用什么办法来教呢？他是用机关枪、大炮、飞机来教。还有帝国主义这个教员，它教育了我们六亿人民。一百多年来，几个帝国主义强国压迫我们，教育了我们。所以，坏事有个教育作用，有个借鉴作用。</font></p>
<hr>
<p><font size="3">军队的生产自给，在我们的条件下，形式上是落后的、倒退的，实质上是进步的，具有重大历史意义的。在形式上，我们违背了分工的原则。但是，在我们的条件下——国家贫困、国家分裂（这些都是国民党主要统治集团所造成的罪恶结果）以及分散的长期的人民游击战争，我们这样做，就是进步的了。大家看，国民党的军队面黄肌瘦，解放区的军队身强力壮。大家看，我们自己，在没有生产自给的时候，何等困难，一经生产自给，何等舒服。</font></p>
]]></content>
      <tags>
        <tag>marx</tag>
      </tags>
  </entry>
  <entry>
    <title>Desugar Scala(18) -- stackable traits</title>
    <url>/blog/2017/06/14/desugar-scala-stackable-traits/</url>
    <content><![CDATA[<p>Stackable traits是一种怎样的特性呢？</p>
<p>来举一个🌰</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">IntQueue</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">get</span></span>(): <span class="type">Int</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">put</span></span>(x: <span class="type">Int</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>定义一个IntQueue，抽象类，定义了get和put，没有实现。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BasicIntQueue</span> <span class="keyword">extends</span> <span class="title">IntQueue</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">val</span> buf = <span class="keyword">new</span> <span class="type">ArrayBuffer</span>[<span class="type">Int</span>]</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">get</span></span>() = buf.remove(<span class="number">0</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">put</span></span>(x: <span class="type">Int</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    buf += x</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>再定义一个BasicIntQueue，把上述IntQueue实现了。<br>它的实现没有什么花样，就是先进先出。</p>
<p>接下来就有意思了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">Incrementing</span> <span class="keyword">extends</span> <span class="title">IntQueue</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">abstract</span> <span class="keyword">override</span> <span class="function"><span class="keyword">def</span> <span class="title">put</span></span>(x: <span class="type">Int</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">super</span>.put(x + <span class="number">1</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">Doubling</span> <span class="keyword">extends</span> <span class="title">IntQueue</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">abstract</span> <span class="keyword">override</span> <span class="function"><span class="keyword">def</span> <span class="title">put</span></span>(x: <span class="type">Int</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">super</span>.put(<span class="number">2</span> * x)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>定义了两个trait，都扩展自IntQueue。<br>一个是把数字先加一再放进队列，另一个是先把数字加倍再放入队列。</p>
<p>要注意这里的modifier：abstract override，以及在trait中对super的调用。稍后反编译的时候可以看懂它们的真实含义。</p>
<p>那这两个trait可以怎么使用呢？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MagicQueue</span> <span class="keyword">extends</span> <span class="title">BasicIntQueue</span> <span class="keyword">with</span> <span class="title">Incrementing</span> <span class="keyword">with</span> <span class="title">Doubling</span></span></span></pre></td></tr></table></figure>

<p>定义一个MagicQueue，它扩展自BasicIntQueue，同时mixin了上面的两个trait。</p>
<p>MagicQueue它自己是一行实现代码都没有的，那么它的行为会是什么样子呢？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> queue = <span class="keyword">new</span> <span class="type">MagicQueue</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">queue.put(<span class="number">100</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">queue.get() <span class="comment">//会返回201</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">queue.put(<span class="number">500</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">queue.get() <span class="comment">//会返回1001</span></span></pre></td></tr></table></figure>

<p>可以看到，它会先把数字乘以二，然后加一再放入队列。</p>
<p>MagicQueue继承了BasicIntQueue，混入了Incrementing和Doubling，它的行为就会是先跑Doubling后跑Incrementing最后跑BasicIntQueue（从右到左依序生效）。</p>
<p>这是种很实用的语言特性，你可以写很多个不同的trait，让它们都extend IntQueue。<br>同时写很多class让它们实现IntQueue。<br>然后每一个实现了IntQueue的class都可以和任意一个或者任意多个trait随意组合应用。</p>
<p>这给语言的使用者提供了很强的composition的便利性。</p>
<p>那下面看下这个语言特性是如何实现的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">IntQueue</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">int</span> <span class="title">get</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> p0)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BasicIntQueue</span> <span class="keyword">extends</span> <span class="title">IntQueue</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ArrayBuffer&lt;Object&gt; buf;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">private</span> ArrayBuffer&lt;Object&gt; <span class="title">buf</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.buf;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">get</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> BoxesRunTime.unboxToInt(<span class="keyword">this</span>.buf().remove(<span class="number">0</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> x)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.buf().$plus$eq((Object)BoxesRunTime.boxToInteger(x));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BasicIntQueue</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.buf = (ArrayBuffer&lt;Object&gt;)<span class="keyword">new</span> ArrayBuffer();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>首先，IntQueue和BasicIntQueue反编译之后平淡无奇，一个抽象类，一个实现类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Doubling</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">void</span> chap12$Doubling$$<span class="keyword">super</span>$put(<span class="keyword">final</span> <span class="keyword">int</span> p0);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> p0)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Doubling</span>$<span class="title">class</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> Doubling $<span class="keyword">this</span>, <span class="keyword">final</span> <span class="keyword">int</span> x)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        $<span class="keyword">this</span>.chap12$Doubling$$<span class="keyword">super</span>$put(<span class="number">2</span> * x);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> $init$(<span class="keyword">final</span> Doubling $<span class="keyword">this</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>Doubling这个trait则被编译成了一个接口加一个抽象类，其中除了put之外还有一个名字有点奇怪的方法声明。<br>稍后可以看到它有什么用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Incrementing</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">void</span> chap12$Incrementing$$<span class="keyword">super</span>$put(<span class="keyword">final</span> <span class="keyword">int</span> p0);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> p0)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Incrementing</span>$<span class="title">class</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> Incrementing $<span class="keyword">this</span>, <span class="keyword">final</span> <span class="keyword">int</span> x)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        $<span class="keyword">this</span>.chap12$Incrementing$$<span class="keyword">super</span>$put(x + <span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> $init$(<span class="keyword">final</span> Incrementing $<span class="keyword">this</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>Incrementing则和Doubling是一个路数。</p>
<p>（这里出现的chap12字样是我写代码时package的名字）</p>
<p>最后揭露真相的时候到了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MagicQueue</span> <span class="keyword">extends</span> <span class="title">BasicIntQueue</span> <span class="keyword">implements</span> <span class="title">Incrementing</span>, <span class="title">Doubling</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> chap12$Doubling$$<span class="keyword">super</span>$put(<span class="keyword">final</span> <span class="keyword">int</span> x) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Incrementing$<span class="class"><span class="keyword">class</span>.<span class="title">put</span>((<span class="title">Incrementing</span>)<span class="title">this</span>, <span class="title">x</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> x)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Doubling$<span class="class"><span class="keyword">class</span>.<span class="title">put</span>((<span class="title">Doubling</span>)<span class="title">this</span>, <span class="title">x</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> chap12$Incrementing$$<span class="keyword">super</span>$put(<span class="keyword">final</span> <span class="keyword">int</span> x) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">super</span>.put(x);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MagicQueue</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Incrementing$<span class="class"><span class="keyword">class</span>.$<span class="title">init</span>$((<span class="title">Incrementing</span>)<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Doubling$<span class="class"><span class="keyword">class</span>.$<span class="title">init</span>$((<span class="title">Doubling</span>)<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>MagicQueue本身被编译成了以上的样子。</p>
<p>我们看一下它的put方法被调用时会怎样呢？</p>
<ol>
<li><p>它去调用Doubling$class.put这个静态方法，把自己和数字都传入</p>
</li>
<li><p>Doubling$class.put则会先把数字乘以二，然后把乘积传给MagicQueue的chap12$Doubling$$super$put</p>
</li>
<li><p>MagicQueue的chap12$Doubling$$super$put方法则会把MagicQueue自己的实例以及乘积都传给Incrementing$class.put这个静态方法</p>
</li>
<li><p>Incrementing$class.put则会把接收到的参数，也就是乘积，加一，然后把加和后的数字传给MagicQueue的chap12$Incrementing$$super$put</p>
</li>
<li><p>MagicQueue的chap12$Incrementing$$super$put最终把乘以二又加了一的数字传给了super.put</p>
</li>
<li><p>super.put其实就是BasicIntQueue.put了，到这里终于把数字存到ArrayBuffer里面了</p>
</li>
</ol>
<p>这样，Doubling,Incrementing,BasicIntQueue它们三个的行为就堆叠（stackable）在一起了。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>当锤子遇到钉子</title>
    <url>/blog/2016/06/04/when-hammer-meets-nail/</url>
    <content><![CDATA[<p><sub>此文标题党。更切题的标题应该叫做《如何用统计科学来黑星座》或者是《积极注册Facehub，促进统计科学蓬勃发展》 </sub></p>
<h1 id="锤子-JS-Promise"><a href="#锤子-JS-Promise" class="headerlink" title="锤子　JS Promise"></a>锤子　JS Promise</h1><p>最近对JS社区里的Promises/A+规范产生了很浓的兴趣，感觉Promise这套东西确实蛮不错的，给异步算法的编写者和异步算法的消费者之间提供了一套统一的沟通手段，也为异步算法的消费者提供了更悦目易读的代码组织方式。</p>
<p>自己把它实现了一遍:<a href="https://www.npmjs.com/package/RWPromise" target="_blank" rel="noopener">https://www.npmjs.com/package/RWPromise</a></p>
<p>然后在武汉办公室run了3次workshop:<a href="http://cuipengfei.me/blog/2016/05/15/promise/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2016/05/15/promise/</a>。</p>
<p>手里有了这么一把锤子就总想找个钉子敲一敲。总在想能去哪里找N多异步操作来让我来组织一下呢?</p>
<h1 id="钉子-《异类》"><a href="#钉子-《异类》" class="headerlink" title="钉子　《异类》"></a>钉子　《异类》</h1><p>无巧不成书，刚好最近在豆瓣上买了一本叫做《异类》的书在看:　<a href="https://read.douban.com/ebook/10580943/" target="_blank" rel="noopener">https://read.douban.com/ebook/10580943/</a></p>
<p>书中一开篇就提到了加拿大冰球运动员选拔机制中很有趣的一件轶事:</p>
<blockquote>
<p>职业青年队绝大多数球员的生日集中在1月、2月和3月，这实在有点不可思议</p>
</blockquote>
<blockquote>
<p>加拿大冰球队按年龄分组所依据的分界线是1月1日，即从1月1日到当年12月31日之间出生的球员将会被分在同一组。也就是说，一个1月1日出生的选手，是在跟许多年纪比他小的队友争夺晋级权——在青春期到来之前，由于有将近12个月的年龄差距，球员之间在生理成熟度上将会表现出巨大的差异。</p>
</blockquote>
<blockquote>
<p>正如冰球队员的成长经历一样，因年龄大几个月而显现的微弱优势会在孩子的成长过程中不断积累，最终引导孩子走向成功或不成功，自信或不自信的轨道中，其影响会延伸许多年。</p>
</blockquote>
<p>能否进入加拿大职业青年冰球队竟然和出生月份有关系，实在是太有趣了。</p>
<p>假想如果你运动天赋非常好，但是不幸出生在12月，于是不得不从小和大你将近一岁的少年运动员一起训练。<br>表现难免显得不那么出色，不受教练待见，得不到正面反馈，出场坐冷板凳……逐渐逐渐，伤仲永。</p>
<p>由此就联想到了，到TW来工作的人其出生月份是否也存在什么神奇的规律呢？</p>
<h2 id="Facehub"><a href="#Facehub" class="headerlink" title="Facehub"></a>Facehub</h2><p>生日信息哪家强？<br>Facehub帮你忙，它可以查到每个注册用户的生日(只有月日，没有年)。</p>
<p>（注：Facehub是ThoughtWorks公司内部的一个社交网站，只对员工开放注册。用户可以在该网站了解其他同事的信息。）</p>
<p>作为一个内部推广的网站，FaceHub在公司内邮件组里总是铺天盖地、见缝插针地作广告宣传，每次看到觉得审美都疲劳了，不过到了需要的时候第一个就想到了它。<br>广告的作用，诚不我欺。</p>
<p>我先目测，Facehub用户不会超过500人。<br>并且用户的id是连续的自增数字，这就很好办了，我只要构造N多获取用户数据的GET请求就好了。<br>这N多的GET请求，肯定不能一个一个慢慢发送，那就需要异步请求了，这就是钉子啊！<br>这就是大量的需要被组织的异步操作啊！<br>钉锤终于有机会合体了。</p>
<p>于是我就写了这么个脚本:　<a href="https://github.com/cuipengfei/Spikes/blob/master/js/birth/birth.js" target="_blank" rel="noopener">https://github.com/cuipengfei/Spikes/blob/master/js/birth/birth.js</a></p>
<p>其中使用Promise把大量的异步Http请求组织起来，然后统计其结果。</p>
<p>(注:如果需要自己跑这个脚本，需要把第七行的token替换成你自己的合法值，如果您没有账号的话就无需尝试了哦)</p>
<p>以下是统计结果，获取到用户数量348：</p>
<iframe width="600" height="400" src="https://live.amcharts.com/FhYjd/embed/" frameborder="0"></iframe>

<p>由此可以看到，来TW工作的人，11月，10月，8月出生的最多，5月出生的最少。</p>
<p>由于样本量不够大(只有348人)，所以统计数据的含义还不太好说。</p>
<h2 id="下面是我胡乱猜测的、不科学的、不严谨的理论"><a href="#下面是我胡乱猜测的、不科学的、不严谨的理论" class="headerlink" title="下面是我胡乱猜测的、不科学的、不严谨的理论"></a>下面是我胡乱猜测的、不科学的、不严谨的理论</h2><blockquote>
<p>我们小学入学时卡生日印象中是用8月卡的，那么这就和加拿大的冰球挑队员卡1月类似。</p>
</blockquote>
<blockquote>
<p>那这样，小学入学后，优势的积累就应该会倾向于8，9，10这几月的学生(类比冰球青年职业队队员集中分布在1，2，3这几个月)。</p>
</blockquote>
<blockquote>
<p>这几个月生日的小学生从小积累优势，并在成年后把优势携带到了求职之中去。</p>
</blockquote>
<blockquote>
<p>那为什么我们统计出来是8，10，11月份的人份额多呢?怎么不是8，9，10这几个月呢?统计出来的数据为什么把9月给跳过去了呢？可怜的9月得罪谁了呢？</p>
</blockquote>
<blockquote>
<p>我猜是由于9月这群人会成为某种星座的几率实在是太高了，被命运之神无情的给镇压掉了。:(抱歉</p>
</blockquote>
<p>既然已经有了这份代码，可以统计出生月份，索性再统计些其他侧面的数据:</p>
<iframe width="600" height="400" src="https://live.amcharts.com/GI4ZG/embed/" frameborder="0"></iframe>

<p>可见天秤和狮子座的最多，金牛座的最少。和上面的月份排名差异不大。</p>
<p>仔细看下的话，发现我只得罪了7.4%的人，好欣慰:)</p>
<iframe width="600" height="400" src="https://live.amcharts.com/JjYjF/embed/" frameborder="0"></iframe>

<p>看入职日期，大学毕业和跳槽的高峰期很凸显。9，10月最少，这倒不算什么惊人的发现。</p>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>由于只统计了348人的信息，所以以上猜测仅供娱乐，请勿当真。</p>
<p>请大家积极注册Facehub，并填写真实的生日信息，以促进统计科学的繁荣发展。<br>（如果您无法注册，可以考虑投一份简历，然后再来促进统计科学的繁荣发展哦）</p>
]]></content>
  </entry>
  <entry>
    <title>自己动手实现Promises/A+规范</title>
    <url>/blog/2016/05/15/promise/</url>
    <content><![CDATA[<p>Promise并不是一个新的概念,它已经有将近30年的历史.</p>
<p>其早期的雏形还有里氏替换原则的提出者Barbara Liskov的贡献在其中.</p>
<p><a href="https://en.wikipedia.org/wiki/Futures_and_promises#History" target="_blank" rel="noopener">https://en.wikipedia.org/wiki/Futures_and_promises#History</a></p>
<p>而Promises/A+这个规范的出现,则为JavaScript世界中众多Promise实现库提供了一套统一的API和交互机制.</p>
<p>Promises/A+提供了配套的测试集:<a href="https://github.com/promises-aplus/promises-tests" target="_blank" rel="noopener">https://github.com/promises-aplus/promises-tests</a>.</p>
<p>其中共有872个测试,如果你的实现能够让全部测试绿起来,则可以认为该实现符合了标准.</p>
<p>我的Promise实现:<a href="https://github.com/cuipengfei/Spikes/tree/master/js/promise" target="_blank" rel="noopener">https://github.com/cuipengfei/Spikes/tree/master/js/promise</a></p>
<p>在npm上的发布:<a href="https://www.npmjs.com/package/RWPromise" target="_blank" rel="noopener">https://www.npmjs.com/package/RWPromise</a></p>
<p>要实现Promises/A+的规范其实并不需要很多代码,我的实现只有88行.当然,仅仅是符合规范和一个可用,易用的Promise库之间还有很大的差距.</p>
<p>如果作为教学或者演示的目的,我认为我的这份实现是已有实现中最简洁的一版.</p>
<h1 id="自己实现Promise规范时需要注意的几点"><a href="#自己实现Promise规范时需要注意的几点" class="headerlink" title="自己实现Promise规范时需要注意的几点:"></a>自己实现Promise规范时需要注意的几点:</h1><h2 id="1-promise的状态一旦确定-不可更改"><a href="#1-promise的状态一旦确定-不可更改" class="headerlink" title="1. promise的状态一旦确定,不可更改"></a>1. promise的状态一旦确定,不可更改</h2><p>一个符合规范的promise有三种可能的状态:pending,resolved,rejected。</p>
<p>这三者是互斥的。</p>
<p>一个pending的promise可以变成resolved,或者rejected。</p>
<p>但是一旦进入resolved或者rejected状态,就再也不能变了。</p>
<p>用形象的语言来描述的话:一个promise就是一个关于未来的承诺,诺言一旦履行,不能反悔。</p>
<p>假设有如下代码:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = ???();<span class="comment">//首先以某种方式拿到一个promise,假设这个promise现在是pending的</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">p.then(x,y);<span class="comment">//然后把你希望在成功和失败时执行的x,y通过then方法挂进去</span></span></pre></td></tr></table></figure>

<p>时间流逝,假设???()方法内部在未来某个不确定的时间执行了:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">p.resolve();</span></pre></td></tr></table></figure>

<p>然后,你的x函数应该会被调用。</p>
<p>再然后,<strong>无论p的resolve方法或者reject方法再怎么被调用,p的状态都不会再变更,x和y也再不会被执行了</strong>。</p>
<h2 id="2-树状结构"><a href="#2-树状结构" class="headerlink" title="2. 树状结构"></a>2. 树状结构</h2><p>对then方法的多次调用会形成一个树状的数据结构。</p>
<p>假设有如下代码:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = ???();<span class="comment">//首先以某种方式拿到一个promise</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">p</span></pre></td></tr><tr><td class="code"><pre><span class="line">    .then(a,b) <span class="comment">//假设这次then的调用返回的是一个新的promise实例,称之为p1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    .then(c,d);<span class="comment">//假设这次then的调用返回的是一个新的promise实例,称之为p2</span></span></pre></td></tr></table></figure>

<p>上述代码等价于:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = ???();<span class="comment">//首先以某种方式拿到一个promise</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = p.then(a,b);</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p2 = p1.then(c,d);</span></pre></td></tr></table></figure>

<img src="http://i2.buimg.com/425a951ceb32210c.png" />

<p>当然,这个代码形成的会是类似于一个链表的结构,可以把它<strong>看作是树状结构的一个特例</strong>,也就是树中每个节点都最多只有一个子节点。</p>
<p>而如下的代码则会形成我们惯常看到的树:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = ???();</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = p.then(a,b);</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p2 = p.then(c,d);</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p3 = p.then(e,f);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p4 = p1.then(g,h);</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p5 = p3.then(i,j);</span></pre></td></tr></table></figure>

<img src="http://i2.buimg.com/83a315d738e7dc45.png" />

<p>这时,树中每一个节点可以有任意多的子节点(取决于它的then被调用了多少次)。</p>
<p>了解promise的树状结构,将有助于实现promise时在自己脑子里构造递归模型。</p>
<h2 id="3-回调的执行时机"><a href="#3-回调的执行时机" class="headerlink" title="3. 回调的执行时机"></a>3. 回调的执行时机</h2><p>这是实现promise的时候,最容易把人搞晕的一点。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = ???();<span class="comment">//首先以某种方式拿到一个promise,假设这时p是pending的状态</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = p.then(a,b);</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p2 = p1.then(c,d);</span></pre></td></tr></table></figure>

<p>以上代码执行完之后,我们手里有3个promise:p,p1,p2.</p>
<p>这时,a,b,c,d都还没有执行。</p>
<p>在未来某个不确定的时间,如果p的resolve方法被调用了,接下来会发生的事情是:</p>
<ul>
<li>p会把传给resolve方法的参数value记住,并把自己的状态标记为resolved (以后就再也不能变了)</li>
<li>a会被调用到,其参数为value<ul>
<li>如果a执行过程中不出错<ul>
<li>p1的状态被变成resolved,p1会把a的返回值记住</li>
<li>c会被调用到,其参数为a的返回值<ul>
<li>如果c执行过程中不出错<ul>
<li>p2的状态被变成resolved,p2会把c的返回值记住</li>
</ul>
</li>
<li>如果c执行过程中出错<ul>
<li>p2的状态被变成rejected,p2会把c抛出的异常记住</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>如果a执行过程中出错<ul>
<li>p1的状态被变成rejected,p1会把a抛出的异常记住</li>
<li>d会被调用到,参数为a抛出的异常<ul>
<li>如果d执行过程中不出错<ul>
<li>p2的状态被变成resolved,p2会把d的返回值记住</li>
</ul>
</li>
<li>如果d执行过程中出错<ul>
<li>p2的状态被变成rejected,p2会把d抛出的异常记住</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>这样,就看出递归的意思来了。不过b并没有在上面出现,这是因为p本身是被resolve的,b只有在p被reject的时候才会执行。</p>
<p>在未来某个不确定的时间,如果p的reject方法被调用了,接下来会发生的事情是:</p>
<p>。。。 。。。</p>
<p>就不用写了,把上面的ａ替换为ｂ就好了。</p>
<p>以上的例子中,我们拿到p的时候它的状态是pending的,我们会先调用p的then,然后p才会被resolve(或者reject掉)。<br>也就是说当我们通过调用then传递给promise两个回调的时候,promise还没有能力确定应该执行哪个回调,只有当未来promise自己被resolve或者reject了的时候,它自己的状态确定了,它才知道该挑哪一个回调来执行。</p>
<p>还有另一种可能性,那就是当你拿到p的时候p就已经被resolve(或者reject掉了),这时如果你再调用then方法的话,所传入的两个回调,到底哪个应该被调用,马上就可以决定了。</p>
<p>也就是说回调被调用的触发点一共有三个,then,resolve,reject这三个方法。</p>
]]></content>
  </entry>
  <entry>
    <title>利用CouchBase为弱网环境构建云同步Android应用</title>
    <url>/blog/2016/03/10/couchbase-android-sync/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>Wifi，4G，3G，这些我们习以为常的东西，未必对所有人来说都是随时可用的。</p>
<p>以我当前所在项目为例，应用场景是某欠发达地区医疗服务机构的药品库存管理。</p>
<p>所谓欠发达，具体怎样呢？</p>
<ul>
<li>没有台式电脑</li>
<li>没有笔记本</li>
<li>只能使用低端的安卓平板</li>
<li>4G，3G信号不要想</li>
<li>我们去过现场的一位同事甚至要爬到树上去，才能勉强收到2G信号<br><img src="http://img2.cache.netease.com/cnews/2009/2/2/20090202093425b68f3.jpg" alt="tree"></li>
<li>即便是2G信号，也是时断时续，非常不稳定</li>
</ul>
<p>因此，需要随时保持连通的BS结构基本不可行，我们选择了重度依赖移动端设备本地存储的CS结构（胖客户端）。</p>
<p>网络不可用时，库存变动存储在安卓本地，何时网络可用，再将数据与服务器同步。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>以上描述的解决方案似乎合情合理，但是真实实施中还是遇到不少问题：</p>
<ul>
<li>本地schema与服务器schema不一致，中间涉及数据转换与回转</li>
<li>本地到服务器的同步数据流动链条过长（本地orm-&gt;本地Json serialization-&gt;服务器Json deserialization-&gt;服务器orm），链条中任何一环都有出差池的可能性。<br>换句话说，导致数据健全性受损的可能性分散在了太多的点上，一旦出错，难以定位</li>
<li>服务器到本地的数据同步，上一条中所描述的链条的逆向，同样是链条太长，潜在的出现错误的点太多</li>
<li>服务器端所掌握的数据只是客户端真实数据的一个变体，并且还未必是最新的，这样就导致当移动端应用因其本地数据而出错的时候，我们只能对着服务器干着急</li>
</ul>
<p>以上描述的问题并不是偶发性的，它不像这里有个bug今天修了，明天那里有个bug再修一次就好。</p>
<p>只要我们仍然要在弱网环境中运行应用，我们就需要重度依赖本地存储，就需要持续的在移动端和服务端进行双向数据同步，以上的问题就将会一直存在。</p>
<p>这是自然环境限制与技术选择所带来的固有的内在的问题。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>上面提到：</p>
<blockquote>
<p>这是自然环境限制与技术选择所带来的固有的内在的问题。</p>
</blockquote>
<p>这句话再解释明白一些，自然环境限制指的是很差的网络可用性，技术选择指的是服务器端提供REST API，移动端利用该API进行通信。</p>
<p>以上这二者相结合导致了上述情况成为了固有的内在的问题。</p>
<p>自然环境的限制我们无法突破，我们不能把基站部署过去，让大家打电话之前不用再爬到树上去。</p>
<p>但是技术选择是完全受我们控制的，是有做文章的空间的。</p>
<p>这就引出了文章标题提到的CouchBase。</p>
<h1 id="CouchBase"><a href="#CouchBase" class="headerlink" title="CouchBase"></a>CouchBase</h1><p>关于CouchBase是一个怎样的DB，请大家自行搜索。</p>
<p>我们主要关注它推出的CouchBase-Lite（android和iOS均有对应版本）。</p>
<p><img src="https://camo.githubusercontent.com/c1aa705fde3eb12245c06730d850c23e5a84ad8d/687474703a2f2f746c657964656e2d6d6973632e73332e616d617a6f6e6177732e636f6d2f636f756368626173652d6c6974652f636f756368626173652d6c6974652d6172636869746563747572652e706e67" alt="replicate"></p>
<p>左边的绿色方框是移动端应用，它通过蓝色标示的Sync Gateway与CouchBase Server通信。</p>
<p>请注意图中的箭头都是双向的，任何一方对本地数据库的写操作，都会导致对方的更新。任何一方的网络暂时中断也没有关系，在网络恢复的时候将会自动重试。</p>
<p>这样一来，数据同步的思路就变了，不再是在服务器端定义上传下载的API，移动端进行调用。而是利用DB自有的replication机制进行数据同步。</p>
<p>这就意味着我们在移动端只需要关注建立领域特定的模型，并将其存储入移动端本地的CouchBase即可，至于后面的序列化、网络通信等等问题就不需要我们去担心了。</p>
<p>关注点中很大一部分就这样被分离了出去，交由infrastructure去完成。</p>
<p>至于DB自有的replication机制的可靠性，应该可以比较安全的做出假设，认为一个有商用场景的DB厂商的通用数据备份机制不会比我们自己拼凑出来的更差。</p>
<h1 id="一个原型"><a href="#一个原型" class="headerlink" title="一个原型"></a>一个原型</h1><p><a href="https://github.com/cuipengfei/Spikes/tree/master/android/sync-prototype" target="_blank" rel="noopener">https://github.com/cuipengfei/Spikes/tree/master/android/sync-prototype</a></p>
<p>上面的链接是一个基本可用的购物清单应用。全部代码都在，供参考。</p>
<p>下面谈如何把玩它。</p>
<h2 id="第一步"><a href="#第一步" class="headerlink" title="第一步"></a>第一步</h2><p>下载CouchBase Server： <a href="http://www.couchbase.com/nosql-databases/downloads#" target="_blank" rel="noopener">http://www.couchbase.com/nosql-databases/downloads#</a>，安装，配置管理员账户，不赘述。</p>
<p>在CouchBase Server的Admin console(默认地址： <a href="http://127.0.0.1:8091/index.html)中创建一个bucket，命名为demodb。" target="_blank" rel="noopener">http://127.0.0.1:8091/index.html)中创建一个bucket，命名为demodb。</a></p>
<h2 id="第二步"><a href="#第二步" class="headerlink" title="第二步"></a>第二步</h2><p>安装sync_gateway，Mac用户可以：<br>    brew install sync_gateway</p>
<p>以上github代码克隆下来后，sync-gateway路径下有个名为start_sync_gateway_server.sh的脚本，运行它来启动sync gateway。</p>
<h2 id="第三步"><a href="#第三步" class="headerlink" title="第三步"></a>第三步</h2><p>运行同一个路径下的create_user.sh，来创建一个名为user1的用户，然后运行create_session.sh，为该用户创建一个session。</p>
<p>create_session.sh脚本有类似如下的输出：</p>
<pre><code>{&quot;session_id&quot;:&quot;a469f18027647e4957ffd1743e2ea33ce0386dbc&quot;,&quot;expires&quot;:&quot;2016-02-21T17:51:43.071175586+08:00&quot;,&quot;cookie_name&quot;:&quot;SyncGatewaySession&quot;}</code></pre><p>把其中的session id记下备用。</p>
<p>（注：这里的用户和session都是sync gateway需要的，与CouchBase Server无直接关系）</p>
<h2 id="第四步"><a href="#第四步" class="headerlink" title="第四步"></a>第四步</h2><p>找到代码中的MainActivity类，在startSync方法中加入session id：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//......</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">Replication pullReplication = database.createPullReplication(syncUrl);</span></pre></td></tr><tr><td class="code"><pre><span class="line">pullReplication.setCookie(<span class="string">"SyncGatewaySession"</span>, <span class="string">"a469f18027647e4957ffd1743e2ea33ce0386dbc"</span>, <span class="keyword">null</span>, <span class="number">86400000000000L</span>, <span class="keyword">false</span>, <span class="keyword">false</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">pullReplication.setChannels(asList(<span class="string">"user1"</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">pullReplication.setContinuous(<span class="keyword">true</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">Replication pushReplication = database.createPushReplication(syncUrl);</span></pre></td></tr><tr><td class="code"><pre><span class="line">pushReplication.setCookie(<span class="string">"SyncGatewaySession"</span>, <span class="string">"a469f18027647e4957ffd1743e2ea33ce0386dbc"</span>, <span class="keyword">null</span>, <span class="number">86400000000000L</span>, <span class="keyword">false</span>, <span class="keyword">false</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">pushReplication.setChannels(asList(<span class="string">"user1"</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">pushReplication.setContinuous(<span class="keyword">true</span>);</span></pre></td></tr></table></figure>

<p>这段代码负责启动replication，双向同步从此而起。</p>
<p>找到createGroceryItem，为其中创建document的代码指定其所归属的用户：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//......</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">Document document = database.createDocument();</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">Map&lt;String, Object&gt; properties = <span class="keyword">new</span> HashMap&lt;String, Object&gt;();</span></pre></td></tr><tr><td class="code"><pre><span class="line">properties.put(<span class="string">"channels"</span>, asList(<span class="string">"user1"</span>));</span></pre></td></tr></table></figure>

<p>这几行代码可以保证各个移动端用户之间的数据不会混杂在一起。</p>
<h2 id="第五步"><a href="#第五步" class="headerlink" title="第五步"></a>第五步</h2><p>在genymotion中启动android虚拟机（如果使用其他虚拟设备或者真机，请注意修改代码中的服务器ip地址）。</p>
<p>在购物清单中创建几条记录，然后清空移动端本机数据，重启应用，可以看到刚刚被清空的购物清单会从服务器上同步回来。</p>
<p>也可以尝试把虚拟机的网络连接断掉，创建或者修改几条记录，稍后重新连通网络，可以发现数据仍然可以上传到服务器。</p>
<p>还可以尝试用第三步中提到的脚本多创建几个用户，在不同的android虚拟机中使用不同用户，可以发现它们对彼此的数据是没有访问权的。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>以上第五步提到的双向同步，离线操作，不同用户之间的数据隔离，都不需要我们写任何特殊的代码来实现。</p>
<p>我们移动端的代码与CouchBase的集成基本就只涉及到第四步中提到的启动replication和创建document，那这样移动端剩下的工作就只有构建业务逻辑了。</p>
<p>如果你的移动端应用也需要在弱网环境下进行离线操作，在网络恢复时与服务器同步数据的话，不妨尝试一下CouchBase。</p>
]]></content>
  </entry>
  <entry>
    <title>2015</title>
    <url>/blog/2016/01/03/2015/</url>
    <content><![CDATA[<p>2015年结束了，一如已经结束了的每一年，非常迅速。</p>
<p>按惯例，从博客说起。</p>
<h1 id="博客"><a href="#博客" class="headerlink" title="博客"></a>博客</h1><p>15年写了16篇博客，其中9篇是与Scala，Reactive，OODP相关的。这个数字倒还不算太坏。</p>
<p>14年总结的时候说：</p>
<blockquote>
<p>明年可以改进的是不要嫌话题小，不要嫌话题不够深。 有了有价值的想法就记下来，形成惯性。</p>
</blockquote>
<p>15年第一季度没有执行这一条原则。其原因在于懒惰。</p>
<p>15年第二季度超额执行。其原因在于找到了主线任务，足够的探索便促成了足够的产出。</p>
<p>第三季度和第四季度未执行。其原因在于到客户现场去工作彻底打乱了所有的日常习惯与日程计划。</p>
<p>这么看来，博客这一块有得有失，得者为要找到一个足够有趣的主线任务，用来催生产出。失者为习惯的打破与难以重补。</p>
<p>由此得出16年需要执行的事项：</p>
<p>主线任务要明确，暂定为Reactive以及与之相关的一切。Review不能每个季度做一次，要每个月做一次，每次可以短，不能没有，这样强迫自己去关注进度，不可斜视。</p>
<h1 id="读书"><a href="#读书" class="headerlink" title="读书"></a>读书</h1><img src="http://i4.tietuku.com/9ba5abac153d116a.png">

<img src="http://i4.tietuku.com/607f32c9450c71cf.png">

<p>这个数字可以，分布情况不太漂亮。5月和9月是两个空档。</p>
<p>5月是因为刚去TWU，说得过去。9月是因为去了客户现场，其实也说得过去。</p>
<p>但是内容控制的不好。</p>
<p>14年总结说的是：</p>
<blockquote>
<p>另外一个改进点是领域，我明年需要看一些轻量级的经济、哲学和社会心理学的书。口说无凭，于此立字为据。</p>
</blockquote>
<p>这方面第一季度执行的可以，第二季度开始用trello track，也不错。<br>但是下半年就废了，还是前文提过的同样的原因。</p>
<p>16年的trello重新建了一个board，遵循上段同样的方式，每个月review。</p>
<h1 id="MOOC"><a href="#MOOC" class="headerlink" title="MOOC"></a>MOOC</h1><p>极其差。</p>
<p>一年只上完了一门课。没有勇气提16年的目标了。看际遇吧。</p>
<h1 id="健身"><a href="#健身" class="headerlink" title="健身"></a>健身</h1><p>这个坚持执行的很好。还探索出了好的玩法。</p>
<img src="http://i4.tietuku.com/59dfa3e76e11f382.jpg">

<p>数据统计不完整，但是可以凑合看，增重0.4，减脂0.1，算是净增1斤肌肉。曲线不难看，但是这个数字不算漂亮。</p>
<img src="http://i4.tietuku.com/93656571b24423e4.jpg">

<p>从8月4号开始用bodyspace来track运动量，至此共91次，重量累计935吨。</p>
<p>这个数字累积到1000吨（也就是100万千克）时就换方法，不着重统计重量了，而是统计围度。</p>
<p>下面统计下出勤率：</p>
<p>从14年6月8日到现在,共574天。出勤362次，出勤率63%。</p>
<h1 id="时间管理与统计"><a href="#时间管理与统计" class="headerlink" title="时间管理与统计"></a>时间管理与统计</h1><p>这个统计是从15年4月1日开始的，9，10月份由于去客户现场的原因，中断掉了。数据就放在这儿，不分析了。</p>
<img src="http://i4.tietuku.com/5e3c1b5be12d1fe5.png">

<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p>这一年脾气见好，不错。</p>
]]></content>
      <tags>
        <tag>me</tag>
      </tags>
  </entry>
  <entry>
    <title>2015第三季度</title>
    <url>/blog/2015/10/05/2015-3rd-season/</url>
    <content><![CDATA[<p>惨。</p>
<p>八月份上了一个大客户的项目，到了晚上没力气也没心情做任何事情。</p>
<h1 id="博客"><a href="#博客" class="headerlink" title="博客"></a>博客</h1><p>没写。上个季度六月的系列告一段落后7月没写。到了8月就不用说了。</p>
<h1 id="读书"><a href="#读书" class="headerlink" title="读书"></a>读书</h1><p>七月八月读了7本书，9月一本未读。</p>
<p>在trello中建的计划，其中7月执行的不错，8月一般，九月，当让，啥都没做。</p>
<h1 id="MOOC"><a href="#MOOC" class="headerlink" title="MOOC"></a>MOOC</h1><p>无。</p>
<h1 id="翻译"><a href="#翻译" class="headerlink" title="翻译"></a>翻译</h1><p>上次提到的书已经交稿了，坐等出版。</p>
<h1 id="健身"><a href="#健身" class="headerlink" title="健身"></a>健身</h1><p>这项不错，我从一开始就想好，无论什么事没时间没心情做，这件事不能停。</p>
<p>从八月份开始，有了系统的计划</p>
<p><img src="http://i13.tietuku.com/3c423d466868f0e6.png" alt="Alt text"></p>
<p>三个大肌群，所有小肌群，各自一个计划。</p>
<p>从八月初执行至今，共记录41次workout，举铁345吨。</p>
<p><img src="http://i13.tietuku.com/237378291054c70a.png" alt="Alt text"></p>
<p>平均每次去健身房8吨，日最高记录15吨。</p>
<p>这段时间出勤率67%。</p>
<p>从去年6月8日到现在，共485天，出勤314次，出勤率64%。</p>
<p>其中武汉201次，印度72次，成都41次。</p>
<h1 id="总之"><a href="#总之" class="headerlink" title="总之"></a>总之</h1><p><img src="http://i.ytimg.com/vi/qQNvkINSEsY/hqdefault.jpg" alt="Alt text"></p>
]]></content>
      <tags>
        <tag>me</tag>
      </tags>
  </entry>
  <entry>
    <title>2015第二季度</title>
    <url>/blog/2015/07/04/2015-second-season/</url>
    <content><![CDATA[<p>2015竟然这么快就过去一半了。逝者如斯夫。</p>
<h1 id="博客"><a href="#博客" class="headerlink" title="博客"></a>博客</h1><p>第一季度总结的时候说：</p>
<blockquote>
<p>今年的博客主线任务定为OO与FP的比较和结合应用吧。</p>
</blockquote>
<p>这个任务完成的不错，这个主题写了7篇博客。</p>
<p>迭代器已经irrelevant了，中介者和备忘录太简单就没写，状态模式没找到好的FP实现方式。<br>这样11种行为模式除去上述4个，算是基本覆盖完了。</p>
<p>Principles of Reactive Programming作业导学写了两篇，后面的有点难，写不出来了。。。<br>这一点后面MOOC再说。</p>
<p>还有一篇gender pay gap的博客，算是今年到目前为止阅读量最多的得意之作。</p>
<p>在武汉做了一次学英语的workshop，在TWU做了pecha kucha，总结成了两篇。</p>
<p>这样，不算季度总结的博客，写了12篇，数量和内容自己都满意。和技术相关的还可以充当复习和刷新记忆的资料。</p>
<p>不错，博客这方面达标了。</p>
<h1 id="读书"><a href="#读书" class="headerlink" title="读书"></a>读书</h1><p>这个季度比较惨。这一季度只读了五本书。</p>
<p>一部分原因在于TWU的日程太紧，一部分原因在于博客，MOOC和翻译占用的时间很多，不可兼得。</p>
<p>还好第一季度看书比较多，所以到目前为止这半年一共读过16本书，总体数量还不算太惨。</p>
<p>这十六本书里有7本有笔记，可以充当复习和刷新记忆的资料，而且效果很好。自己写的笔记，瞄一眼，整本书的内容和重点就全部复活。</p>
<p>不过看书的领域方面有点杂，2014总结的时候说：</p>
<blockquote>
<p>明年需要看一些轻量级的经济、哲学和社会心理学的书。</p>
</blockquote>
<p>这方面执行的不好。</p>
<p><img src="http://i1.tietuku.com/26b6dd870a313983.png" alt="http://i1.tietuku.com/26b6dd870a313983.png"></p>
<p>于是建了一个trello，把2015下半年要读的书预先plan出来。每个月plan两三本，一部分符合上述他山之石的领域，一部分符合OOP和FP的技术主线。</p>
<p><img src="http://i1.tietuku.com/09607b3bd8c47d1d.png" alt="http://i1.tietuku.com/09607b3bd8c47d1d.png"></p>
<p>这样plan的数量并不大，如果突然出现兴趣很高，或者优先级很高的书的话，可以随时插入计划中。</p>
<p>另外，pipeline定义清晰，每本书要读完，有笔记，有复习，有某种形式的产出（笔记也算）。</p>
<h1 id="MOOC"><a href="#MOOC" class="headerlink" title="MOOC"></a>MOOC</h1><p>把Principles of Reactive Programming跟完了，证书拿到了。</p>
<p><img src="http://i1.tietuku.com/cff384ac4788e10b.png" alt="http://i1.tietuku.com/cff384ac4788e10b.png"></p>
<p>但是照实说，这门课没学懂，只是应付过了。后四周的作业导学没写出来。</p>
<p>主要原因在于事先对课程难度估计过低，投入时间不够。下次开课，需要再跟一遍。</p>
<p>除此之外，下半年对于MOOC这方面不做过多预期，有特别好的特别感兴趣的就跟，没有就算了。</p>
<p>算是对年初说过的话彻底食言了。。。</p>
<h1 id="体重"><a href="#体重" class="headerlink" title="体重"></a>体重</h1><p>在印度这段时间健身房出勤率74%左右，还挺好。</p>
<p>现在，60.5公斤，第一季度结束时是61。基本算是没变。充分说明了没有改进目标就不会有成绩。</p>
<p>不过鉴于我现在已经不算胖子了，这方面仍然不做过多奢求，维持就好。</p>
<p>另外，腹肌的轮廓开始出现了，我很开心：）</p>
<img src="http://i1.tietuku.com/4866dc89e0ffac6a.jpg" style="width: 300px;"/>

<img src="http://i1.tietuku.com/6b5710437ae31c2f.jpg" style="width: 300px;"/>

<h1 id="翻译"><a href="#翻译" class="headerlink" title="翻译"></a>翻译</h1><p>接了出版社一个翻译的活儿，《Seven more languages in seven weeks》，是本蛮不错的书。这个额外的任务投入时间较多也是读书方面有欠缺的原因之一。</p>
<p>对出版充满期待。</p>
<h1 id="时间管理与统计"><a href="#时间管理与统计" class="headerlink" title="时间管理与统计"></a>时间管理与统计</h1><p>从四月一号开始，开始使用pomotodo这款超赞的番茄钟软件。从开始的第一天开始，到现在为止，凡是需要坐下来专心执行的事情都有记录。</p>
<p><img src="http://i1.tietuku.com/50d083b12ab39342.png" alt="http://i1.tietuku.com/50d083b12ab39342.png"></p>
<p>这款软件的统计分析功能很好用，每个番茄钟还可以加tag。可以看到，这三个月做多的时间放在了TWU的备课上（26%），其次是翻译书（16%），mooc和blog紧跟其后（13%和10%），然后还有读书和写TWU需要的总结反馈（都是7%）。</p>
<p>这样，有数据，为什么读书少就一目了然了。</p>
<p>另外，可以看到这三个月平均日完成7.55个番茄钟。这个数量很说明问题，工作的效率靠感觉是感觉不出来的，要靠统计数据和分析。<br>7.55个番茄钟，相当于四个小时左右。每天平均专心工作，执行任务的时间只有四个小时啊！</p>
<p>这里面有一部分原因是TWU过程中需要听别的讲师的很多sessions，如果没有这个因素，日均数量应该是9个左右。</p>
<p>下个季度这方面的数据会是什么样的很难说，如果做的工作中有很大部分涉及沟通和协调的话，这个数字或许会走低。</p>
]]></content>
      <tags>
        <tag>me</tag>
      </tags>
  </entry>
  <entry>
    <title>模板方法模式：子类型多态和高阶函数</title>
    <url>/blog/2015/06/23/template-method-pattern/</url>
    <content><![CDATA[<blockquote>
<p>模板方法模式定义了一个算法的步骤，并允许次类别为一个或多个步骤提供其实践方式。让次类别在不改变算法架构的情况下，重新定义算法中的某些步骤。</p>
</blockquote>
<p>以上是wiki对模板方法的定义。</p>
<p>比较容易理解，我们有一个算法，其中某些步骤是确定的不太会变的代码。而另外一些步骤则需要变化并且自由组合。</p>
<p>《Head First Design Patterns》里有一个🌰：</p>
<p>假设我们需要制作咖啡因饮品，其实就是咖啡和茶。制作步骤有些类似，分为四步：1烧水，2泡，3装杯，4加调料。</p>
<p>其中第一步和第三步是稳定的代码，变化可能性不大，而第二步和第四步则每种饮品有自己的风味。</p>
<p>这样就有了下面的代码：</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/templatemethodJ/CaffeineBeverage.java?slice=1:&footer=minimal">
</script>

<p>首先有一个咖啡因饮品的抽象类，定义一个算法骨架：1烧水，2泡，3装杯，4加调料。<br>其中的第二步和第四步是有待实现的抽象方法，留给子类决定怎么搞。第一步和第三步是写死的。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/templatemethodJ/Coffee.java?slice=1:">
</script>

<p>接下来是咖啡，它实现了过滤咖啡和添加牛奶、糖的步骤。这样当它的实例的prepareRecipe方法被调用时就会执行父类的烧水、装杯，以及自己的泡和加调料。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/templatemethodJ/Tea.java?slice=1:">
</script>

<p>还有，就是茶了。它和咖啡不一样，是用浸泡而不是过滤，加的是柠檬而不是牛奶和糖。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/templatemethodJ/BeverageTestDrive.java?slice=1:">
</script>

<p>最后用一个main函数来执行制作咖啡和茶的代码。</p>
<p>很好，如果再有其他的咖啡因饮品，只需要增加一个子类，并且实现两个方法就好了。只要我们对于四个步骤的定义在该领域中足够稳定，这份代码就是很好很强大，易于扩展的。</p>
<p>有代码如此，夫复何求呢？</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>这个模式想要达到的，不过是将一个算法的某些部分做的灵活一些，可以自由替换和组合。</p>
<p>那这个，不就是函数组合吗？如果我们使用的是允许高阶函数的语言的话，那还有什么必要把这些函数包装在类里呢？</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>接下来是用Scala实现的版本：</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/templatemethodS/Beverages.scala?slice=1:">
</script>

<p>首先，定义三个type，分别是泡和加调料这两个步骤，还有饮品本身（这三个type其实是一样的，看起来有点傻）。</p>
<p>然后有一个算法骨架，把第一和第三步锁死，把第二和第四步空出来，分别用一个参数来实现注入不同的实现。</p>
<p>接下来有泡和加调料的四种不同实现，分别是一个函数，符合各自的函数签名。</p>
<p>最后，用一个main函数来执行。可以看到，泡和加调料的函数是作为参数传入的。如果我们需要加牛奶和糖的茶，或者是柠檬味的咖啡的话，也会变得非常容易。</p>
<p>就这样，51行代码变成了28行。四个类变成了一个object。</p>
<p>而如果是要用子类型多态（subtype polymorphism）来做到这样的自由组合，那么我们需要的或许就是策略模式，把泡和加调料分别写成接口并提供不同的实现类来组合。可以想象，这会导致很多的boilerplate。</p>
<h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>Java代码中实现多态的方式是通过子类继承父类并且实现抽象方法来实现的。而Scala代码中则是通过把不同的函数传入骨架组合出一个新的函数来实现的。</p>
<p>子类型多态（subtype polymorphism）是个好东西，但是在某些场景下显得有点重。能用高阶函数这种轻量级的方式来实现的时候，就没有必要选择子类型多态这种过重的方式。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>访问者模式 in FP：Pattern Matching</title>
    <url>/blog/2015/06/16/visitor-pattern-pattern-match/</url>
    <content><![CDATA[<blockquote>
<p>访问者模式是一种将算法与对象结构分离的软件设计模式。</p>
</blockquote>
<blockquote>
<p>这个模式的基本想法如下：首先我们拥有一个由许多对象构成的对象结构，这些对象的类都拥有一个accept方法用来接受访问者对象；访问者是一个接口，它拥有一个visit方法，这个方法对访问到的对象结构中不同类型的元素作出不同的反应；在对象结构的一次访问过程中，我们遍历整个对象结构，对每一个元素都实施accept方法，在每一个元素的accept方法中回调访问者的visit方法，从而使访问者得以处理对象结构的每一个元素。我们可以针对对象结构设计不同的实在的访问者类来完成不同的操作。</p>
</blockquote>
<p>以上是wiki对访问者模式的定义。</p>
<p>这个定义着实难读。我们来看wiki给出的例子：</p>
<p>假设我们要为汽车建模，汽车有不同的组成部件，轮胎，车身，和引擎。</p>
<p>在开车之前需要先检查车辆每个部件的状况，然后依次启动所有部件以启动汽车。</p>
<p>在这里我们很容易识别出车的组件各自应该是一个实体。而对车辆组件进行检查和启动的代码应该分别处于不同的实体中。</p>
<p>这样就有了访问者的代码（来自wiki）：</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">ICarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">accept</span><span class="params">(ICarElementVisitor visitor)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Wheel</span> <span class="keyword">implements</span> <span class="title">ICarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> String name;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Wheel</span><span class="params">(String name)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.name = name;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">accept</span><span class="params">(ICarElementVisitor visitor)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        visitor.visit(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Engine</span> <span class="keyword">implements</span> <span class="title">ICarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">accept</span><span class="params">(ICarElementVisitor visitor)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        visitor.visit(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Body</span> <span class="keyword">implements</span> <span class="title">ICarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">accept</span><span class="params">(ICarElementVisitor visitor)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        visitor.visit(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Car</span> <span class="keyword">implements</span> <span class="title">ICarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    ICarElement[] elements;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Car</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.elements = <span class="keyword">new</span> ICarElement[]&#123;<span class="keyword">new</span> Wheel(<span class="string">"front left"</span>),</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">new</span> Wheel(<span class="string">"front right"</span>), <span class="keyword">new</span> Wheel(<span class="string">"back left"</span>),</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">new</span> Wheel(<span class="string">"back right"</span>), <span class="keyword">new</span> Body(), <span class="keyword">new</span> Engine()&#125;;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">accept</span><span class="params">(ICarElementVisitor visitor)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">for</span> (ICarElement elem : elements) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            elem.accept(visitor);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        visitor.visit(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>首先是汽车部件的实体。它们都实现同一个ICarElement的接口。<br>该接口定义一个accept方法，用来接受访问者然后用访问者来访问所有汽车部件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">ICarElementVisitor</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">visit</span><span class="params">(Wheel wheel)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">visit</span><span class="params">(Engine engine)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">visit</span><span class="params">(Body body)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">visit</span><span class="params">(Car car)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CarElementPrintVisitor</span> <span class="keyword">implements</span> <span class="title">ICarElementVisitor</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Wheel wheel)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Visiting "</span> + wheel.getName() + <span class="string">" wheel"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Engine engine)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Visiting engine"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Body body)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Visiting body"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Car car)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Visiting car"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CarElementDoVisitor</span> <span class="keyword">implements</span> <span class="title">ICarElementVisitor</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Wheel wheel)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Kicking my "</span> + wheel.getName() + <span class="string">" wheel"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Engine engine)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Starting my engine"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Body body)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Moving my body"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(Car car)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Starting my car"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>然后就是访问者的实体。它们都实现ICarElementVisitor接口。<br>这个接口里定义的方法有点多，分别对应每个汽车部件定义了一个visit方法的重载。</p>
<p>在实现的时候自然是做检查的实体实现每个部件的检查，启动的实体实现每个部件的启动。</p>
<p>这里就有一个陷阱，如果代码发展的趋势是汽车部件的种类会增加的话，那这个接口就很不稳定。每增加一种汽车部件就要修改接口并且修改每个实现类。</p>
<p>而如果代码发展的趋势是在自检和启动之外加一些保养啊，洗车啊之类的话就没问题，不需要对已有代码进行修改。</p>
<p>所以使用访问者模式的时候要注意识别被访问者是否是相对稳定而访问者是有扩展趋势的，这样用这个模式才合适。</p>
<p>接下来的代码把以上所有代码串起来执行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">VisitorDemo</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        ICarElement car = <span class="keyword">new</span> Car();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        car.accept(<span class="keyword">new</span> CarElementPrintVisitor());</span></pre></td></tr><tr><td class="code"><pre><span class="line">        car.accept(<span class="keyword">new</span> CarElementDoVisitor());</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>从最后的main函数来看，只要能确保汽车部件的数量不会增加，而只有访问者增加，那么客户代码只需要增加一行就能够增加对整车进行清洗或者保养的行为。</p>
<p>车的部件和对部件的操作相互分离，独立发展。很灵活，很巧妙，对吧？</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>其实也不需要使劲想了，如果你看过这一系列博文前面的几篇的话，想必已经能够猜到我的用意了。</p>
<p>这些访问者存在的意义就在于承载对汽车部件的某些具体操作，操作是个好听的词儿，说白了就是函数啊。</p>
<p>那既然这些类只是承载函数而已，何不直接就用函数而不费劲去用类包裹一层呢？</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>那接下来就是用Scala的实现：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">CarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">accept</span></span>(visitor: <span class="type">Visitor</span>) = visitor(<span class="keyword">this</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Body</span>(<span class="params"></span>) <span class="keyword">extends</span> <span class="title">CarElement</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Engine</span>(<span class="params"></span>) <span class="keyword">extends</span> <span class="title">CarElement</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Wheel</span>(<span class="params">name: <span class="type">String</span></span>) <span class="keyword">extends</span> <span class="title">CarElement</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Car</span>(<span class="params"></span>) <span class="keyword">extends</span> <span class="title">CarElement</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> elements: <span class="type">Seq</span>[<span class="type">CarElement</span>] = <span class="type">Seq</span>(</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">Wheel</span>(<span class="string">"front left"</span>), <span class="type">Wheel</span>(<span class="string">"front right"</span>),</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">Wheel</span>(<span class="string">"back left"</span>), <span class="type">Wheel</span>(<span class="string">"back right"</span>),</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">Body</span>(), <span class="type">Engine</span>())</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">override</span> <span class="function"><span class="keyword">def</span> <span class="title">accept</span></span>(visitor: <span class="type">Visitor</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    elements.foreach(_.accept(visitor))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    visitor(<span class="keyword">this</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>以上是汽车各种部件的定义，和Java代码没有太大区别。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Visitors</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="class"><span class="keyword">type</span> <span class="title">Visitor</span> </span>= <span class="type">CarElement</span> =&gt; <span class="type">Unit</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> printVisitor: <span class="type">Visitor</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Wheel</span>(name) =&gt; println(<span class="string">s"Visiting <span class="subst">$name</span> wheel"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Body</span>() =&gt; println(<span class="string">"Visiting Body"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Engine</span>() =&gt; println(<span class="string">"Visiting Engine"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Car</span>() =&gt; println(<span class="string">"Visiting Car"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> doVisitor: <span class="type">Visitor</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Wheel</span>(name) =&gt; println(<span class="string">s"Kicking my <span class="subst">$name</span> wheel"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Body</span>() =&gt; println(<span class="string">"Moving my body"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Engine</span>() =&gt; println(<span class="string">"Starting my engine"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> <span class="type">Car</span>() =&gt; println(<span class="string">"Starting my car"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>上面这一段定义了一个叫做Visitor的type，它只是一个函数签名。任何接受一个汽车部件作为参数并且没有返回值的函数都符合它的签名，也就可以被视作Visitor。</p>
<p>接下来是两个符合Visitor签名的函数，都是用pattern match实现的。</p>
<p>pattern match这种神奇的语言特性是如何实现的呢？背后的原因并不神奇，更多详情请参考我之前的另一篇博客：<a href="http://cuipengfei.me/blog/2013/12/29/desugar-scala-8/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2013/12/29/desugar-scala-8/</a></p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">VisitorDemo</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> car = <span class="type">Car</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">    car.accept(printVisitor)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    car.accept(doVisitor)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>最后定义一个main函数，与Java的main函数做的事情是等价的。</p>
<p>这样，100行变成了45行。Visitor不再作为臃肿的实体存在，而只是函数。</p>
<p>而且如果遵照同样的假设，认为车的部件是稳定的，而访问者是会增多的，那这段Scala代码的增长趋势是每加一个访问者就加一个函数。与Java代码的增长趋势相同。</p>
<h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>这次分析的访问者模式和之前的一些模式很类似，当我们需要的实体仅仅是作为承载某种行为的一个载具，那就可以考虑将实体消去，而换用函数这种更简单，更轻量级的抽象方式来实现我们想要的东西。</p>
<p>当年OO模式出现的时候，FP并不盛行，原作者提出的方案无可厚非。不过我们今天有了FP这种更趁手的工具，就可以考虑在合适的时候将其与OO结合使用来达到更好的设计的目的。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>观察者模式 in FP：Mutation vs Transformation</title>
    <url>/blog/2015/06/13/observers-pattern-fp/</url>
    <content><![CDATA[<h1 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h1><blockquote>
<p>观察者模式（有时又被称为发布/订阅模式）是软件设计模式的一种。在此种模式中，一个目标对象管理所有相依于它的观察者对象，并且在它本身的状态改变时主动发出通知。这通常透过呼叫各观察者所提供的方法来实现。此种模式通常被用来实作事件处理系统。</p>
</blockquote>
<p>以上是wiki对观察者模式的解释。</p>
<p>举一个《Head first design pattern》中的例子：</p>
<p>比如说有一个气象站，每当气象有变化的时候就需要显示当前天气。<br>需要显示历史平均气温，最高气温和最低气温。<br>还需要根据气压预测晴雨。</p>
<p>这种情况就很适合使用观察者模式，每种需要显示气象的装置作为观察者，气象数据本身作为可以被观察的对象。<br>每当气象变化的时候，被观察的对象就会通知观察者来根据新的数据作出新的显示。</p>
<p>以下是书中给出的代码：</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Observer</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">float</span> temp, <span class="keyword">float</span> humidity, <span class="keyword">float</span> pressure)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Subject</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">registerObserver</span><span class="params">(Observer o)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">notifyObservers</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>首先定义两个接口，一个是观察者，接收新的气象数据。一个是被观察者，可以注册观察者以及通知观察者。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WeatherData</span> <span class="keyword">implements</span> <span class="title">Subject</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> ArrayList&lt;Observer&gt; observers = <span class="keyword">new</span> ArrayList&lt;&gt;();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> temperature;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> humidity;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> pressure;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">registerObserver</span><span class="params">(Observer o)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        observers.add(o);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">notifyObservers</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">for</span> (Observer observer : observers) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            observer.update(temperature, humidity, pressure);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setMeasurements</span><span class="params">(<span class="keyword">float</span> temperature, <span class="keyword">float</span> humidity, <span class="keyword">float</span> pressure)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.temperature = temperature;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.humidity = humidity;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.pressure = pressure;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        notifyObservers();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>接下来定义气象数据本身。代码很容易理解，把观察者保存在一个list里，每当气象数据变化的时候就通知这些观察者去做出新的处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CurrentConditionsDisplay</span> <span class="keyword">implements</span> <span class="title">Observer</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CurrentConditionsDisplay</span><span class="params">(Subject weatherData)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        weatherData.registerObserver(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">float</span> temperature, <span class="keyword">float</span> humidity, <span class="keyword">float</span> pressure)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Current conditions: "</span> + temperature</span></pre></td></tr><tr><td class="code"><pre><span class="line">                + <span class="string">"F degrees and "</span> + humidity + <span class="string">"% humidity"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StatisticsDisplay</span> <span class="keyword">implements</span> <span class="title">Observer</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> maxTemp = <span class="number">0.0f</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> minTemp = <span class="number">200</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> tempSum = <span class="number">0.0f</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> numReadings;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">StatisticsDisplay</span><span class="params">(WeatherData weatherData)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        weatherData.registerObserver(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">float</span> temp, <span class="keyword">float</span> humidity, <span class="keyword">float</span> pressure)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        tempSum += temp;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        numReadings++;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (temp &gt; maxTemp) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            maxTemp = temp;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (temp &lt; minTemp) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            minTemp = temp;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="string">"Avg/Max/Min temperature = "</span> + (tempSum / numReadings)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                + <span class="string">"/"</span> + maxTemp + <span class="string">"/"</span> + minTemp);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ForecastDisplay</span> <span class="keyword">implements</span> <span class="title">Observer</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> currentPressure = <span class="number">29.92f</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">float</span> lastPressure;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ForecastDisplay</span><span class="params">(WeatherData weatherData)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        weatherData.registerObserver(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">float</span> temp, <span class="keyword">float</span> humidity, <span class="keyword">float</span> pressure)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        lastPressure = currentPressure;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        currentPressure = pressure;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.print(<span class="string">"Forecast: "</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (currentPressure &gt; lastPressure) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(<span class="string">"Improving weather on the way!"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (currentPressure == lastPressure) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(<span class="string">"More of the same"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (currentPressure &lt; lastPressure) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(<span class="string">"Watch out for cooler, rainy weather"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>然后有三个观察者，分别负责显示当前气象，气象历史分析和晴雨预测。</p>
<p>CurrentConditionsDisplay是最简单的，没有任何状态，它只是负责在每次气象有变化的时候把最新的气象显示出来。</p>
<p>StatisticsDisplay复杂一点点，它需要记录历史气温，以便于计算平均温度，最高和最低气温。这是一个会有状态变化的对象。</p>
<p>ForecastDisplay也有状态变化，它需要记录上次的气压，以便于根据气压变化来预测晴雨。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WeatherStation</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        WeatherData weatherData = <span class="keyword">new</span> WeatherData();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        CurrentConditionsDisplay currentDisplay = <span class="keyword">new</span> CurrentConditionsDisplay(weatherData);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        StatisticsDisplay statisticsDisplay = <span class="keyword">new</span> StatisticsDisplay(weatherData);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        ForecastDisplay forecastDisplay = <span class="keyword">new</span> ForecastDisplay(weatherData);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        weatherData.setMeasurements(<span class="number">80</span>, <span class="number">65</span>, <span class="number">30.4f</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        weatherData.setMeasurements(<span class="number">82</span>, <span class="number">70</span>, <span class="number">29.2f</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        weatherData.setMeasurements(<span class="number">78</span>, <span class="number">90</span>, <span class="number">29.2f</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>最后，有一个main函数，把以上所有代码串起来。</p>
<p>三个观察者都在观察同一个气象数据，每当气象有变化的时候，三个观察者都会被通知，并作出相应处理。</p>
<p>如果我们需要其他的更复杂的气象显示装置，只需要实现Observer接口，注册到气象数据上去，那么在每次气象有变化的时候就可以收到通知并作出处理。不需要对已有代码做出任何改变。</p>
<p>很灵活，很强大，对吧？</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>观察者模式有没有更好地实现方式呢？</p>
<p>答案肯定是有的。</p>
<p>C#的delegate和Event就是一种用来实现观察者模式的很好的语言特性。它在语言级别为添加事件订阅和取消订阅提供了支持。</p>
<p>不过这一篇博客主要是想要讲一个immutable的观察者模式实现，C#就不多讲了。</p>
<p>可以想一下，上面的Java代码里的三个观察者，CurrentConditionsDisplay是没有任何状态变化的，它存在的意义仅在于其update方法。<br>而这个方法每次都是接受最新的气象数据，并作出输出。</p>
<p>StatisticsDisplay和ForecastDisplay则是截取气象历史数据不同的片段，将其作为可变状态封装在内部，并据其状态的改变决定update方法的行为。</p>
<p>这样看来，如果我们有一种方式，可以提供完整的气象数据历史，那么这三个观察者就都可以各取所需，而不需要拥有自己的可变状态了。</p>
<p>具体该怎么做呢？</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>以下是Scala的实现：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">WeatherData</span>(<span class="params">temperature: <span class="type">Float</span> = 0,</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="params">                       humidity: <span class="type">Float</span> = 0,</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="params">                       pressure: <span class="type">Float</span> = 0,</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="params">                       observers: <span class="type">Seq</span>[<span class="type">Observer</span>] = <span class="type">Nil</span>,</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="params">                       history: <span class="type">Seq</span>[<span class="type">WeatherData</span>] = <span class="type">Seq</span>(<span class="type">WeatherData</span>(history = <span class="type">Nil</span></span>))) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">register</span></span>(observer: <span class="type">Observer</span>) =</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.copy(temperature, humidity, pressure, observers :+ observer, history)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">weatherChanged</span></span>(weatherData: <span class="type">WeatherData</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> newHistory = history :+ weatherData</span></pre></td></tr><tr><td class="code"><pre><span class="line">    observers.foreach(observer =&gt; observer(newHistory))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.copy(temperature, humidity, pressure, observers, newHistory)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Observers</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="class"><span class="keyword">type</span> <span class="title">Observer</span> </span>= <span class="type">Seq</span>[<span class="type">WeatherData</span>] =&gt; <span class="type">Unit</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> currentConditionsDisplay: <span class="type">Observer</span> = history =&gt;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">s"Current conditions: "</span> +</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="string">s"<span class="subst">$&#123;history.last.temperature&#125;</span> F degrees and "</span> +</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="string">s"<span class="subst">$&#123;history.last.humidity&#125;</span> % humidity"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> statisticsDisplay: <span class="type">Observer</span> = history =&gt;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">s"Avg/Max/Min temperature = "</span> +</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="string">s"<span class="subst">$&#123;history.map(_.temperature).sum / history.size&#125;</span>"</span> +</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="string">s"/<span class="subst">$&#123;history.map(_.temperature).max&#125;</span>"</span> +</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="string">s"/<span class="subst">$&#123;history.map(_.temperature).max&#125;</span>"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> forecastDisplay: <span class="type">Observer</span> = history =&gt; &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> currentPressure = history.last.pressure</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> lastPressure = history.dropRight(<span class="number">1</span>).last.pressure</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    print(<span class="string">"Forecast: "</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (currentPressure &gt; lastPressure) println(<span class="string">"Improving weather on the way!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (currentPressure == lastPressure) println(<span class="string">"More of the same"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (currentPressure &lt; lastPressure) println(<span class="string">"Watch out for cooler, rainy weather"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">WeatherStation</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> weatherData = <span class="type">WeatherData</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .register(currentConditionsDisplay)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .register(statisticsDisplay)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .register(forecastDisplay)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    weatherData</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .weatherChanged(<span class="type">WeatherData</span>(<span class="number">80</span>, <span class="number">65</span>, <span class="number">30.4</span>f))</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .weatherChanged(<span class="type">WeatherData</span>(<span class="number">82</span>, <span class="number">70</span>, <span class="number">29.2</span>f))</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .weatherChanged(<span class="type">WeatherData</span>(<span class="number">78</span>, <span class="number">90</span>, <span class="number">29.2</span>f))</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>以上就是Scala实现的全部代码了。</p>
<p>开始分析之前，先做一个极其复杂的数学运算：<br>106行的Java代码，等价于54行Scala代码。<br>7个类，变成了3个。</p>
<p>下面开始正经的分析。</p>
<p>首先有一个叫做WeatherData的case class，它是完全不可变的。</p>
<p>其register方法，接受一个新的Observer作为参数，并产生一个新的包含比原来多一个Observer的WeatherData实例。</p>
<p>其weatherChanged方法接受一个新的气象数据，生成一个新的历史数据Seq，并把目前为止包含所有历史气象数据的Seq传递给每一个Observer去做处理。最后返回一个包含最新历史数据的新的WeatherData实例。</p>
<p>那么这些Observer具体是怎么定义的呢？</p>
<p>首先Observer只是一个type，不是一个class，它是没有状态的，用来定义函数签名。</p>
<p>三个具体的display仅仅是三个符合Observer签名的函数，它们都接受气象历史数据作为参数，在历史数据中各取所需，作出处理。都是没有任何副作用的。<br>这很合理，毕竟只是display，仅需要对数据进行分析和显示，只读不写，没有什么要改变已有数据的必要性。</p>
<p>最后一个main函数把所有代码串起来，就得到了一份没有任何可变性的代码。</p>
<h1 id="Mutation-vs-Transformation"><a href="#Mutation-vs-Transformation" class="headerlink" title="Mutation vs Transformation"></a>Mutation vs Transformation</h1><p>在Java版的代码中，不同的显示设备不断地根据最新的气象数据改变自己的状态，并根据改变之后的状态来决定其update的行为。</p>
<p>而在Scala代码中，不同的显示设备没有状态，它们都仅仅是函数而已。它们在每次气象变化时根据全部气象历史数据决定自己的行为。</p>
<p>全部代码中没有重新赋值语句，所有的赋值操作都是对局部变量的赋值，程序员可以感知到的变化就只在于observers列表和history列表的增长。而即便是这两个数据结构的增长都是通过不断生成新的不可变的Seq来实现的。</p>
<p>总结来说，Java版代码通过改变已有数据来达成行为的改变。而Scala代码则通过利用不可变的函数和不断生成不可变的数据来实现行为的改变。</p>
<p>这种不可变的代码于什么优势呢？</p>
<p>其好处在于需要程序员操心的事情更少。变化的点越少，麻烦事越少。</p>
<p>如果以上的Java代码有问题，程序员除了需要检查计算平均气温，最高最低气温，气压变化的算法之外，还需要检查重新赋值语句所造成的效果。气温的sum是否算对了？测温次数是否算错了？气压变化是否记录对了？这些都是变化的点，这些都是导致错误的可能性之所在。</p>
<p>而在Scala代码中，如果代码有问题，同样需要检查算法的正确性，也就是检查不可变的函数的正确性。除此之外，只需要检查history列表的增长就可以了。而一个列表的增长是很难出错的。</p>
<p>Java中所有对象状态的改变分散在代码中不同的地方，到了Scala代码中它们都集中到了一个列表的增长上，仅仅通过对这个列表的transformation就驱动了其余全部代码的行为改变。减少了变化的点，就减少了出错的可能情况的数量，减少了程序员的负担。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>解释器模式：OOP versus Functional Decomposition</title>
    <url>/blog/2015/06/05/interpreter-pattern-oop-versus-functional-decomposition/</url>
    <content><![CDATA[<h1 id="解释器模式"><a href="#解释器模式" class="headerlink" title="解释器模式"></a>解释器模式</h1><blockquote>
<p>In computer programming, the interpreter pattern is a design pattern that specifies how to evaluate sentences in a language. The basic idea is to have a class for each symbol (terminal or nonterminal) in a specialized computer language. The syntax tree of a sentence in the language is an instance of the composite pattern and is used to evaluate (interpret) the sentence for a client.</p>
</blockquote>
<p>以上是wiki对解释器模式的描述。</p>
<p>这是一个学术性稍强的模式，不太好找到生活化的比喻。</p>
<p>就直接上代码吧。</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Expression</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">interpret</span><span class="params">(Map&lt;String, Expression&gt; variables)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>首先有一个表达式的接口，定义一个求值的方法，该方法接收一个String -&gt; Expression的map。</p>
<p>可以猜到，这个map就是该表达式求值的时候需要用到的context。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Plus</span> <span class="keyword">implements</span> <span class="title">Expression</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Expression leftOperand;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Expression rightOperand;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Plus</span><span class="params">(Expression left, Expression right)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        leftOperand = left;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        rightOperand = right;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">interpret</span><span class="params">(Map&lt;String, Expression&gt; variables)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> leftOperand.interpret(variables) + rightOperand.interpret(variables);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Minus</span> <span class="keyword">implements</span> <span class="title">Expression</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Expression leftOperand;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Expression rightOperand;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Minus</span><span class="params">(Expression left, Expression right)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        leftOperand = left;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        rightOperand = right;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">interpret</span><span class="params">(Map&lt;String, Expression&gt; variables)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> leftOperand.interpret(variables) - rightOperand.interpret(variables);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Number</span> <span class="keyword">implements</span> <span class="title">Expression</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> number;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Number</span><span class="params">(<span class="keyword">int</span> number)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.number = number;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">interpret</span><span class="params">(Map&lt;String, Expression&gt; variables)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> number;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Variable</span> <span class="keyword">implements</span> <span class="title">Expression</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> String name;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Variable</span><span class="params">(String name)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.name = name;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">interpret</span><span class="params">(Map&lt;String, Expression&gt; variables)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (<span class="keyword">null</span> == variables.get(name)) <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> variables.get(name).interpret(variables);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>然后有表达式的四个实现类：加法表达式，减法表达式，数字表达式，还有变量。</p>
<p>数字表达式在求值的时候就直接返回它被创建时拿到的数字就好了，这是最简单的。</p>
<p>加法和减法的interpret方法在求值的时候仅仅是把行为委托给了两个子表达式，再对子表达式的求值结果做加减法。</p>
<p>变量在求值的时候则是去context里面寻找其name对应的表达式（也就是它所指向的表达式），然后对其求值。</p>
<p>下面是对它们的结合使用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">InterpreterExample</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Map&lt;String, Expression&gt; context = <span class="keyword">new</span> HashMap&lt;&gt;();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        context.put(<span class="string">"w"</span>, <span class="keyword">new</span> Number(<span class="number">5</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        context.put(<span class="string">"x"</span>, <span class="keyword">new</span> Number(<span class="number">10</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        context.put(<span class="string">"z"</span>, <span class="keyword">new</span> Number(<span class="number">42</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        Plus expr = <span class="keyword">new</span> Plus(<span class="keyword">new</span> Variable(<span class="string">"w"</span>),</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">new</span> Minus(<span class="keyword">new</span> Variable(<span class="string">"x"</span>),</span></pre></td></tr><tr><td class="code"><pre><span class="line">                        <span class="keyword">new</span> Variable(<span class="string">"z"</span>)));</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(expr.interpret(context));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>首先构造一个context，里面有w，x，z三个数字。然后计算w+(x-z)的值（看着像不像Lisp？）。</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>这些代码实际做的是什么事呢？</p>
<p>实际就是把一个以遇到Number为退出条件的递归算法拆碎了。</p>
<p>如果我们不把它拆碎，就写成递归函数会如何呢？</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>用Scala试着实现一下：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">Expr</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Plus</span>(<span class="params">left: <span class="type">Expr</span>, right: <span class="type">Expr</span></span>) <span class="keyword">extends</span> <span class="title">Expr</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Minus</span>(<span class="params">left: <span class="type">Expr</span>, right: <span class="type">Expr</span></span>) <span class="keyword">extends</span> <span class="title">Expr</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Number</span>(<span class="params">n: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">Expr</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Var</span>(<span class="params">name: <span class="type">String</span></span>) <span class="keyword">extends</span> <span class="title">Expr</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">object</span> <span class="title">ExprEval</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">eval</span></span>(expr: <span class="type">Expr</span>, context: <span class="type">Map</span>[<span class="type">String</span>, <span class="type">Expr</span>]): <span class="type">Int</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    expr <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Plus</span>(l, r) =&gt; eval(l, context) + eval(r, context)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Minus</span>(l, r) =&gt; eval(l, context) - eval(r, context)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Var</span>(name) =&gt; eval(context(name), context)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Number</span>(n) =&gt; n</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> context = <span class="type">Map</span>(<span class="string">"w"</span> -&gt; <span class="type">Number</span>(<span class="number">5</span>), <span class="string">"x"</span> -&gt; <span class="type">Number</span>(<span class="number">10</span>), <span class="string">"z"</span> -&gt; <span class="type">Number</span>(<span class="number">42</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> expr = <span class="type">Plus</span>(<span class="type">Var</span>(<span class="string">"w"</span>), <span class="type">Minus</span>(<span class="type">Var</span>(<span class="string">"x"</span>), <span class="type">Var</span>(<span class="string">"z"</span>)))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(eval(expr, context))</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>以上就是全部代码，与Java版等价。<br>递归函数很容易看懂，其退出条件也很容易看出来。</p>
<p>69行代码变成了26行。</p>
<p>四个case class代表四种表达式，其中并没有什么方法，只是用来作为数据的承载者。</p>
<p>一个eval函数，用pattern match来对四种表达式进行不同的处理。</p>
<p>不过这次我倒不是要宣扬说解释器模式属于是用不合适的工具解决问题。</p>
<p>而是要介绍两种组织代码的方式：按行组织还是按列组织。</p>
<h1 id="按行组织代码与按列组织代码"><a href="#按行组织代码与按列组织代码" class="headerlink" title="按行组织代码与按列组织代码"></a>按行组织代码与按列组织代码</h1><p>昨天我在看解释器模式，准备写一个Java实现，再写一个Scala实现，并以此来达到我黑Java的阴暗目的。</p>
<p>但是看了wiki上的示例代码后，马上就想起了去年上过的一门MOOC：<a href="https://www.coursera.org/course/proglang" target="_blank" rel="noopener">《Programming languages》</a>。<br>（这门课是由华盛顿大学的Dan Grossman教授讲授的，内容极好，强烈推荐。）</p>
<p>这门课里有一节就提到了上面说的两种组织代码的方式：按行组织还是按列组织。<br>这节课的视频在这里：<a href="https://www.youtube.com/watch?v=uEHnI3pq_FM" target="_blank" rel="noopener">https://www.youtube.com/watch?v=uEHnI3pq_FM）</a></p>
<p>比如我们上面的两版代码，Java代码把对表达式的求值分散在每个不同的表达式类里。</p>
<p>而Scala代码把求值代码集中写在一个函数里，pattern match每种表达式类型并求值。</p>
<p>如果要做成一个表格的话，就是这样的：</p>
<p><img src="http://ww2.sinaimg.cn/large/8b1ece2agw1esug8rpnudj207o0bwt8v.jpg" alt="table"></p>
<p>其中的问号代表具体的求值实现。</p>
<p>Java代码横向组织，有一个Plus类，里面有interpret方法，有一个Minus类，里面有interpret方法，等等。这是按照行组织。</p>
<p>而Scala代码则纵向组织，有一个eval函数，纵向把四种表达式的求值都包揽了。这是按列组织。</p>
<p>上面的表格太小，看着不明显，现在假设我们需要打印表达式的功能。那么表格就会变成这样：</p>
<p><img src="http://ww4.sinaimg.cn/large/8b1ece2agw1esugg4y9wij20ba0c43yu.jpg" alt="table2"></p>
<p>可以想象，Java代码里会在每个表达式类里加一个toString函数的实现。横向扩展，一个类把数据和算法组织在一起。</p>
<p>而在Scala代码里则会写一个toString的递归函数，包揽所有字符串打印的工作。纵向扩展，一个函数去分辨数据类型，并据此选择计算策略。</p>
<h1 id="OOP-versus-Functional-Decomposition"><a href="#OOP-versus-Functional-Decomposition" class="headerlink" title="OOP versus Functional Decomposition"></a>OOP versus Functional Decomposition</h1><p>那到底哪种组织方式更好呢？</p>
<p>并没有确定的答案，Dan Grossman教授在课程中给出的解释是这样的：</p>
<blockquote>
<p>FP and OOP often doing the same thing in exact opposite way: organize the program “by rows” or “by columns”.<br>Which is “most natural” may depend on what you are doing (e.g., an interpreter vs. a GUI) or personal taste.</p>
</blockquote>
<p>到底如何组织取决于你想要解决什么样的问题，比如你要做一个GUI库，那么数据与算法放在一起，互相接近是最自然的组织方式。这时选择OOP是最好的设计决策。</p>
<p>而如果你要实现的东西类似于本文中的解释器，那么一个递归的算法来统一处理所有表达式类型则是最自然的。这时选择Functional Decomposition是最好的设计决策。</p>
<h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>OOP与Functional Decomposition，这二者并不是完全对立的。</p>
<p>熟练掌握多种抽象与代码组织方式，正确识别应用场景，据此选择合适的范式，或者是选择多种范式结合使用，才是这一系列博文的真实用意。</p>
<p>只不过由于传统的OO设计模式过于盛行，FP范式接受度不够，才会有这一系列博文黑Java，捧Scala的表象。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>命令模式的不爽就像用指甲刀刮胡子</title>
    <url>/blog/2015/06/01/command-pattern-fp-implementation/</url>
    <content><![CDATA[<h1 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h1><blockquote>
<p>在面向对象程式设计的范畴中，命令模式是一种设计模式，它尝试以物件来代表实际行动。命令物件可以把行动(action) 及其参数封装起来，于是这些行动可以被：</p>
</blockquote>
<blockquote>
<ul>
<li>重复多次</li>
<li>取消（如果该物件有实作的话）</li>
<li>取消后又再重做</li>
</ul>
</blockquote>
<p>以上是wiki对命令模式的定义（术语像是台湾的）。</p>
<p>下面是来自《Head first design patterns》的一个例子：</p>
<p>假设你有很多家用电器：电灯泡，电视，音响，还有一个水疗浴缸。（就是没有手电筒）</p>
<p>每个家用电器都有自己的开关装置，处于不同的位置。如果你想把它们都开启，需要一个一个地去按按钮。</p>
<p>现在你想要有一个遥控器，一键开启所有电器，一键关闭所有电器。</p>
<p>或者是一键完成任意的电器操作组合。</p>
<p>每个电器的接口都是不同的，但是又需要和同一个遥控器集成，于是呢，肯定要有一个统一的接口了。</p>
<p>于是就有了下面命令模式的实现代码。</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/appliances/Light.java?slice=1:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/appliances/TV.java?slice=1:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/appliances/Stereo.java?slice=1:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/appliances/Hottub.java?slice=1:&footer=minimal">
</script>

<p>首先是有四大件家用电器。各自之间没有什么关系。</p>
<p>这里面的代码都有点傻，不过没关系，我们就想象这都是些很复杂的硬件通信之类的代码就好了。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/Command.java?slice=1:&footer=minimal">
</script>

<p>然后，定义一个Command接口，其中只有一个execute()方法。</p>
<p>之后我们会用它的实现类来操作各种电器。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/LightOnCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/LightOffCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/TVOnCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/TVOffCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/StereoOnCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/StereoOffCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/HottubOnCommand.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/commands/HottubOffCommand.java?slice=4:&footer=minimal">
</script>

<p>这一大坨，就是Command的实现了。</p>
<p>四大件电器，于是便有八个Command，分别负责每个电器的开启和关闭。</p>
<p>有些电器的开启和关闭比别的要复杂一些，不过这没有关系，因为它们的细节都被封装在Command的实现类里面了，我们接下来的代码只要和Command这个接口打交道就好了。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/MacroCommand.java?slice=1:&footer=minimal">
</script>

<p>还有一个宏命令，用来组合其他命令。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/runner/RemoteControl.java?slice=3:&footer=minimal">
</script>

<p>可以实现遥控器了。</p>
<p><img src="http://elisabethrobson.com/wp-content/uploads/2014/07/Command.jpg" alt="http://elisabethrobson.com/wp-content/uploads/2014/07/Command.jpg"></p>
<p>这个遥控器上的按钮都是空白的，我们可以给它置入任意我们想要的命令。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/command/runner/RemoteLoader.java?slice=9:&footer=minimal">
</script>

<p>终于可以写一个main函数了：</p>
<ul>
<li>把家用电器和其对应的Command联系起来</li>
<li>把各种Command组合成开启和关闭两个宏命令</li>
<li>把宏命令置入遥控器</li>
</ul>
<p>然后，只要按一个按钮，就可以开启所有电器，享受资产阶级奢靡的生活了。</p>
<p>享受够了之后只要再按一个按钮就可以把所有电器关闭掉。</p>
<p>如果再有别的电器，只需要实现几个新的Command，把新的Command组合入宏命令，继续使用遥控器就好了。</p>
<p>换句话说，因为遥控器和电器之间通过Command解耦了，增加新的电器和新的Command对于遥控器没有影响，遥控器的代码是稳定的。这也就是所谓的对扩展开放，对修改关闭。</p>
<p>很好，很符合良好的设计原则，看着就舒服对吧？</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>电灯的开启和关闭这两个命令仅仅是对电灯的两个方法的简单代理。</p>
<p>音响的开启和关闭这两个命令仅仅是对音响的两个方法的简单代理。</p>
<p>电视机的关闭也是简单的代理。</p>
<p>这些命令类是否看起来太单薄了呢？它们的方法异常瘦弱，营养不良。</p>
<p>它们除了持有一个需要操作的电器的实例之外，基本没有什么实例级状态。</p>
<p>（电视开机还好，由于需要选择频道，好歹调用了两个方法。<br>水疗浴缸操作比较复杂，需要调节温度，所以也还稍微好一些。）</p>
<p>每次看到这种贫血的类，我就怀疑它们存在的必要性。</p>
<p>如果我们只是想要给家用电器内的方法构造一个统一个的对外接口，是不是可以用函数式来实现呢？</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>来试试用Scala实现：</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/appliances/Light.scala?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/appliances/TV.scala?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/appliances/Stereo.scala?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/appliances/Hottub.scala?slice=1:&footer=minimal">
</script>

<p>首先是有四大件家用电器，这部分和Java的代码等价。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/Commands.scala?slice=3:&footer=minimal">
</script>

<p>这一段用来定义各种命令的代码就不同了。</p>
<p>我们对家用电器的各种方法的调用都是只期待其副作用，不期待任何返回值的。所以可以定义一个函数签名Command来涵盖所有这类操作。</p>
<p>和上面的Java代码类似，这里也有一个宏命令，只不过实现简单一些。</p>
<p>电视的开启，水疗浴缸的开和关都有对应的方法来把家用电器的实例封入闭包中。</p>
<p>咦？电灯的开关，音响的开关，以及电视的关闭都跑哪儿去了呢？</p>
<p>由于这几个操作都只涉及到一个方法的调用，它们直接就符合Command的函数签名，所以不用再封入任何闭包了。这一点看下面的代码就明白了。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/RemoteControl.scala?slice=3:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/commandFP/RemoteLoader.scala?slice=4:&footer=minimal">
</script>

<p>我们可以定义一个遥控器。其中有开启，和关闭两排按钮。</p>
<p>最后，可以写一个main函数，其中所做的事情和之前Java代码main函数所做的事情是一样的。</p>
<p>只不过，不需要创建各种Command的实例。</p>
<p>而且light.on，stereo.on，light.off，stereo.off，tv.off这几个方法由于符合Command的签名，是可以直接拿来当Command用的。（注意方法名后面没有()，不是调用，而是函数传递）</p>
<p>前后两版代码是等价的。只不过：</p>
<ul>
<li>247行代码变成了93行代码</li>
<li>16个实体变成了7个</li>
</ul>
<p>作为一个多按几个按钮都嫌麻烦的好逸恶劳的资产阶级，这个结果是我所乐于见到的。</p>
<p>更少，更紧凑的代码。更少的实体。我终于可以用更小的成本来享受我昂贵的家用电器了。</p>
<h1 id="指甲刀刮胡子"><a href="#指甲刀刮胡子" class="headerlink" title="指甲刀刮胡子"></a>指甲刀刮胡子</h1><p>最后回到标题上去：指甲刀刮胡子，意即用不合适的工具解决问题。</p>
<p>命令模式想要做到的事情其实就是给各种不同的操作寻找一个统一的接口，从而实现调用者（遥控器）和被调用者（家用电器）之间的解耦。</p>
<p>给不同的操作寻找一个统一的接口这件事可以通过接口来做，但是我们同时要承担写一堆贫血类的代价。</p>
<p>而如果直接用函数来做的话，则可以得到更紧凑简洁的代码（就像object Commands这个实体内的代码一样）。</p>
<p>该模式提出的时候FP并不如今日盛行，其作者选用了可能会导致贫血类泛滥的解决方案，这无可厚非。传播了解耦和开闭等良好设计的原则也实为功德。</p>
<p>不过今天我们有了剃须刀，就无需一定要用指甲刀来刮胡子了。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>职责链模式的别扭就像用门框夹核桃</title>
    <url>/blog/2015/05/30/fp-implementation-of-chain-of-responsibility/</url>
    <content><![CDATA[<h1 id="职责链模式"><a href="#职责链模式" class="headerlink" title="职责链模式"></a>职责链模式</h1><blockquote>
<p>责任链模式在面向对象程式设计里是一种软件设计模式，它包含了一些命令对象和一系列的处理对象。每一个处理对象决定它能处理哪些命令对象，它也知道如何将它不能处理的命令对象传递给该链中的下一个处理对象。该模式还描述了往该处理链的末尾添加新的处理对象的方法。</p>
</blockquote>
<p>以上是wiki对职责链模式的定义。</p>
<p>举个例子来说，我们的系统中需要记录日志的功能。日志需要根据优先级被发送到不同的地方。</p>
<p>低优先级的日志输出到命令行就好了。而高优先级的错误信息则需要通过邮件通知相关人员并且输出到命令行。</p>
<p>这个例子也是来自wiki的。</p>
<p>以下是wiki提供的Java实现：</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/chainofresponsibility/Logger.java?slice=1:&footer=minimal">
</script>

<p>首先定义一个Logger抽象类。从其setNext和message这两个方法可以看出，我们后面会把多个具有不同writeMessage实现的Logger链到一起，并且依次让它们处理某件需要被记录的事件。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/chainofresponsibility/StdoutLogger.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/chainofresponsibility/EmailLogger.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/chainofresponsibility/StderrLogger.java?slice=1:&footer=minimal">
</script>

<p>然后有三个Logger的实现，分别为向命令行输出消息，发送邮件（当然是假的），向命令行输出错误。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/chainofresponsibility/ChainOfResponsibilityExample.java?slice=1:&footer=minimal">
</script>

<p>最后，有一个main函数，创建三个Logger的实例，把它们通过setNext链在一起。 只需要调用一次message就可以让三个Logger依次工作。</p>
<p>如果以后再有更多的Logger呢，还是可以通过同样的方式把它们链接起来协同工作。</p>
<p>很好，很强大，很易于扩展，对吧？</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>这三个Logger的实现类看起来都非常的单薄，弱不禁风。</p>
<p>一个接收mask的构造函数，其唯一职责就是把接收到的mask传递给父类的构造函数。</p>
<p>然后父类根据mask和所发生事件优先级的大小关系决定到底要不要调用子类实现的writeMessage方法。</p>
<p>也就是说，子类完全没有定义自己的实例级状态，其实例级方法的行为也就谈不上随着其状态的变化而变化了。</p>
<p>换句话说，这几个子类存在的价值就在于为父类提供writeMessage这个函数。</p>
<p>啊。。。。。。！</p>
<p>一说到提供函数，我就想到了。。。。。。</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>我想到的自然是FP了，既然需要的是函数，那我们就使用函数好了。</p>
<p>何必用更重的抽象手段：类，去包裹函数呢？</p>
<p>下面就是比较偏函数式的Scala实现：</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/chain/Loggers.scala?slice=1:&footer=minimal">
</script>

<p>这个代码已经简短到我不想解释的程度了。不过还是解释一下吧。</p>
<p>三个log的的等级ERR，NOTICE和DEBUG和之前Java的实现是一样的。</p>
<p>一个case class Event，用来包裹需要被log的事件。</p>
<p>type Logger则是声明了一个函数签名，凡是符合这个签名的函数都可以作为logger被使用。</p>
<p>然后便是三个函数实现，它们将mask通过闭包封进函数内。这三个函数共同依赖一个私有handleEvent函数，其作用和Java代码中的message类似，判断mask和正在发生的事件之间优先级大小关系，并以此决定当前logger是否需要处理该事件。</p>
<p>哎？等一下，这个是职责链模式啊，那个啥，链在哪儿呢？就在main函数里。其中的andThen就可以把三个logger链在一起。</p>
<p>这个andThen是个什么东西？何以如此神奇？</p>
<p>欲知详情，请参考我之前的另一篇博客： <a href="http://cuipengfei.me/blog/2013/12/30/desugar-scala-9/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2013/12/30/desugar-scala-9/</a></p>
<p>而链接之后的结果本身也是一个函数，于是我们就可以调用chain并传入Event了。</p>
<p>这份代码和前面Java版的行为是等价的，输出是一致的。</p>
<h1 id="门框夹核桃"><a href="#门框夹核桃" class="headerlink" title="门框夹核桃"></a>门框夹核桃</h1><p>最后回到标题上去：门框夹核桃，意即用不合适的工具解决问题。</p>
<p>职责链模式想要做到的事情其实就是把多个函数链起来调用。</p>
<p>该模式提出的时候FP并不如今日盛行，其作者选用类来包装需要被链接的多个函数，这无可厚非。</p>
<p>无论是class，还是function，都是为程序员提供抽象的手段。当我们想要链接的东西就是多个function，选择直接用function而非class就会显得更加自然，也更加轻量且合适。</p>
<p>当年design pattern的作者广为传播各种patterns，实为功德。</p>
<p>不过今天我们有了核桃夹，就无需一定要用门框了。</p>
<p>最后，依照惯例，羞辱Java一小下下。<br>以上wiki提供的实现有77行，偏FP风的实现只有38行，只有一个实体Event。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>策略模式的尴尬就像用菜刀开啤酒</title>
    <url>/blog/2015/05/27/trait-and-fp-makes-strategy-pattern-irrelevant/</url>
    <content><![CDATA[<h1 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h1><blockquote>
<p>策略模式作为一种软件设计模式，指对象有某个行为，但是在不同的场景中，该行为有不同的实现算法。</p>
</blockquote>
<p>以上是中文wiki中对策略模式的定义。</p>
<blockquote>
<p>In computer programming, the strategy pattern (also known as the policy pattern) is a software design pattern that enables an algorithm’s behavior to be selected at runtime. The strategy pattern:</p>
</blockquote>
<ul>
<li>defines a family of algorithms,</li>
<li>encapsulates each algorithm, and</li>
<li>makes the algorithms interchangeable within that family.</li>
</ul>
<blockquote>
<p>Strategy lets the algorithm vary independently from clients that use it.</p>
</blockquote>
<p>以上是英文版的。</p>
<h1 id="鸭子"><a href="#鸭子" class="headerlink" title="鸭子"></a>鸭子</h1><p>这种偏学术性的描述实在太绕嘴，来思考一个实例：</p>
<p>我们需要创建一些鸭子，鸭子有什么行为呢？</p>
<ul>
<li>鸭子会飞</li>
<li>会叫</li>
<li>会游泳</li>
</ul>
<p>不过，是否所有的鸭子都是这样呢？万一是玩具鸭子呢？万一是猎人放在水里的用来勾引公鸭子的木质母鸭子呢？万一是外星来客太空鸭呢？</p>
<p>你已经知道什么意思了。</p>
<p>鸭子的各个子类的飞和叫的行为不尽相同。所以我们想把飞和叫这两种行为独立开来，让它们可以自由组合在鸭子的不同子类中。</p>
<p>以上例子来自著名的《Head first design patterns》。</p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><p>以下是《Head first design patterns》附带的代码：</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/fly/FlyBehavior.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/fly/FlyWithWings.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/fly/FlyNoWay.java?slice=1:&footer=minimal">
</script>

<p>飞行的接口，以及两个实现：一个真会飞，一个不会飞。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/quack/QuackBehavior.java?slice=1:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/quack/Quack.java?slice=1:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/quack/MuteQuack.java?slice=1:&footer=minimal">
</script>

<p>叫的接口，两个实现，一个真会叫，一个不会叫。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/ducks/Duck.java?slice=4:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/ducks/MallardDuck.java?slice=4:&footer=minimal">
</script>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/java/strategy/ducks/DecoyDuck.java?slice=4:&footer=minimal">
</script>

<p>最后，终于到了鸭子。鸭子的顶层抽象类声明两个字段，一个用来飞，一个用来叫。</p>
<p>这样在子类里就可以把这两个字段锁定到某个特定的实现，以实现任意的组合。</p>
<p>可以看到，绿头鸭（mallard）组合了真会飞和真会叫。而诱饵鸭（decoy，猎人用来勾引鸭子上钩的那个）则组合了不会飞和不会叫。</p>
<p>可以想象随着飞和叫这两个家族的扩大，我们可以组合出更多种类的鸭子来。</p>
<p>很好，很灵活，很强大，对吧？</p>
<h2 id="不过再想一下"><a href="#不过再想一下" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>我们想要的不过是把两个家族的不同行为塞到鸭子的子类里去。是否有更容易的办法来做到呢？</p>
<h1 id="trait"><a href="#trait" class="headerlink" title="trait"></a>trait</h1><p>一说到把行为塞到某个类里，就会想到mix in，很自然就想到了Scala的trait。</p>
<p>更多关于Scala的trait的详情请参考我的另一篇博客： <a href="http://cuipengfei.me/blog/2013/10/13/scala-trait/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2013/10/13/scala-trait/</a></p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/strategytrait/Fly.scala?slice=1:&footer=minimal">
</script>

<p>飞行家族。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/strategytrait/Quack.scala?slice=1:&footer=minimal">
</script>

<p>叫的行为的家族。</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/strategytrait/Duck.scala?slice=1:&footer=minimal">
</script>

<p>最后，鸭子的各种实现。</p>
<p>貌似和Java版的实现差距不大，飞和叫的interface和class变成了trait。</p>
<p>Duck原来是持有Fly和Quack的实例，现在则是变成了混入Fly和Quack这两个trait。</p>
<p>这个代码比Java短一些，紧凑一些，构造函数中的赋值变成了类型声明时的混入。</p>
<h2 id="不过再想一下-1"><a href="#不过再想一下-1" class="headerlink" title="不过再想一下"></a>不过再想一下</h2><p>我们不过是想要把某种行为塞入到某个类里面去，真的有必要用interface，class，trait来把这些行为包裹起来吗？</p>
<p>行为通常是以哪种形式承载的呢？</p>
<h1 id="functions"><a href="#functions" class="headerlink" title="functions"></a>functions</h1><p>行为通常是以函数承载的。</p>
<p>也就是说我们想要做的不过是把符合某个签名的函数塞到鸭子的子类里去而已，而却用interface，class，trait来把这些行为包裹起来了。有些臃肿不是吗？</p>
<p>下面是直接把函数塞入鸭子子类的做法：</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/blob/master/OODPFP/src/main/scala/strategyfp/Duck.scala?slice=3:&footer=minimal">
</script>

<p>Fly和Quack不再是interface或者是trait。而是type aliase。</p>
<p>Scala的type aliase就类似于C#的delegate，用来声明function signature。</p>
<p>更多关于type aliase的更多详情请参考我的另一篇博客： <a href="http://cuipengfei.me/blog/2013/12/23/desugar-scala-4/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2013/12/23/desugar-scala-4/</a></p>
<p>这样，会飞不会飞，会叫不会叫就无需被class或者trait包裹着了，直接就是一个个的函数。</p>
<p>鸭子的子类通过构造函数接收飞和叫的两个函数作为参数，就能够组合不同的行为了。</p>
<p>如果说之前triat的实现方式与Java实现版相比偏重了inheritance而不是composition，这一版的实现则又回到了纯composition的路上了。</p>
<p>紧凑程度，实体数量都比以上两版有改进。这一点从行数上可以窥见：Java版63行，trait版29行，最后一版21行。</p>
<h1 id="菜刀开啤酒"><a href="#菜刀开啤酒" class="headerlink" title="菜刀开啤酒"></a>菜刀开啤酒</h1><p>最后回到标题上去：菜刀开啤酒，意即用不合适的工具解决问题。</p>
<p>strategy patten要解决的问题其实就是如何把一族行为的不同实现注入到某个类里去。</p>
<p>这一点，最开头的wiki定义已经说的很明白了：</p>
<blockquote>
<p>Strategy lets the algorithm vary independently from clients that use it.</p>
</blockquote>
<p>无论是class，还是function，都是为程序员提供抽象的手段。当我们想要抽象的东西就是一段algorithm（正如wiki所说）的时候，用function来做抽象就是更加轻量且合适的选择。</p>
<p>该模式提出的时候FP并不如今日盛行，其作者选用纯OO的方式解决了问题，并广为传播，实为功德。</p>
<p>不过今天我们有了开瓶器，就无需一定要用菜刀了。</p>
<p>最后是一个Java 8的实现：</p>
<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/tree/master/OODPFP/src/main/java/strategyj8/Fly.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/tree/master/OODPFP/src/main/java/strategyj8/Quack.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/tree/master/OODPFP/src/main/java/strategyj8/BehaviorsRepo.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/tree/master/OODPFP/src/main/java/strategyj8/Duck.java?slice=1:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/tree/master/OODPFP/src/main/java/strategyj8/MallardDuck.java?slice=4:&footer=minimal">
</script>

<script src="http://gist-it.appspot.com/https://github.com/cuipengfei/BlogCode/tree/master/OODPFP/src/main/java/strategyj8/DecoyDuck.java?slice=4:&footer=minimal">
</script>

<p>看起来比最开始的那一版好一些，但是我还是看它不顺眼。</p>
<p>为什么呢？</p>
<p>一定是由于我强烈的偏见而没有其他任何原因，一定是这样的。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>OODP</tag>
      </tags>
  </entry>
  <entry>
    <title>Principles of Reactive Programming Week Two作业导学</title>
    <url>/blog/2015/04/26/principles-of-reactive-programming-week-2/</url>
    <content><![CDATA[<h1 id="声明"><a href="#声明" class="headerlink" title="声明"></a>声明</h1><p>这系列博文的目标读者仅限于报名参加了这门课并且看完了视频，看完了作业的instruction之后仍有困难的同学。</p>
<p>这系列博文不会公布作业的答案，那是违反Coursera的code of honor的。</p>
<p>我只会试着解释作业中已有的代码，以及应该如何入手。</p>
<p>其实，写这个系列博文对我的帮助比对读者的帮助要大。</p>
<p>这周的作业不太难，主要就是一个观察者模式。</p>
<h1 id="Signal是怎么work的？"><a href="#Signal是怎么work的？" class="headerlink" title="Signal是怎么work的？"></a>Signal是怎么work的？</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; <span class="keyword">val</span> a = <span class="type">Var</span>(<span class="number">1</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">a: calculator.<span class="type">Var</span>[<span class="type">Int</span>] = calculator.<span class="type">Var</span>@<span class="number">7</span>ca6f5b9</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; <span class="keyword">val</span> b = <span class="type">Var</span>(<span class="number">2</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">b: calculator.<span class="type">Var</span>[<span class="type">Int</span>] = calculator.<span class="type">Var</span>@<span class="number">2286</span>d26</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; <span class="keyword">val</span> c = <span class="type">Signal</span>(a() + b())</span></pre></td></tr><tr><td class="code"><pre><span class="line">c: calculator.<span class="type">Signal</span>[<span class="type">Int</span>] = calculator.<span class="type">Signal</span>@<span class="number">5</span>c60548d</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; c()</span></pre></td></tr><tr><td class="code"><pre><span class="line">res8: <span class="type">Int</span> = <span class="number">3</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; a()=<span class="number">10</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; c()</span></pre></td></tr><tr><td class="code"><pre><span class="line">res10: <span class="type">Int</span> = <span class="number">12</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; b()=<span class="number">20</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">scala&gt; c()</span></pre></td></tr><tr><td class="code"><pre><span class="line">res12: <span class="type">Int</span> = <span class="number">30</span></span></pre></td></tr></table></figure>

<p>如果能搞懂上面的代码是如何work的，作业题中需要用到Signal的地方就不会有太大问题了。</p>
<p>a=1，b=2，c=a+b，所以c就是3。</p>
<p>a变成10之后c就变成了12（10+2）。</p>
<p>b再变成20之后，c就变成了30（10+29）。</p>
<p>这个级联的变化是如何发生的呢？</p>
<p>有两个关键点：</p>
<ul>
<li>Signal的constructor</li>
<li>Signal的update方法</li>
</ul>
<p>先看Signal的constructor。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Signal</span>[<span class="type">T</span>](<span class="params">expr: =&gt; <span class="type">T</span></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="comment">//......</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>以上是它的签名，关键在于expr的类型签名，expr的类型不是T，而是=&gt;T。</p>
<p>这就意味着expr可以是任何类型为T的表达式，可以是一个字面量，也可以是任意复杂的代码块。</p>
<p>比如Signal(123)是可以的，Signal(complicatedMethodCall())也可以。</p>
<p>最上面那块代码中的val c = Signal(a() + b())就属于后一种。</p>
<p>a() + b()不会被立即求值成3然后传入Signal的constructor，而是整体作为一个可以被反复求值的表达式被记录在Signal的实例中。</p>
<p>constructor的入口参数可以被反复求值是级联变化的基础，那是什么触发了真正的变化呢？</p>
<p>那就是关键点之二：update方法。</p>
<p>update方法的妙处在于，如果一个类A有update方法，那么：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> x = <span class="keyword">new</span> <span class="type">A</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">x(y)=z</span></pre></td></tr></table></figure>

<p>在编译之后会变成这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> x = <span class="keyword">new</span> <span class="type">A</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">x.update(y,z)</span></pre></td></tr></table></figure>

<p>详情请见我之前的一篇博客：<a href="http://cuipengfei.me/blog/2014/06/12/scala-update-method/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2014/06/12/scala-update-method/</a></p>
<p>Signal的update方法是protected的，不可访问，所以它只可以从变，不可自变。</p>
<p>而Var把update方法public出来了，这样，在下面这样的代码执行时：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">a()=<span class="number">10</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">//a.update(10)</span></span></pre></td></tr></table></figure>

<p>a就会通知它的observers去重新求值。<br>这样就实现了a或者b这样的Var变化的时候，c这样的Signal跟着变化的效果。</p>
<p>搞懂了上面的内容就足以去做作业了。</p>
<h1 id="怎么和html页面结合起来的？"><a href="#怎么和html页面结合起来的？" class="headerlink" title="怎么和html页面结合起来的？"></a>怎么和html页面结合起来的？</h1><p>执行instruction里提到的webUI/fastOptJS这个task就会把Scala作业代码编译成js。</p>
<p>这个task是scalajs这个dependency带进来的（在webui.sbt里）。</p>
<p>webui这个项目里有一个CalculatorUI.scala文件，也会被编译成js。其中的代码就把作业代码和html的UI结合起来了。</p>
<p>就是这样了，这周的作业不难懂也不太难做。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>MOOC</tag>
        <tag>Reactive</tag>
      </tags>
  </entry>
  <entry>
    <title>twu pecha kucha</title>
    <url>/blog/2015/04/22/twu-pecha-kucha/</url>
    <content><![CDATA[<iframe src="//slides.com/pengfeicui/deck-4/embed" width="576" height="420" scrolling="no" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
]]></content>
      <tags>
        <tag>TWU</tag>
      </tags>
  </entry>
  <entry>
    <title>Principles of Reactive Programming Week One作业导学</title>
    <url>/blog/2015/04/19/principles-of-reactive-programming-week-one/</url>
    <content><![CDATA[<p>#前尘<br>Principles of Reactive Programming在4月13号又开课了。<br><a href="https://www.coursera.org/course/reactive" target="_blank" rel="noopener">https://www.coursera.org/course/reactive</a></p>
<p>上次开课是在2013年的11月，当时我刚第一次上完Functional programming principles in Scala，热情很高于是就报名参加了这门课。<br>还群发了一个邮件找人一起上课。</p>
<p>但是上了几周发现有点难，于是就放弃了。现在去bitbucket看，最后一次push停留在了2013-11-18。</p>
<p>后来还在上海被8x鄙视于无形之中。</p>
<h1 id="后世"><a href="#后世" class="headerlink" title="后世"></a>后世</h1><p>14年做了几个月的Scala开发，后来Functional programming principles in Scala再次开课又上了一遍，拿了个认证证书。</p>
<p>感觉似乎可以再挑战一次。</p>
<h1 id="今生"><a href="#今生" class="headerlink" title="今生"></a>今生</h1><p>上课习得的知识放在脑子里是不牢靠的。大脑有遗忘周期。</p>
<p>需要有成文或者成代码的产出，作为日后回忆和做spaced repetition的资料。</p>
<p>于是就有了这个即将成为系列的博文中的第一篇《Principles of Reactive Programming Week One作业导学》。</p>
<p>这系列博文的目标读者仅限于报名参加了这门课并且看完了视频，看完了作业的instruction之后仍有困难的同学。</p>
<p>这系列博文不会公布作业的答案，那是违反Coursera的code of honor的。</p>
<p>我只会试着解释作业中已有的代码，以及应该如何入手。</p>
<p>其实，写这个系列博文对我的帮助比对读者的帮助要大。</p>
<h1 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h1><h2 id="Heap-scala"><a href="#Heap-scala" class="headerlink" title="Heap.scala"></a>Heap.scala</h2><p>第一周的代码下载下来之后，先来看一下Heap.scala这个文件。</p>
<p>这个文件里定义了很多个trait。现在只需要关注其中一个Heap。</p>
<p>这个就是所有其他trait都会去extend的基类（这个说法合适吗？）。<br>它定义了所有Heap的实现者都需要实现的方法。</p>
<p>然后BinomialHeap完整实现了Heap定义的所有东西。</p>
<p>Bogus1BinomialHeap到Bogus5BinomialHeap都是继承自BinomialHeap，其中各自覆盖了BinomialHeap的不同方法，以不同的方式引入了bug。<br>第一周作业的目的就是用ScalaCheck把其中的bug找出来。</p>
<p>这个文件里还有一个IntHeap，这个稍后再说。</p>
<p>实现代码其实就只有这一个文件，接下来看测试代码。</p>
<h2 id="QuickCheckSuite-scala"><a href="#QuickCheckSuite-scala" class="headerlink" title="QuickCheckSuite.scala"></a>QuickCheckSuite.scala</h2><p>这个文件里主要定义了QuickCheckSuite这个测试类。</p>
<p>这个测试类继承自FunSuite，这是ScalaTest的测试基类。同时mix in了Checkers，这是ScalaTest为了与ScalaCheck集成而提供的trait。</p>
<p>接下来看测试的case：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">checkBogus</span></span>(p: <span class="type">Prop</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">var</span> foundBug = <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    check(p)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125; <span class="keyword">catch</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">case</span> e: <span class="type">TestFailedException</span> =&gt;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      foundBug = <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  assert(foundBug, <span class="string">"A bogus heap should NOT satisfy all properties. Try to find the bug!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">test(<span class="string">"Binomial heap satisfies properties."</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  check(<span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">BinomialHeap</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">test(<span class="string">"Bogus (1) binomial heap does not satisfy properties."</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  checkBogus(<span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">Bogus1BinomialHeap</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">test(<span class="string">"Bogus (2) binomial heap does not satisfy properties."</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  checkBogus(<span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">Bogus2BinomialHeap</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以看到，每个case都调用了check这个方法，或者是check的变体-checkBogus。</p>
<p>checkBogus里面则调用了check，并且assert说一定要出现TestFailedException异常了，测试才算成功。也就是说checkBogus的目的就是要在某些Heap的实现中找到bug。</p>
<p>现在来看check这个方法本身。它接受一个类型为Prop的参数，这些参数从哪儿来呢？这些参数就是：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">BinomialHeap</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">Bogus1BinomialHeap</span></span></pre></td></tr></table></figure>
<p>这些代码。</p>
<p>这就意味着QuickCheckHeap一定要是一个Prop，是不是这样呢？</p>
<h2 id="QuickCheckHeap-scala"><a href="#QuickCheckHeap-scala" class="headerlink" title="QuickCheckHeap.scala"></a>QuickCheckHeap.scala</h2><p>那就到QuickCheckHeap.scala这个文件中来看一下。</p>
<p>可以看到QuickCheckHeap这个抽象类确实是extends了Properties，而properties又extends了Prop。那么，没问题，这个类型是匹配的。</p>
<p>QuickCheckHeap里可以定义任意多个property，这些property将会检查Heap的实现正确与否。</p>
<p>而且它还mix in了IntHeap，就是前面略过的那个trait。它的目的是锁定Heap这个trait里所定义的A这个元素的类型到Int。</p>
<h2 id="全部连起来"><a href="#全部连起来" class="headerlink" title="全部连起来"></a>全部连起来</h2><p>第一周作业的已有代码很少，有用的就是这三个文件。</p>
<p>Heap.scala定义了很多个Heap的不同实现。有些是正确的，有些是有bug的。</p>
<p>QuickCheckSuite.scala则是测试的入口点，它由JunitRunner拽着跑起来。<br>其中的test case使用ScalaCheck去检查对于Heap这种数据结构恒定为true的properties是不是hold住的。</p>
<p>对于Heap这种数据结构恒定为true的properties从哪儿来呢？就来自于QuickCheckHeap.scala。<br>QuickCheckHeap本身是一个抽象类，不可以被实例化。但是由于有了牛逼的trait，就可以用这种代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">BinomialHeap</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">QuickCheckHeap</span> <span class="keyword">with</span> <span class="type">Bogus1BinomialHeap</span></span></pre></td></tr></table></figure>
<p>创建出实例，进行测试了。</p>
<p>最后，我们的任务就是在QuickCheckHeap.scala添加更多的properties，把所有实现有误的Heap都揪出来。</p>
<h1 id="题外话"><a href="#题外话" class="headerlink" title="题外话"></a>题外话</h1><p>有没有发现QuickCheckHeap.scala里面有些奇怪的代码？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">property(<span class="string">"min1"</span>) = forAll &#123; (heap: <span class="type">H</span>, a: <span class="type">A</span>) =&gt;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> min = <span class="keyword">if</span> (isEmpty(heap)) a <span class="keyword">else</span> findMin(heap)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  findMin(insert(min, heap)) == min</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>property(“min should be min”)，这看起来像是一个方法调用啊。</p>
<p>尼玛，方法调用后面怎么跟着一个等号啊？等号后面还有一个有返回值的表达式啊？</p>
<p>这是啥啊？</p>
<p>这是个乍一看很自然，但是仔细一想很费解的Scala语言特性-update方法。</p>
<p>详情请见我之前的一篇博客：<a href="http://cuipengfei.me/blog/2014/06/12/scala-update-method/" target="_blank" rel="noopener">http://cuipengfei.me/blog/2014/06/12/scala-update-method/</a></p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>MOOC</tag>
        <tag>Reactive</tag>
      </tags>
  </entry>
  <entry>
    <title>English workshop Wuhan 2015.04.14</title>
    <url>/blog/2015/04/11/wuhan-english-workshop/</url>
    <content><![CDATA[<iframe src="//slides.com/pengfeicui/deck/embed" width="576" height="420" scrolling="no" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
]]></content>
  </entry>
  <entry>
    <title>职业女性确实处于劣势吗？记一次不甚严谨的考据 -- 向胡适之先生的遥远致敬</title>
    <url>/blog/2015/04/04/women-in-finance-it-and-r-and-d/</url>
    <content><![CDATA[<h1 id="源起"><a href="#源起" class="headerlink" title="源起"></a>源起</h1><p>前两天，在一个武汉本地程序员聚集的技术社区微信群里某位群友发了两张图片：</p>
<p><img src="http://ww4.sinaimg.cn/mw690/8b1ece2agw1eqsw4fbiytj20fe0a6q8i.jpg" alt=""></p>
<p><img src="http://ww1.sinaimg.cn/mw690/8b1ece2agw1eqsw4dzf8ej20fe09v0z9.jpg" alt=""></p>
<p>这是某个IT公司的招聘宣传，为程序员提供的鼓励师。</p>
<p>（由于图片出现在愚人节期间，不确定该公司是真的有这样的人员配备，还是恶作剧的，此处暂且存疑）</p>
<p>马上群里就有一位X君跳出来说这种事情就是混蛋啊，怎么女人就得给男人端茶倒水擦汗啊。</p>
<p>另外一位Y君就说没有啊，这就是开个玩笑啊，不要这么较真啊。</p>
<p>X君继续说：！@#￥%……&amp;*啊</p>
<p>Y君回应：*&amp;……%￥#@！啊</p>
<p>于是，你也可以猜到的，这中间X君就说了IT行业对于女性从业者存在歧视，收入不平等之类的话。</p>
<p>这让我颇为感慨：武汉也无非是这样。武大的樱花烂熳的时节，群中却有这样标致极了的讨论。其实，又何止武汉呢？</p>
<p>正当X君与Y君酣战之时，有另外一位群友问声称存在收入歧视的X君是否有数据支持其观点。</p>
<p>恰巧我这周正在看《胡适文选》。胡适之先生反复提醒读者<strong>要有怀疑精神，凡事要讲求证据，要用科学的手段得出科学的结论。</strong></p>
<p><strong>胡适之先生之言于我心有戚戚焉，于是我便想要搜罗数据，深入了解一下这个话题，算作是对胡适之先生的遥远致敬。</strong></p>
<p>以上，为源起。</p>
<h1 id="定题"><a href="#定题" class="headerlink" title="定题"></a>定题</h1><p>近来我正尝试着自我疏离本性中近似于周树人先生的那一部分。</p>
<p>便不太愿用“歧视”这个颇为尖刻的词汇，因而本文的标题中用了“劣势”这一稍为中性的说法。</p>
<p>既然要考据，就不妨把话题放大些，不独观察IT行业的女性，莫若把视角扩宽到整个职业女性上去。</p>
<p>再加上我不是专业的考据家，并没有投入大量时间精力去搜索资料，交叉引证也不够详备，那就不能怕会过谦，也在标题上加上“不甚严谨”这几个字。</p>
<p>于是便有了这个颇显啰嗦的标题：<strong>《职业女性确实处于劣势吗？记一次不甚严谨的考据 – 向胡适之先生的遥远致敬》。</strong></p>
<p>是为题目由来。</p>
<h1 id="开篇"><a href="#开篇" class="headerlink" title="开篇"></a>开篇</h1><p>我是一个颇为庸俗的人，也时常会被称为理性的人。</p>
<p><strong>于是，别人看待职业女性的眼光、上司是否会给小鞋穿、同事是否会区别对待等等这些无法量化，难以考量的因素均不采用。</strong></p>
<p><strong>我就只认准了：</strong></p>
<p><strong>在一个行业中具有某种特征的人群占多数就可以被称作是有优势的</strong></p>
<p><strong>在一个行业中具有某种特征的人群挣钱多就可以被称作是有优势的</strong></p>
<p><strong>这两条考校标准。</strong></p>
<p>所以接下来通篇都围绕着<strong>人数多寡和挣钱多少</strong>展开。</p>
<h1 id="什么行业收入高？"><a href="#什么行业收入高？" class="headerlink" title="什么行业收入高？"></a>什么行业收入高？</h1><p><strong>中华人民共和国国家统计局，中国统计年鉴（2014）</strong>中有一个条目：<strong>按行业分城镇单位就业人员平均工资</strong>，以Excel格式提供：</p>
<p>（这一行Excel实在是太长了，我把它分开截了两张图）</p>
<p><img src="http://ww3.sinaimg.cn/large/8b1ece2agw1eqsy6hday7j21wc090q76.jpg" alt=""></p>
<p><img src="http://ww4.sinaimg.cn/large/8b1ece2agw1eqsy4ojncwj21dq09a0wn.jpg" alt=""></p>
<p>其中收入最高的三个行业标记为了红色，分别为金融，IT和科研。</p>
<p>原数据可以在这里找到：<br><a href="http://www.stats.gov.cn/tjsj/ndsj/2014/indexch.htm" target="_blank">http://www.stats.gov.cn/tjsj/ndsj/2014/indexch.htm</a></p>
<p>点击左侧的“四、就业和工资”然后点击第“4-15”项，里面可以下载Excel。</p>
<p>或者也可以直接通过这个链接下载Excel：<br><a href="http://www.stats.gov.cn/tjsj/ndsj/2014/zk/html/Z0415C.xls" target="_blank" rel="noopener">http://www.stats.gov.cn/tjsj/ndsj/2014/zk/html/Z0415C.xls</a></p>
<p>那接下来就按图索骥，考量这三个行业中女性的状况。</p>
<p>（为什么只有三个？笔者精力有限，只求管窥，不求完全覆盖）</p>
<h1 id="金融"><a href="#金融" class="headerlink" title="金融"></a>金融</h1><p>搜寻良久，实在是找不到国内的资料，只好拿些英文的资料作为旁证了。</p>
<p>以下是来自美国的<strong>公平就业机会委员会（Equal Employment Opportunity Commission,缩写为EEOC）</strong>2006年发布的一份报告中关于<strong>女性金融从业者比例</strong>的图表：</p>
<p><img src="http://ww2.sinaimg.cn/large/8b1ece2agw1eqtfg16y00j210q0gg7ez.jpg" alt=""></p>
<p>从最后一行的汇总信息可以看出，经理级别的职位，女性占18%左右。</p>
<p>专业从业者中，女性约为26%。</p>
<p>技术与销售类的职位则只有个位数的百分点。</p>
<p>但到了书记员，抄写员（Clerical，表中最后一列）这一类的职位，却有43%是女性。</p>
<p><strong>由此不难观察到，在美国，金融这个高薪行业中女性在做着勤务工作，升到经理职位的甚少。</strong></p>
<p>这份报告的出处：<br><a href="http://www.eeoc.gov/eeoc/statistics/reports/finance/finance.pdf" target="_blank">http://www.eeoc.gov/eeoc/statistics/reports/finance/finance.pdf</a></p>
<p>公平就业机会委员会的wiki页面：<a href="http://zh.wikipedia.org/wiki/公平就业机会委员会" target="_blank">http://zh.wikipedia.org/wiki/公平就业机会委员会</a></p>
<p>而另外一份来自于英国<strong>平等与人权委员会（Equality and Human Rights Commission，EHRC）</strong>2009年春季发布的报告则有些不同：</p>
<p><img src="http://ww3.sinaimg.cn/large/8b1ece2agw1eqtfj8tpjsj214010ahcb.jpg" alt=""></p>
<p>从这张<strong>男女性别比例</strong>的图表可以看出，英国的金融行业男女从业人数基本一比一，差距不大。<br>从最后一行的汇总数据来看，女性还比男性多一个百分点。</p>
<p>而下面这张出自同一报告的关于<strong>收入差距</strong>的图表，则显露了另外的信息：</p>
<p><img src="http://ww2.sinaimg.cn/large/8b1ece2agw1eqtfg31dloj218c14qe6v.jpg" alt=""></p>
<p>英国金融行业中，全职工作的男性年收入比全职工作的女性多55%。</p>
<p>而在全社会所有行业中，这个数字也有28%。</p>
<p><strong>可见在英国女性虽然以同等的人数参与进了金融行业，但是却没有拿到哪怕是接近同等的薪水。</strong></p>
<p>这份报告的出处：<br><a href="http://www.equalityhumanrights.com/sites/default/files/documents/download__finance_gender_analyis_research.pdf" target="_blank">http://www.equalityhumanrights.com/sites/default/files/documents/download__finance_gender_analyis_research.pdf</a></p>
<p>平等与人权委员会的wiki页面：<a href="http://en.wikipedia.org/wiki/Equality_and_Human_Rights_Commission" target="_blank">http://en.wikipedia.org/wiki/Equality_and_Human_Rights_Commission</a></p>
<h1 id="IT"><a href="#IT" class="headerlink" title="IT"></a>IT</h1><p>接下来开始看三大高收入行业中的第二名：IT行业中女性的状况。</p>
<p>我找到了一份来自美国的非营利机构：<strong>National Center for Women &amp; Information Technology (NCWIT)</strong>在2009年发布的报告。</p>
<p>（没找到这个机构确切的中文翻译，就保留原文吧）</p>
<p>报告中有一张<strong>女性IT从业人员比例随年份变化的趋势图</strong>：</p>
<p><img src="http://ww4.sinaimg.cn/large/8b1ece2agw1eqszx7ctf5j20vc0jujte.jpg" alt=""></p>
<p>容易看出，从上个世纪八十年代中期到九十年代初期，女性IT从业者比例在攀升，从30%增长到37%左右。</p>
<p>在此之后则一路下降，到2008年已经减少到了25%左右。</p>
<p>出自同一报告的还有另外一张<strong>男女收入差距随工作经验变化的趋势图</strong>：</p>
<p><img src="http://ww1.sinaimg.cn/large/8b1ece2agw1eqszx63er1j20vk0h8jto.jpg" alt=""></p>
<p>可以看出，入行初期男女收入没太大区别，但从第三年开始，逐渐拉开差距，由3%增加到12%。</p>
<p>好了，又是一个高薪行业。<strong>女性只占其中的四分之一，而且收入还比男性少。</strong></p>
<p>报告出处：<a href="http://www.ncwit.org/sites/default/files/legacy/pdf/NCWIT_TheFacts_rev2010.pdf" target="_blank">http://www.ncwit.org/sites/default/files/legacy/pdf/NCWIT_TheFacts_rev2010.pdf</a></p>
<p>NCWIT的wiki页面：<a target="_blank" href="http://en.wikipedia.org/wiki/National_Center_for_Women_%26_Information_Technology">http://en.wikipedia.org/wiki/National_Center_for_Women_%26_Information_Technology</a></p>
<h1 id="科研"><a href="#科研" class="headerlink" title="科研"></a>科研</h1><p>高薪行业之三，科研。</p>
<p>找到了两份来自欧盟的报告。</p>
<p>第一份报告中有一张<strong>科研行业中女性从业者比例</strong>的图表，数据采集自1999年：</p>
<p><img src="http://ww3.sinaimg.cn/large/8b1ece2agw1eqtgj4x3wdj20w80pcwhe.jpg" alt=""></p>
<p>不难看出，其中希腊和葡萄牙的女性科研工作人员较多，占有41%和43%。</p>
<p>德国和匈牙利则很低，女性只有14%到19%。</p>
<p>其他八个国家大致是落在26%到33%这个区间。</p>
<p>第二份报告发布于2012年，其中有一张<strong>男女收入差距</strong>的图表：</p>
<p><img src="http://ww3.sinaimg.cn/large/8b1ece2agw1eqtgj4s92yj20ty0t6wl8.jpg" alt=""></p>
<p>该图表数据统计于2002年和2006年，从中不难看出，女性在科研行业的各个分支中收入比男性低20%到40%。</p>
<p><strong>由此可见，在欧洲，科研行业作为一个高薪行业，其中女性从业人员较少。<br>即便进入这个行业的女性，其收入也要较男性低。</strong></p>
<p>第一份报告出处：<a href="https://ec.europa.eu/research/swafs/pdf/pub_gender_equality/wir_final.pdf" target="_blank">https://ec.europa.eu/research/swafs/pdf/pub_gender_equality/wir_final.pdf</a></p>
<p>第二份报告出处：<a href="http://ec.europa.eu/research/science-society/document_library/pdf_06/meta-analysis-of-gender-and-science-research-synthesis-report.pdf" target="_blank">http://ec.europa.eu/research/science-society/document_library/pdf_06/meta-analysis-of-gender-and-science-research-synthesis-report.pdf</a></p>
<p>发布报告的欧盟网站：<a href="http://ec.europa.eu/index_en.htm" target="_blank">http://ec.europa.eu/index_en.htm</a></p>
<h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p><strong>以上观察了三个薪水最高的行业：金融，IT和科研，这三个行业中都呈现出了女性从业人员少于男性，且收入低于男性的态势。</strong></p>
<p>如果这条结论和以上干巴巴的数据无法让您获得感性的认知的话，那我们再结合其他数据做个分析。</p>
<p>以下是来源于非营利组织<strong>National Association of Colleges and Employers (NACE)</strong>的一份报告中关于<strong>平均年工资涨幅</strong>的数据：</p>
<p><img src="http://ww1.sinaimg.cn/large/8b1ece2agw1eqtzeznwmwj21020eqwgt.jpg" alt=""></p>
<p>可以从最后一行看出，平均工资涨幅是每年7.5%。</p>
<p>这意味着什么呢？</p>
<figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line">[<span class="number">1</span>] pry(main)&gt; Math.log(<span class="number">1.55</span>,<span class="number">1.075</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">=&gt; <span class="number">6.059885534213904</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">[<span class="number">2</span>] pry(main)&gt; Math.log(<span class="number">1.12</span>,<span class="number">1.075</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">=&gt; <span class="number">1.5670305391527257</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">[<span class="number">3</span>] pry(main)&gt; Math.log(<span class="number">1.20</span>,<span class="number">1.075</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">=&gt; <span class="number">2.5210161634544224</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">[<span class="number">4</span>] pry(main)&gt; Math.log(<span class="number">1.40</span>,<span class="number">1.075</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">=&gt; <span class="number">4.652504958776575</span></span></pre></td></tr></table></figure>

<p>如果您不是IT行业的看不懂上面的代码没关系，我来解释一下。</p>
<p>这意味着，如果您是一名金融行业的女性从业者，您旁边座位上是一名和您同时进公司的男同事。<br>你们的关系很好，他甚至都不介意让您看他的工资单。这给在公司属于珍稀物种的您带来了不少宽慰。<br>但是经过分析自己历年的工资涨幅，您会发现如果您想要和他赚到一样多的钱的话，您还要再工作<strong>六年</strong>才行。</p>
<p>而这个数字在IT行业是<strong>一年半</strong>。</p>
<p>在科研行业是<strong>两年半</strong>到<strong>四年半</strong>。</p>
<p>以上引用报告出处：<a href="https://www.naceweb.org/uploadedFiles/Content/static-assets/downloads/executive-summary/2014-september-salary-survey-executive-summary.pdf" target="_blank">https://www.naceweb.org/uploadedFiles/Content/static-assets/downloads/executive-summary/2014-september-salary-survey-executive-summary.pdf</a></p>
<p>NACE的wiki页面：<a href="http://en.wikipedia.org/wiki/National_Association_of_Colleges_and_Employers" target="_blank">http://en.wikipedia.org/wiki/National_Association_of_Colleges_and_Employers</a></p>
<h1 id="然后呢？"><a href="#然后呢？" class="headerlink" title="然后呢？"></a>然后呢？</h1><p>以上仅仅是通过交叉引证来描述了职业女性的状况。是属于<strong>实证性的表述（positive statement）。</strong></p>
<p>而关于职业女性应该处于何种状况，那是属于<strong>规范性的表述（normative statement）</strong>，本文就不涉及了。</p>
<p>女性在这些高薪行业中人数少于男性，这是好事吗？这是坏事吗？</p>
<p>女性在这些高薪行业中收入低于男性，应该如何评价这件事呢？</p>
<p>金融，IT和科研，听起来都是理工宅男的专长啊，女的少不是属于正常现象吗？</p>
<p>女性的收入低于男性，那有可能是她们干活不给力啊，那收入低就是应该的吧？</p>
<p>所有这些问题，都属于<strong>价值判断</strong>。通过上面引用的数据，以及常识的积累，我对这些问题会有确定性的判断。<br>想来你也能猜到我的判断是什么。但是我不把它说出来，<strong>留待读者自己得出结论</strong>。</p>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>如果您觉得这篇博客写的还可以，请用手机支付宝扫描下面的二维码：</p>
<p><img src="https://tfsimg.alipay.com/images/mobilecodec/T1J3FfXbpsXXXXXXXX" alt=""></p>
<p>我会把收到的巨款用来置装，美容，健身。</p>
<p>然后穿的花枝招展，抹的五彩绚烂，露出两条人鱼线。</p>
<p>站在女程序员们旁边，给她们端茶倒水擦汗。</p>
<p>并且忘掉我也可以是一个独立的个体，也可以通过某种其他的方式体现自我价值。</p>
<p>成为一名雄性鼓励师，从此人生走上巅峰。</p>
<h1 id="最后的后面"><a href="#最后的后面" class="headerlink" title="最后的后面"></a>最后的后面</h1><p>最后的后面怎么还有呢？因为标题已经啰嗦了，索性结尾也啰嗦一下。</p>
<p>说是不严谨的考据，但是还是用了十几个番茄钟，五六次git commit，三四次审校。</p>
<p><img src="http://ww2.sinaimg.cn/large/8b1ece2agw1eqtkcps0zaj21g60zejya.jpg" alt=""></p>
<p><img src="http://ww4.sinaimg.cn/large/8b1ece2agw1eqtkec0012j21fq0ku7a1.jpg" alt=""></p>
<p>七易其稿也不过是如此的两倍嘛。</p>
<p>不过从此以后，我要与这样的辛劳说再见了。我要成为一个靠性别，靠脸，靠身材吃饭的男人。</p>
<p>所以，你懂的，趁我还能靠智识谋生，扫码吧。</p>
<p>别跑，说的就是你，别骗我，我知道你余额宝收益好几十块钱一天呢。</p>
<p>难道你就不想为一位志存高远的未来雄性鼓励师提供一些帮助吗？</p>
<p>（胆敢说我最终还是免不了被周树人附体的善款要x2）</p>
<p>现在，请回到页面的最上端，再看一遍那两张图片，请问您现在看到的东西和读本文之前还一样吗？</p>
]]></content>
  </entry>
  <entry>
    <title>2015第一季度</title>
    <url>/blog/2015/03/29/2015-first-season/</url>
    <content><![CDATA[<p>时间是以何种方式流逝的呢？</p>
<p>三个月瞬间就不见了。</p>
<p>对照着去年的总结和当时对未来的期望写一下2015年第一季度吧。</p>
<h1 id="博客"><a href="#博客" class="headerlink" title="博客"></a>博客</h1><p>惨不忍睹。</p>
<p>除了一个应付任务的tech radar的session写在了博客上，其他啥都没写。</p>
<p>去年博客有产出是因为在刷Scala这个主线任务。</p>
<p>今年做的都是支线任务，这一点那一点，难以形成有效的产出。</p>
<p>要形成有效的产出，需要有plan，有execution，有retro。</p>
<p>兴之所至，就把今年的博客主线任务定为OO与FP的比较和结合应用吧。</p>
<h1 id="读书"><a href="#读书" class="headerlink" title="读书"></a>读书</h1><p>不错。</p>
<p>第一个季度已经读完10本书。数量达标。</p>
<p>说过要做的笔记也做了。</p>
<p>笔记的作用确实很好，每隔一段时间回顾一次，spaced repetition可以促进和加深记忆。<br>不会再有“卧槽，这本书我看过吗？作者都讲了些啥啊？”的尴尬事。</p>
<p>看完了《经济学原理》的微观分册，这是最近几年来读过的最大部头的一本书。鉴于之前一直没有耐心读厚书，这可以算作是一个进步。</p>
<p>开始把待读的书按照内容领域和期待得到的效果分类放到豆列里。这样每次没书看了就去想读的豆列里挑一本。不用每次都700选一了。</p>
<p>读书的领域扩张也在按照之前的计划进行，没有风险。</p>
<p>博客需要有主线任务，读书要配合，可以优先选一些讲paradigm的书来读。</p>
<h1 id="MOOC"><a href="#MOOC" class="headerlink" title="MOOC"></a>MOOC</h1><p>尴尬。</p>
<p>这三个月一个mooc都没有跟。4月13日，reactive programming课要开，这个一定要跟，而且一定要跟完。</p>
<p>估计完成当时6门课的目标有点悬了。</p>
<p>非常无耻地修改一下mooc的目标吧：配合主线任务，以读书和mooc作为输入，博客作为产出。<br>数量和领域都不做具体限定。<br>（果然够无耻）</p>
<h1 id="体重"><a href="#体重" class="headerlink" title="体重"></a>体重</h1><p>稳定。</p>
<p>上次写年终总结的时候是61.1公斤，现在是61.0公斤。</p>
<p>当时写的是：</p>
<blockquote>
<p>明年没有太多改进的目标，维持就ok了。</p>
</blockquote>
<p>没有了减重目标，没有了改进方向，效果果然是不会自动出现的啊。<br>这是很有意思的一个现象，没有了改进的意愿，或许潜意识里对热量摄入和消耗都没有那么敏感和严格。</p>
<p>还好胸在变大，肱三在变大，腹肌在浮出水面。</p>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>写个阶段性总结还是有用，发掘出了主线任务。</p>
]]></content>
      <tags>
        <tag>me</tag>
      </tags>
  </entry>
  <entry>
    <title>Tech radar 武汉 microservice envy</title>
    <url>/blog/2015/01/13/tech-radar-wuhan/</url>
    <content><![CDATA[<iframe src="//slides.com/pengfeicui/microservice-envy/embed" width="576" height="420" scrolling="no" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
]]></content>
  </entry>
  <entry>
    <title>2014年总结</title>
    <url>/blog/2014/12/27/2014/</url>
    <content><![CDATA[<p>2014年即将结束，需要做一些总结。</p>
<p>既然总结是写在博客上的，第一项就先说博客吧。</p>
<h1 id="博客"><a href="#博客" class="headerlink" title="博客"></a>博客</h1><p>2014年写了18篇博客，其中15篇和Scala有关，自认为都是有且仅有干货的。</p>
<p>对此，我比较_<strong>满意</strong>_。</p>
<p>不过这个数字存在欺骗性，15篇Scala的博客，其中4篇写于1月份，6篇写于6月份，其余的零散的写就与其他月份。</p>
<p>66.66…%集中地爆发于两个月里，其余十个月只贡献了总体的33.33…%</p>
<p>可见写博客这件事于我而言并没有形成持久的习惯，只是随激情而来的自我娱乐。</p>
<p>明年可以_<strong>改进</strong>_的是不要嫌话题小，不要嫌话题不够深。<br>有了有价值的想法就记下来，形成惯性。</p>
<h1 id="读书"><a href="#读书" class="headerlink" title="读书"></a>读书</h1><p><img src="http://i3.tietuku.com/363db6f06852244e.png" alt="alt text"></p>
<p>38本，加上马上快读完的一本Ruby的书，算是39本。</p>
<p>对这个数字，我比较_<strong>满意</strong>_。</p>
<p>但是其中直接或间接与技术相关的只有8本，只占20%。</p>
<p>所以，和上面一节类似，单独观察总体数字本身是极具欺骗性的。</p>
<p>另一个我不满意的是读书的结果。把这39本书的封面罗列出来放到面前，我都能记得我看过这本书，但是有很多我都记不起其主要观点是什么。</p>
<p>所以明年的一个_<strong>改进</strong>_点是做笔记和书评，有利于记忆和吸收。</p>
<p>另外一个_<strong>改进</strong>_点是领域，我明年需要看一些轻量级的经济、哲学和社会心理学的书。口说无凭，于此立字为据。</p>
<h1 id="MOOC"><a href="#MOOC" class="headerlink" title="MOOC"></a>MOOC</h1><p><img src="http://i2.tietuku.com/98b280b6a1ac2512.png" alt=""></p>
<p>上图来自MOOC学院，data visualization做的很漂亮，不过没有分年统计。</p>
<p>2014年实际只上完了4门课。</p>
<p>对这些课程的质量，从中的收获我都比较_<strong>满意</strong>_。</p>
<p>不太满意的是数字，明年_<strong>改进</strong>_的目标定在6。</p>
<p>与读书不同，上课需要的时间比较长。有的课会持续两个月。一年能上完的课不会太多，领域不宜太广。</p>
<p>暂定技术相关的至少3门，其他三门如果有特别好的非技术课就上一下，没有的话就还是上技术课。给自己留一些随性自由发挥的空间：）</p>
<h1 id="Well-being"><a href="#Well-being" class="headerlink" title="Well-being"></a>Well-being</h1><p>今年体重从72.5公斤降到了61.1公斤。</p>
<p>最近三个月的数据统计</p>
<img src="http://i3.tietuku.com/f020ea0a6adc01a6.png" style="width: 500px;"/>

<p>上张一个月前的裸照：）</p>
<img src="http://i3.tietuku.com/df10eb25abd486c8.png" style="width: 200px;"/>

<p>明年没有太多改进的目标，维持就ok了。</p>
<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p>脾气还是不好，察言观色并据此反应的能力还是差。</p>
<p>今年试过的手段有降低分贝，减慢语速，少用激进词汇，延长反应时间，多听少说。</p>
<p>有效果，但不是很明显。执行不够有效，自我监控不够严格。</p>
<p>对此，我_<strong>不满意</strong>_。</p>
<p>像这种与天性作斗争的行为，其过程一定是困难，漫长，且鲜有正面自我回馈的。</p>
<p>明年需要_<strong>改进</strong>_的是，持续以上改进手段的执行，增加自控强度。</p>
]]></content>
      <tags>
        <tag>me</tag>
      </tags>
  </entry>
  <entry>
    <title>2015武汉郑大晔校第二次课</title>
    <url>/blog/2014/11/16/zdyx2015wuhansecondclass/</url>
    <content><![CDATA[<iframe src="//slides.com/pengfeicui/285849/embed" width="576" height="420" scrolling="no" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
]]></content>
  </entry>
  <entry>
    <title>Spark RDD的fold和aggregate为什么是两个API？为什么不是一个foldLeft？</title>
    <url>/blog/2014/10/31/spark-fold-aggregate-why-not-foldleft/</url>
    <content><![CDATA[<p>大家都知道Scala标准库的List有一个用来做聚合操作的foldLeft方法。</p>
<p>比如我定义一个公司类：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Company</span>(<span class="params">name:<span class="type">String</span>, children:<span class="type">Seq</span>[<span class="type">Company</span>]=<span class="type">Nil</span></span>)</span></span></pre></td></tr></table></figure>
<p>它有名字和子公司。<br>然后定义几个公司：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> companies = <span class="type">List</span>(<span class="type">Company</span>(<span class="string">"B"</span>),<span class="type">Company</span>(<span class="string">"A"</span>),<span class="type">Company</span>(<span class="string">"T"</span>))</span></pre></td></tr></table></figure>

<p>三家大公司，然后呢，我假设有一家超牛逼的公司把它们给合并了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">companies.foldLeft(<span class="type">Company</span>(<span class="string">"King"</span>))((king,company)=&gt;<span class="type">Company</span>(name=king.name,king.children:+company))</span></pre></td></tr></table></figure>

<p>这个执行的结果是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; companies.foldLeft(<span class="type">Company</span>(<span class="string">"King"</span>))((king,company)=&gt;<span class="type">Company</span>(name=king.name,king.children:+company))</span></pre></td></tr><tr><td class="code"><pre><span class="line">res6: <span class="type">Company</span> = <span class="type">Company</span>(<span class="type">King</span>,<span class="type">List</span>(<span class="type">Company</span>(<span class="type">B</span>,<span class="type">List</span>()), <span class="type">Company</span>(<span class="type">A</span>,<span class="type">List</span>()), <span class="type">Company</span>(<span class="type">T</span>,<span class="type">List</span>())))</span></pre></td></tr></table></figure>

<p>可见foldLeft的结果是一家包含了BAT三大家得新公司。</p>
<p>由List[Company]聚合出一个新的Company，这种属于foldLeft的同构聚合操作。</p>
<p>同时，foldLeft也可以做异构的聚合操作：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">companies.foldLeft(<span class="string">""</span>)((acc,company)=&gt;acc+company.name)</span></pre></td></tr></table></figure>

<p>它的执行结果是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; companies.foldLeft(<span class="string">""</span>)((acc,company)=&gt;acc+company.name)</span></pre></td></tr><tr><td class="code"><pre><span class="line">res7: <span class="type">String</span> = <span class="type">BAT</span></span></pre></td></tr></table></figure>

<p>由List[Company]聚合出一个String。</p>
<p>这样的API感觉很方便，只要是聚合，无论同构异构，都可以用它来做。</p>
<p>最近接触了Spark，其中的RDD是做分布式计算时最常用的一个类。</p>
<p>RDD有一个叫做fold的API，它和foldLeft的签名很像，唯一区别是它只能做同构聚合操作。</p>
<p>也就是说如果你有一个RDD[X]，通过fold，你只能构造出一个X。</p>
<p>如果我想通过一个RDD[X]构造一个Y出来呢？</p>
<p>那就得用aggregate这个API了，aggregate的签名是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">aggregate[<span class="type">U</span>](zeroValue: <span class="type">U</span>)(seqOp: (<span class="type">U</span>, <span class="type">T</span>) ⇒ <span class="type">U</span>, combOp: (<span class="type">U</span>, <span class="type">U</span>) ⇒ <span class="type">U</span>)(<span class="keyword">implicit</span> arg0: <span class="type">ClassTag</span>[<span class="type">U</span>]): <span class="type">U</span></span></pre></td></tr></table></figure>

<p>它比fold和foldLeft多需要一个combOp做参数。</p>
<p>这让我很不解，同构和异构的API干嘛非得拆成两个呢？怎么不能学Scala的标准库，把它做成类似foldLeft的样子呢？</p>
<p>后来想明白了，这是由于Spark需要分布运算造成的。</p>
<p>先想一下Scala List的foldLeft是怎么工作的？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">companies.foldLeft(<span class="type">Company</span>(<span class="string">"King"</span>))((king,company)=&gt;<span class="type">Company</span>(name=king.name,king.children:+company))</span></pre></td></tr></table></figure>

<ol>
<li>拿到初始值，即名字为king的公司，把它和list中的第一个公司合并，成为一个包含一家子公司的新公司</li>
<li>把上一步中的新公司拿来和list中的第二个公司合并，成为一个包含两家子公司的新公司</li>
<li>把上一步中的新公司拿来和list中的第三个公司合并，成为一个包含三家子公司的新公司</li>
</ol>
<p>这是同构的过程。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">companies.foldLeft(<span class="string">""</span>)((acc,company)=&gt;acc+company.name)</span></pre></td></tr></table></figure>

<ol>
<li>拿到初始值，即空字符串，把它和list中的第一个公司的名字拼在一起，成为B</li>
<li>把上一步中的B第二个公司名字拼一起，成为BA</li>
<li>把上一步中的BA拿来和list中的第三个公司的名字拼一起，成为BAT</li>
</ol>
<p>这是异构的过程。</p>
<p>像多米诺骨牌一样，从左到右依次把list中的元素吸收入结果中。</p>
<p>现在假设RDD[X]中有一个类似foldLeft的API，其签名和foldLeft一致，我现在调用foldLeft，给它一个f:(Y,X)=&gt;Y，接下来该发生什么呢？</p>
<ol>
<li>因为要分布计算，所以我先要把手里的很多个X分成几份，分发到不同的节点上去</li>
<li>每个节点把拿到的很多个X计算出一个Y出来</li>
<li>把所有节点的结果拿来，这时我手里就有了很多个Y</li>
<li>啊。。。我不知道怎么把很多个Y变成一个Y啊。。。</li>
</ol>
<p>由于Spark的RDD不像Scala的List一样只需要推倒一副多米诺骨牌，而是要推倒很多副，最后再对很多副多米诺骨牌的结果做聚合。</p>
<p>这时如果是同构还好，我只需要再用f:(X,X)=&gt;X做一遍就ok了。</p>
<p>但是如果是异构的，那我就必须得再需要一个f:(Y,Y)=&gt;Y了。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>ScalaInAction</tag>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala中Stream的应用场景及其实现原理</title>
    <url>/blog/2014/10/23/scala-stream-application-scenario-and-how-its-implemented/</url>
    <content><![CDATA[<h1 id="假设一个场景"><a href="#假设一个场景" class="headerlink" title="假设一个场景"></a>假设一个场景</h1><p>需要在50个随机数中找到前两个可以被3整除的数字。</p>
<p>听起来很简单，我们可以这样来写：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">randomList</span> </span>= (<span class="number">1</span> to <span class="number">50</span>).map(_ =&gt; <span class="type">Random</span>.nextInt(<span class="number">100</span>)).toList</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">isDivisibleBy3</span></span>(n: <span class="type">Int</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> isDivisible = n % <span class="number">3</span> == <span class="number">0</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  println(<span class="string">s"<span class="subst">$n</span> <span class="subst">$isDivisible</span>"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  isDivisible</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">randomList.filter(isDivisibleBy3).take(<span class="number">2</span>)</span></pre></td></tr></table></figure>

<p>一个产生50个随机数的函数；</p>
<p>一个检查某数字是否能被3整除的函数；</p>
<p>最后，对含有50个随机数的List做filter操作，找到其中所有能够被3整除的数字，取其中前两个。</p>
<p>把这段代码在Scala的console里面跑一下，结果是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; randomList.filter(isDivisibleBy3).take(<span class="number">2</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">31</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">71</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">95</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">7</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">38</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">48</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">88</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">52</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">2</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">27</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">90</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">55</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">96</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">91</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">82</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">83</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">8</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">51</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">96</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">27</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">12</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">76</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">17</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">53</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">54</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">70</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">29</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">49</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">12</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">83</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">18</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">6</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">7</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">76</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">51</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">95</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">76</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">85</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">87</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">84</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">44</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">44</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">89</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">84</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">42</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">44</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">0</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">23</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">35</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">55</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">res34: <span class="type">List</span>[<span class="type">Int</span>] = <span class="type">List</span>(<span class="number">48</span>, <span class="number">27</span>)</span></pre></td></tr></table></figure>

<p>其最终结果固然是没有问题，找到了48和27这两个数字。但是非常明显的可以看出，isDivisibleBy3被调用了50次，找到了远多于两个的能被3整除的数字，但是最后我们只关心其中前两个结果。</p>
<p>这似乎有点浪费，做了很多多余的运算。</p>
<p>对于这个例子来说，这还没什么，我们的List很小，判断整除于否也不是什么耗时操作。</p>
<p>但是如果List很大，filter时所做的运算很复杂的话，那这种做法就不可取了。</p>
<h1 id="现有解法的优缺点"><a href="#现有解法的优缺点" class="headerlink" title="现有解法的优缺点"></a>现有解法的优缺点</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">randomList.filter(isDivisibleBy3).take(<span class="number">2</span>)</span></pre></td></tr></table></figure>
<p>这行代码有一个优点：</p>
<p>用描述性、声明性的语言描述了我们要做的事是什么，而无需描述怎么做。我们只需说先用filter过滤一下，然后拿前两个，整件事就完成了。</p>
<p>但是它同时也有一个缺点：</p>
<p>做了多余的运算，浪费资源，而且这个缺点会随着数据量的增大以及计算复杂度的增加而更为凸显。</p>
<h1 id="试着解决其缺点"><a href="#试着解决其缺点" class="headerlink" title="试着解决其缺点"></a>试着解决其缺点</h1><p>解决多余运算的思路很简单，不要过滤完整个List之后再取前两个。而是在过滤的过程中如果发现已经找到两个了，那剩下的就忽略掉不管了。</p>
<p>顺着这个思路很容易写出如下很像Java的代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">first2UsingMutable</span></span>: <span class="type">List</span>[<span class="type">Int</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> result = <span class="type">ListBuffer</span>[<span class="type">Int</span>]()</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  randomList.foreach(n =&gt; &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (isDivisibleBy3(n)) result.append(n)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (result.size == <span class="number">2</span>) <span class="keyword">return</span> result.toList</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  result.toList</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>创建一个可变的List，开始遍历随机数，找到能被3整除的就把它塞进可变List里面去，找够了两个就返回。</p>
<p>执行的结果如下：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; first2UsingMutable</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">31</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">89</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">21</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">29</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">12</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">res35: <span class="type">List</span>[<span class="type">Int</span>] = <span class="type">List</span>(<span class="number">21</span>, <span class="number">12</span>)</span></pre></td></tr></table></figure>

<p>可以看到，运算量确实变少了，找够了两个就直接收工了。</p>
<p>但是这实在很糟糕，显式使用了return同时还引入了可变量。</p>
<p>有什么东西像是一个foreach循环而又可以不引入可变量呢？fold</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">first2UsingFold</span></span>: <span class="type">List</span>[<span class="type">Int</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  randomList.foldLeft(<span class="type">Nil</span>: <span class="type">List</span>[<span class="type">Int</span>])((acc, n) =&gt; &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (acc.size == <span class="number">2</span>) <span class="keyword">return</span> acc</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (isDivisibleBy3(n)) n :: acc</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">else</span> acc</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>执行：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; first2UsingFold</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">98</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">77</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">68</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">93</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">93</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">res36: <span class="type">List</span>[<span class="type">Int</span>] = <span class="type">List</span>(<span class="number">93</span>, <span class="number">93</span>)</span></pre></td></tr></table></figure>

<p>效果和上面一段代码类似，没有多余的运算。但是由于需要early termination，所以还是摆脱不了return。</p>
<p>这两种解法在去除多余运算这个缺点的同时也把原来的优点给丢掉了，我们又退化回了描述如何做而不是做什么的程度了。</p>
<h1 id="如何保持代码的表意性而又不用做多余运算呢？"><a href="#如何保持代码的表意性而又不用做多余运算呢？" class="headerlink" title="如何保持代码的表意性而又不用做多余运算呢？"></a>如何保持代码的表意性而又不用做多余运算呢？</h1><p>其实类似的问题是有套路化的解决方案的：使用Stream。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">randomList.toStream.filter(isDivisibleBy3).take(<span class="number">2</span>).toList</span></pre></td></tr></table></figure>

<p>这行代码执行的结果：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">scala&gt; randomList.toStream.filter(isDivisibleBy3).take(<span class="number">2</span>).toList</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">86</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">15</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">53</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">20</span> <span class="literal">false</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">93</span> <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">res42: <span class="type">List</span>[<span class="type">Int</span>] = <span class="type">List</span>(<span class="number">15</span>, <span class="number">93</span>)</span></pre></td></tr></table></figure>

<p>可见没有多余运算了，而且这行代码和最初代码极为相似，都是通过描述先做filter再做take来完成任务的。缺点没有了，优点也保留了下来。</p>
<p>这同样都是filter和take，代码跟代码的差距咋就这么大呢？</p>
<p>答案就是：因为Stream利用了惰性求值（lazy evaluation），或者也可以称之为延迟执行（deferred execution）。</p>
<p>接下来就看一下这两个晦涩的名词是如何帮助Stream完成工作的吧。</p>
<h1 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h1><p>在这里我借用一下Functional programming in Scala这本书里对Stream实现的代码，之所以不用Scala标准库的源码是因为我们只需要实现filter，take和toList这三个方法就可以展示Stream的原理，就不需要动用重型武器了。</p>
<p>先假设我们自己实现了一个MyStream，它的用法和Stream是类似的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">MyStream</span>(randomList: _*).filter(isDivisibleBy3).take(<span class="number">2</span>).toList</span></pre></td></tr></table></figure>

<p>以这一行代码为引子，我们来开始解剖MyStream是如何工作的。</p>
<h1 id="类型签名"><a href="#类型签名" class="headerlink" title="类型签名"></a>类型签名</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">MyStream</span>[+<span class="type">A</span>] </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	. . . . . .</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">object</span> <span class="title">Empty</span> <span class="keyword">extends</span> <span class="title">MyStream</span>[<span class="type">Nothing</span>]</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Cons</span>[+<span class="type">A</span>](<span class="params">h: (</span>) <span class="title">=&gt;</span> <span class="title">A</span>, <span class="title">t</span></span>: () =&gt; <span class="type">MyStream</span>[<span class="type">A</span>]) <span class="keyword">extends</span> <span class="type">MyStream</span>[<span class="type">A</span>]</span></pre></td></tr></table></figure>

<p>一个trait叫做MyStream，其中的内容我们暂时忽略掉。</p>
<p>它有两个子类，一个Cons，一个Empty。Empty当然是代表空Stream了。</p>
<p>而Cons则是头尾结构的，头是Stream中的一个元素，尾是Stream中余下的元素。请注意头和尾这两个参数的类型并不是A，头的类型是一个能够返回A的函数，尾的类型是一个能够返回MyStream[A]的函数。</p>
<h1 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h1><p>有了以上的类型定义以及头尾结构，我们就可以把很多个Cons加一个Empty（或者是无限多个Cons，没有Empty）连起来就构成一个Stream了，比如这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">Cons</span>(()=&gt;<span class="number">1</span>,()=&gt;<span class="type">Cons</span>(()=&gt;<span class="number">2</span>,()=&gt;<span class="type">Empty</span>))</span></pre></td></tr></table></figure>

<p>这样就可以构造一个含有1，2的Stream了。</p>
<p>不过，请注意，上面的说法并不严谨，实际上它是一个包含着两个分别会返回1和2的函数的Stream。</p>
<p>也就是说当上面的代码在构造Cons的时候，1和2还没有“出生”，它们被包在一个函数里，等着被释放出来。</p>
<p>如果说我们通常熟知的一些集合包含的是花朵的话，那Stream所包含的就是花苞，它本身不是花，但是有开出花来的能力。</p>
<h1 id="Smart初始化"><a href="#Smart初始化" class="headerlink" title="Smart初始化"></a>Smart初始化</h1><p>当然，如果直接暴露Cons的构造函数出去给别人用的话，那这API也未免太不友好了，所以Stream需要提供一个易用的初始化的方式：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">MyStream</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">apply</span></span>[<span class="type">A</span>](elems: <span class="type">A</span>*): <span class="type">MyStream</span>[<span class="type">A</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (elems.isEmpty) empty</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">else</span> cons(elems.head, apply(elems.tail: _*))</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">cons</span></span>[<span class="type">A</span>](hd: =&gt; <span class="type">A</span>, tl: =&gt; <span class="type">MyStream</span>[<span class="type">A</span>]): <span class="type">MyStream</span>[<span class="type">A</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">lazy</span> <span class="keyword">val</span> head = hd</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">lazy</span> <span class="keyword">val</span> tail = tl</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">Cons</span>(() =&gt; head, () =&gt; tail)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">empty</span></span>[<span class="type">A</span>]: <span class="type">MyStream</span>[<span class="type">A</span>] = <span class="type">Empty</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个没有太多好解释的，我们就是用apply和小写的cons这两个方法来把客户代码原本要写的一大堆匿名函数给代劳掉。</p>
<p>需要注意的一点是apply方法看似是递归的，好像是你调用它的时候如果给它n个元素的话，它会自己调用自己n-1次。事实上它确实会调用自己n-1次，但是并不是立即发生的，为什么呢？</p>
<p>因为小写的cons方法所接受的第二个参数不是eager evaluation的，这就会使得apply(elems.tail: _*)这个表达式不会立即被求值。这就意味着，apply缺失会被调用n次，但是这n次并不是一次接一次连续发生的，它只会在我们对一个Cons的尾巴求值时才会发生一次。</p>
<p>如果说普通的集合中包含的是数据的话，那Stream中所包含的就是能够产生数据的算法。</p>
<p>如何？是不是花朵花苞的感觉又回来了？</p>
<p>还记得我们开始剖析的时候那句代码是什么吗？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">MyStream</span>(randomList: _*).filter(isDivisibleBy3).take(<span class="number">2</span>).toList</span></pre></td></tr></table></figure>

<p>现在我们算是把MyStream(randomList: _*)这一小点说清了。</p>
<p>接下来看MyStream(randomList: _*).filter(isDivisibleBy3)是如何work的。</p>
<h1 id="filter"><a href="#filter" class="headerlink" title="filter"></a>filter</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">MyStream</span>[+<span class="type">A</span>] </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">filter</span></span>(p: <span class="type">A</span> =&gt; <span class="type">Boolean</span>): <span class="type">MyStream</span>[<span class="type">A</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span> <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Cons</span>(h, t) =&gt;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (p(h())) cons(h(), t().filter(p))</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">else</span> t().filter(p)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Empty</span> =&gt; empty</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">. . . . . .</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个方法定义在基类里，又是一个看似递归的实现。</p>
<p>为什么说是看似呢？因为在</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (p(h())) cons(h(), t().filter(p))</span></pre></td></tr></table></figure>

<p>这行代码中我们又用到了小写的cons，它所接受的参数不会被立即求值。也就是说filter一旦找到一个合适的元素，它就不再继续跑了，剩下的计算被延迟了。</p>
<p>比较值得提一下的是：这里的h()是什么呢？h是构造Cons时的第一个参数，它是什么类型的？()=&gt;A。它就是之前提到的能够生产数据的算法，就是那个能够开出花朵的花苞。在这里我们说h()，就是在调用这个函数来拿到它所生产的数据，就是让一个花苞开出花朵。</p>
<h1 id="take"><a href="#take" class="headerlink" title="take"></a>take</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">MyStream</span>(randomList: _*).filter(isDivisibleBy3).take(<span class="number">2</span>)</span></pre></td></tr></table></figure>

<p>接下来就该说take是如何work的了。在这里我们可以回顾一下，MyStream(randomList: _*)返回一个类型为MyStream[Int]，其中包含很多个可以返回Int的函数的容器。然后我们调用了这个容器的filter方法，filter又返回一个包含很多个可以返回Int的函数的容器。请注意，到这里为止，真正的计算还没有开始，真正的计算被包含到了一个又一个的函数（花苞）中，等待着被调用（绽放）。</p>
<p>那对filter的结果调用take又会怎样呢？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">MyStream</span>[+<span class="type">A</span>] </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  . . . . . .</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">take</span></span>(n: <span class="type">Int</span>): <span class="type">MyStream</span>[<span class="type">A</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (n &gt; <span class="number">0</span>) <span class="keyword">this</span> <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Cons</span>(h, t) <span class="keyword">if</span> n == <span class="number">1</span> =&gt; cons(h(), <span class="type">MyStream</span>.empty)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Cons</span>(h, t) =&gt; cons(h(), t().take(n - <span class="number">1</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> _ =&gt; <span class="type">MyStream</span>.empty</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">else</span> <span class="type">MyStream</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  . . . . . .</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>看过了前面的apply和filter之后，take就显得顺眼了很多。我们又见到了小写的cons，条件反射一般，我们就可以意识到，只要看见cons，那就意味着作为它的参数的表达式不会被立即求值，那这就意味着计算被放到了函数里，稍后再执行。那稍后到底是什么时候呢？</p>
<p>那就得看下面的toList了。</p>
<h1 id="toList"><a href="#toList" class="headerlink" title="toList"></a>toList</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">MyStream</span>[+<span class="type">A</span>] </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  . . . . . .</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">toList</span></span>: <span class="type">List</span>[<span class="type">A</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span> <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Cons</span>(h, t) =&gt; h() :: t().toList</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">case</span> <span class="type">Empty</span> =&gt; <span class="type">Nil</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>又是一个递归实现，但是这次可不是看似递归了，这次是实打实的递归：只要还没有遇到空节点，就继续向后遍历。这次没有使用cons，没有任何计算被延迟执行，我们通过不断地对h()求值，来把整个Stream中每一个能够生产数据的函数都调用一遍以此来拿到我们最终想要的数据。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>要把以上的代码细节全部load进脑子跑一遍确实不太容易，我们人类的大脑栈空间太浅了。</p>
<p>所以我们试着从上面所罗列出的纷繁的事实中抽象出一些适合人脑理解的描述性语句吧：</p>
<ul>
<li>List(1,2,3)会构造一个容器，容器中包含数据</li>
<li>List(1,2,3).filter(n=&gt;n&gt;1)会构造出一个新的容器，其中包含2和3，这两块具体的数据</li>
<li>List(1,2,3).filter(n=&gt;n&gt;1).take(1)会把上一步中构造成的容器中的第一块数据取出，放入一个新容器</li>
</ul>
<ul>
<li>MyStream(1,2,3)也会构造一个容器，但是这个容器中不包含数据，它包含能够生产数据的算法</li>
<li>MyStream(1,2,3).filter(n=&gt;n&gt;1)也会构造出一个新的容器，这个容器中所包含的仍然是算法，是基于上一步构造出的能生产1，2，3的算法之上的判断数字是否大于1的算法</li>
<li>MyStream(1,2,3).filter(n=&gt;n&gt;1).take(1)会把上一步中构造成的算法容器中的第一个算法取出，放入一个新容器</li>
<li>MyStream(1,2,3).filter(n=&gt;n&gt;1).take(1).toList终于把上面所有步骤构造出的算法执行了，从而得到了最终想要的结果</li>
</ul>
<p>上面对List和Stream的应用的区别在哪儿呢？</p>
<p>就在于List是先把数据构造出来，然后在一堆数据中挑选我们心仪的数据。</p>
<p>而Stream是先把算法构造出来，挑选心仪的算法，最后只执行一大堆算法中我们需要的那一部分。</p>
<p>这样，自然就不会执行多余的运算了。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>ScalaInAction</tag>
      </tags>
  </entry>
  <entry>
    <title>solving regular problems in scala</title>
    <url>/blog/2014/09/06/solving-regular-problems-in-scala/</url>
    <content><![CDATA[<iframe src="//slides.com/pengfeicui/solving-regular-problems-in-scala/embed" width="800" height="600" scrolling="no" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>ScalaInAction</tag>
      </tags>
  </entry>
  <entry>
    <title>Desugar Scala(17) -- Option和for，以及脑子里发生的事情</title>
    <url>/blog/2014/08/30/options-for/</url>
    <content><![CDATA[<p>Scala里的for关键字是个很有趣的东西。可以用来把多层嵌套for循环写成一层。比如这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(i&lt;<span class="number">-1</span> to <span class="number">10</span>;j&lt;<span class="number">-1</span> to <span class="number">10</span>;k&lt;<span class="number">-1</span> to <span class="number">10</span>) <span class="keyword">yield</span>(<span class="string">s"<span class="subst">$i</span> <span class="subst">$j</span> <span class="subst">$k</span>"</span>)</span></pre></td></tr></table></figure>

<p>这行代码执行的结果是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">2</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">3</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">4</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">5</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">6</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">7</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">8</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">9</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">1</span> <span class="number">10</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">2</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">3</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">4</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">5</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">6</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">7</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">8</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">9</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="number">1</span> <span class="number">2</span> <span class="number">10</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">......</span></pre></td></tr><tr><td class="code"><pre><span class="line">......</span></pre></td></tr></table></figure>

<p>这样，就可以用一行代码写出三层循环的效果。代码看起来非常紧凑，噪音很少。</p>
<p>但是今天主要要说的不是这种for，而是它和Option结合的写法。</p>
<p>Option本身是一个抽象类，代表一个可能存在，也可能不存在的值（那谁谁的喵？）。它有两个实现类，分别是Some和None。顾名思义，Some代表有值，None代表没有。</p>
<p>实际上，上面的说法不够准确，Some是一个实现类，而None实际是一个单例，不过这点对后面的内容没影响。</p>
<p>现在设想一个很简单的场景，需要用单价和数量来算总价，而单价和数量未必拿得到，那代码大概会是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculateTotal</span></span>: <span class="type">Option</span>[<span class="type">Int</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> price: <span class="type">Option</span>[<span class="type">Int</span>] = getPrice</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> amount: <span class="type">Option</span>[<span class="type">Int</span>] = getAmount</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">if</span> (price.isEmpty || amount.isEmpty) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">None</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125; <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">Some</span>(price.get * amount.get)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>getPrice和getAmount都返回一个Option[Int]，就类似Java中Integer可以为null一样。计算出来的总价也是一个Option[Int]，说不定会有，也说不定没有。</p>
<p>在这段代码中先检查单价和数量是否存在，如果二者中任意一个不存在，那就返回None，代表无法求得总价。如果二者都存在，那就将二者的乘积用Some包起来返回。</p>
<p>这代码看起来还ok，很常规的写法，但是稍显啰嗦。如果用上for的话，可以大大简化这段代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculateTotalWithFor</span></span>: <span class="type">Option</span>[<span class="type">Int</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">for</span> (price &lt;- getPrice; amount &lt;- getAmount) <span class="keyword">yield</span> price * amount</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个方法体只有一行了，而它实现出来的行为和上面那段代码是完全一致的。</p>
<p>这感觉好神奇啊，不用判断价格和数量是否存在，也不需要根据判断结果决定到底返回None还是Some。它是怎么搞的呢？</p>
<p>看一下反编译的结果吧：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Option&lt;Object&gt; <span class="title">calculateTotalWithFor</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> getPrice().flatMap(<span class="keyword">new</span> AbstractFunction1() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> Option&lt;Object&gt; <span class="title">apply</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> price)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">return</span> OptionAndFor..MODULE$.account$of$OptionAndFor$$getAmount().map(<span class="keyword">new</span> AbstractFunction1.mcII.sp() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> price$<span class="number">1</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">apply</span><span class="params">(<span class="keyword">int</span> amount)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    <span class="keyword">return</span> apply$mcII$sp(amount);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">public</span> <span class="keyword">int</span> apply$mcII$sp(<span class="keyword">int</span> amount) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    <span class="keyword">return</span> price * amount;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个反编译的结果很不好读，不过还是可以看出个大概。它先是对getPrice的返回值调用了flatMap，给其传入一个匿名函数（AbstractFunction1），在这个匿名函数里面又对getAmount的返回值调用了map，也给其传入了一个匿名函数，再在这第二层匿名函数里做了乘法运算。</p>
<p>如果用Scala把它表达出来，是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculateTotalWithFlatMapAndMap</span></span>: <span class="type">Option</span>[<span class="type">Int</span>] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  getPrice.flatMap(price =&gt; getAmount.map(amount =&gt; amount * price))</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>由此可见，上面使用for的代码的神奇之处在于它利用了Option的flatMap和map方法。</p>
<p>这两个方法具有一个共同特征：如果被调用flatMap或者map的当前Option实例为None的话，则忽略传入的匿名函数，直接返回None。</p>
<p>这很容易理解，要参与运算的成员之一已经是None了，那就不用管剩下的成员到底是啥了，它随便是啥，最终的计算结果都会是None。这和最初写出的用 || 运算符的代码的逻辑是一致的。</p>
<p>到此为止，我们给Option和for的结合使用脱光了衣服，它就是利用Option的flatMap和map来实现紧凑的代码的。</p>
<h3 id="神奇之处不仅在于更短的代码，还在于它提高了信噪比，给我们提供了更加简化的思考模型"><a href="#神奇之处不仅在于更短的代码，还在于它提高了信噪比，给我们提供了更加简化的思考模型" class="headerlink" title="神奇之处不仅在于更短的代码，还在于它提高了信噪比，给我们提供了更加简化的思考模型"></a>神奇之处不仅在于更短的代码，还在于它提高了信噪比，给我们提供了更加简化的思考模型</h3><p>最初那段用if else的代码，在写它或者读它的时候，我们的脑子里面发生了什么呢？</p>
<pre><code>1. 要获取价格和数量
2. 要判断价格是否为空，要判断数量是否为空        （与业务关联较小，属于技术范畴）
3. 如果任意一个为空，结果是空                 （与业务关联较小，属于技术范畴）
4. 如果两个都不是空，再做乘法运算</code></pre><p>而在写或者读用for的那段代码的时候，脑子里又是怎么想的呢？</p>
<pre><code>1. 获取价格和数量
2. 做乘法运算</code></pre><p>我们写这段代码的目的是要表述业务逻辑，是要给未来读代码的人传递和业务相关的信息。</p>
<p>而空值判断是偏技术的，把这种代码消掉，我们传递给其他程序员的信息里就含有更少的与业务无关的噪音。而且我们自己写起来的时候，脑子里也不需要考虑那么多的东西。</p>
<p>对自己，对他人都有利。这实在是一个美妙的语言特性。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>大理</title>
    <url>/blog/2014/08/10/da-li/</url>
    <content><![CDATA[<p>昆明机场，等待去大理的飞机</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t5jg23zj20hs0nu0wt.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t5jg23zj20hs0nu0wt.jpg</a> =800x)</p>
<p>下了飞机，出租车上，窗外有云，和蓝天</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t5lh0jvj21w02io1ky.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t5lh0jvj21w02io1ky.jpg</a> =800x)</p>
<p>第二天早上，帮校长画墙</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t6vgeh4j20f20qowfs.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t6vgeh4j20f20qowfs.jpg</a> =800x)</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t6umefgj20qo0f23zj.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t6umefgj20qo0f23zj.jpg</a> =800x)</p>
<p>王鹏和顺子也在画</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t5ohwv6j21w02ioe82.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t5ohwv6j21w02ioe82.jpg</a> =800x)</p>
<p>去环绕洱海</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t5rds9nj22io1w0x6p.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t5rds9nj22io1w0x6p.jpg</a> =1200x)</p>
<p>视野开阔，水面如镜</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t5tphlyj22io1w0kjl.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t5tphlyj22io1w0kjl.jpg</a> =1200x)</p>
<p>云，与水，与山</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t5vwmxkj22io1w0kjl.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t5vwmxkj22io1w0kjl.jpg</a> =1200x)</p>
<p>云诡</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t5yjz3ij22io1w0kjl.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t5yjz3ij22io1w0kjl.jpg</a> =1200x)</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t677ddmj22io1w0u0x.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t677ddmj22io1w0u0x.jpg</a> =1200x)</p>
<p>无尽的路</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t6cjok0j21w02io7wi.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t6cjok0j21w02io7wi.jpg</a> =1200x)</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t6ouqxcj22io1w04qq.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t6ouqxcj22io1w04qq.jpg</a> =1200x)</p>
<p>你是一株什么植物？</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t6ilxm1j22io1w0b2a.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t6ilxm1j22io1w0b2a.jpg</a> =1200x)</p>
<p>你又是一簇什么花？</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t6lishyj22io1w0x6p.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t6lishyj22io1w0x6p.jpg</a> =1200x)</p>
<p>晚上回来，墙画好了</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7twb68w8j20np0hsdh6.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7twb68w8j20np0hsdh6.jpg</a> =1200x)</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7twfj2qfj20np0hs76x.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7twfj2qfj20np0hs76x.jpg</a> =1200x)</p>
<p>大门口也颇有点样子了</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7twdzj0gj20np0hs76v.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7twdzj0gj20np0hs76v.jpg</a> =1200x)</p>
<p>晒出一条麒麟臂</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t6tr10gj20qo0zkgya.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t6tr10gj20qo0zkgya.jpg</a> =1200x)</p>
<p>你是谁家的狗？</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t6xx53lj21w02iou0x.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t6xx53lj21w02iou0x.jpg</a> =1200x)</p>
<p>为什么一到饭点就来？</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t70jalbj22io1w0npd.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t70jalbj22io1w0npd.jpg</a> =1200x)</p>
<p>扎染的布，不需要买一匹</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t740ay1j22io1w0kjm.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7t740ay1j22io1w0kjm.jpg</a> =1200x)</p>
<p>走，去喝酒</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tb6qowij20qo0f0dgu.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tb6qowij20qo0f0dgu.jpg</a> =1200x)</p>
<p>尼玛，为什么这么悲壮？</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tb7fs00j20qo0f0ta4.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tb7fs00j20qo0f0ta4.jpg</a> =1200x)</p>
<p>洱海门下听歌</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7tb8ywj6j20qo0f00u6.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7tb8ywj6j20qo0f00u6.jpg</a> =1200x)</p>
<p>被雨困住走不了</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tb9y5zbj20qo0f0abc.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tb9y5zbj20qo0f0abc.jpg</a> =1200x)</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbb7rv4j20qo0k076g.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbb7rv4j20qo0k076g.jpg</a> =1200x)</p>
<p>时间的轨迹变得模糊，不记得这是哪天晚上，大家在画墙</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t74us92j20hs0nujup.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t74us92j20hs0nujup.jpg</a> =800x)</p>
<p>燕子来了，走，我们再去一次洱海</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbhdshdj20hs0d9dgm.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbhdshdj20hs0d9dgm.jpg</a> =800x)</p>
<p>Kratos ！ ！ ！</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tbi2kq4j20hs0d9mxy.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tbi2kq4j20hs0d9mxy.jpg</a> =800x)</p>
<p>大家都骑的自行车，是的</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tbc1njnj20hs0dc0to.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tbc1njnj20hs0dc0to.jpg</a> =800x)</p>
<p>停车拍照</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tbjyfjgj20hs0npmyw.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tbjyfjgj20hs0npmyw.jpg</a> =800x)</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/mw690/8b1ece2agw1ej7tbj3z0mj20hs0dc0tz.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/mw690/8b1ece2agw1ej7tbj3z0mj20hs0dc0tz.jpg</a> =800x)</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t7xldcyj22io1w0x6p.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7t7xldcyj22io1w0x6p.jpg</a> =800x)</p>
<p>又是波谲云诡<br><a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t99kua8j28c01ss4qv.jpg" target="_blank">点击看大图</a></p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t99kua8j28c01ss4qv.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7t99kua8j28c01ss4qv.jpg</a> =4000x)</p>
<p>又是无尽的路</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t9nv70uj22io1w0u0x.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7t9nv70uj22io1w0u0x.jpg</a> =1200x)</p>
<p>无尽的路全景<br><a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7taa5gkrj28c01ssx6v.jpg" target="_blank">点击看大图</a></p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7taa5gkrj28c01ssx6v.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7taa5gkrj28c01ssx6v.jpg</a> =4000x)</p>
<p>左手山，右手水<br><a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tatvzpgj28c01ssb2g.jpg" target="_blank">点击看大图</a></p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tatvzpgj28c01ssb2g.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tatvzpgj28c01ssb2g.jpg</a> =4000x)</p>
<p>这是我们当天的队伍</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tb640cfj20hs0d9js4.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tb640cfj20hs0d9js4.jpg</a> =800x)</p>
<p>下午累了，吃蛋炒饭。我的索马里海盗造型。</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tb496o3j20hs0nuafg.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tb496o3j20hs0nuafg.jpg</a> =800x)</p>
<p>脚蹬子掉了，海盗修车</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tb55t2mj20hs0dgab4.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tb55t2mj20hs0dgab4.jpg</a> =800x)</p>
<p>这是哪天的饭？</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tbglwrxj20k00qodjf.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7tbglwrxj20k00qodjf.jpg</a> =800x)</p>
<p>翻墙</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7tcodq6nj20qo0f275v.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7tcodq6nj20qo0f275v.jpg</a> =800x)</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tcmilqjj20qo0f2jsp.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/large/8b1ece2agw1ej7tcmilqjj20qo0f2jsp.jpg</a> =800x)</p>
<p>降魔杵</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7vgwbjczj20k00qodj2.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7vgwbjczj20k00qodj2.jpg</a> =800x)</p>
<p>寺外全景<br><a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tc6hvxsj28c01ssu12.jpg" target="_blank">点击看大图</a></p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tc6hvxsj28c01ssu12.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tc6hvxsj28c01ssu12.jpg</a> =800x)</p>
<p><a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbxyah7j28c01ssqvc.jpg" target="_blank">点击看大图</a></p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbxyah7j28c01ssqvc.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tbxyah7j28c01ssqvc.jpg</a> =800x)</p>
<p>此为何物？</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7vgyuar9j20k00qo0ww.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7vgyuar9j20k00qo0ww.jpg</a> =800x)</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/large/8b1ece2agw1ej7vgxhsk3j20qo0k00u8.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/large/8b1ece2agw1ej7vgxhsk3j20qo0k00u8.jpg</a> =800x)</p>
<p>我要撤了，大家包饺子</p>
<p>![：）](<a href="http://ww2.sinaimg.cn/mw690/8b1ece2agw1ej7tcgqk6lj20dc09zaac.jpg" target="_blank" rel="noopener">http://ww2.sinaimg.cn/mw690/8b1ece2agw1ej7tcgqk6lj20dc09zaac.jpg</a> =600x)</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/large/8b1ece2agw1ej7tchgeihj209y0dcmxl.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/large/8b1ece2agw1ej7tchgeihj209y0dcmxl.jpg</a> =400x)</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7tciado3j209y0dcmxd.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7tciado3j209y0dcmxd.jpg</a> =400x)</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/mw690/8b1ece2agw1ej7tcjbj5cj20hs0nsdgw.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/mw690/8b1ece2agw1ej7tcjbj5cj20hs0nsdgw.jpg</a> =600x)</p>
<p>合影</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7tcbixf4j20f00qowgu.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7tcbixf4j20f00qowgu.jpg</a> =600x)</p>
<p>再见！</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tceiarqj22io1w0x6p.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/large/8b1ece2agw1ej7tceiarqj22io1w0x6p.jpg</a> =1000x)</p>
<p>我走之后棚子搭好了</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7vgv5561j20qo0k0adm.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7vgv5561j20qo0k0adm.jpg</a> =600x)</p>
<p>![：）](<a href="http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7vgt5k59j20qo0k0di4.jpg" target="_blank" rel="noopener">http://ww1.sinaimg.cn/mw690/8b1ece2agw1ej7vgt5k59j20qo0k0di4.jpg</a> =600x)</p>
<p>大家还在一起吃饭</p>
<p>![：）](<a href="http://ww3.sinaimg.cn/mw690/8b1ece2agw1ej7tckandsj20qo0f00ua.jpg" target="_blank" rel="noopener">http://ww3.sinaimg.cn/mw690/8b1ece2agw1ej7tckandsj20qo0f00ua.jpg</a> =600x)</p>
<p>![：）](<a href="http://ww4.sinaimg.cn/mw690/8b1ece2agw1ej7tcl911qj20qo0f0763.jpg" target="_blank" rel="noopener">http://ww4.sinaimg.cn/mw690/8b1ece2agw1ej7tcl911qj20qo0f0763.jpg</a> =600x)</p>
<p>故事还在继续</p>
<pre><code>THE END</code></pre>]]></content>
  </entry>
  <entry>
    <title>在使用play framework的evolutions？需要支持SQL Server？用Liquibase吧</title>
    <url>/blog/2014/07/18/play-evolutions-to-liquibase/</url>
    <content><![CDATA[<p>我所在的项目在用Scala + Play framework做一个web app。</p>
<p>Play自带的evolutions是一个DB Migration工具，从一开始我们就在用它来做所有阶段的数据迁移工作。</p>
<p>运行自动化测试时它可以帮每个测试用例在H2中创建数据（H2是Play默认的内存数据库）。<br>在下一个测试用例运行时evolutions则会创建一份和上次完全相同的新数据，这样我们的测试可以获得独立性而不用担心之前的测试遗留的副作用。也不用担心会给下一个测试遗留下什么脏数据。</p>
<p>在测试或者部署环境中运行时它也可以针对Postgres做数据迁移。</p>
<p>这一切看起来都挺好，我们就差喊evolutions是我们忠实的好伙伴了。</p>
<p>但是，快到给终端客户部署时，某一家客户提出他们一定要使用SQL Server，我们最初提出的使用Postgres他们不接受了。这时我们才发现evolutions的设计初衷就是在开发和测试阶段提供便利性，它根本就没想成为一个production ready的东西。</p>
<p>这样看来我们必须得寻找一个正经的DB Migration的工具了。而且这个DB Migration工具一定要满足以下几点：</p>
<ol>
<li>能够在运行自动化测试时和H2结合使用（因为我们已经有很多测试在依赖于H2跑了，要换掉成本较高）</li>
<li>能支持多种数据库（今天有人要SQL Server的支持，明天说不定还会有人要其他的）</li>
<li>在支持多种数据库时不需要我们写不同风格的SQL脚本（要写出让各个DB都不挑剔的SQL实在是太费劲了）</li>
</ol>
<p>我最先想到的就是Flyway，之前用过，而且TW的tech radar也提到过它。</p>
<p>但是它并没有入选，原因在于上面的第三点。Flyway要求使用者自己提供执行所需的SQL脚本。<br>这就意味着我们写SQL时需要同时兼顾H2，Postgres，SQL Server的异同。而且还无法预知未来的其他数据库会对我们现在写出的SQL脚本产生什么样的影响。</p>
<p>最后我们选择了Liquibase，我们可以通过JSON，YAML，或者XML来定义数据。Liquibase自己负责把我们定义的数据翻译给各种不同的数据库。</p>
<p>这样，通过一层中间语言。我们就隔离了数据库的差异对我们开发工作可能会造成的影响。</p>
<p>Ok，要用Liquibase这个大方向就确定了。但是具体怎么把它跑起来呢？在什么时机跑它呢？</p>
<p>用脚本跑？</p>
<p>Liquibase确实提供了Standalone，我们可以用脚本来调用它。</p>
<p>但是这怎么和build结合起来呀？在测试时调用它？在app启动时调用它？</p>
<p>那H2运行的端口每次都未必是一样的，这怎么办啊？</p>
<p>这个方案想想就费劲。</p>
<p>把它做成sbt的一个task？</p>
<p>这样确实比直接用脚本要稍微距离我们的build近一点，但是还是会有类似的问题。我们需要显式地去调用它，还要选择合适的时机去调用它。实现起来也会很麻烦。</p>
<p>而实际上，Play自己是支持plug in的。我们想要控制执行时机，而有谁比Play自己更了解它的运行时机呢？</p>
<p>而且已经有人做了liquibase play plug in。我把它fork了一份，更新了liquibase和play的版本，提高了log的level。并且部署到了sonatype去。</p>
<p>由于是Play自己的plug in，不是我们试图插入的生硬的脚本或者sbt task。Play自己知道该在什么合适的时机去执行它。</p>
<p>下面说一下如何应用它吧。</p>
<ul>
<li>在所有的conf文件中删掉所有和evolutions有关的配置</li>
</ul>
<p>这两个东西不能一起用，要不然我们需要同时维护两种DB Migration的脚本。</p>
<ul>
<li>在dependencies中加入这一项：</li>
</ul>
<p>“com.github.cuipengfei” % “play-liquibase_2.11” % “1.1”</p>
<p>很明显，这是用来引入这个plugin的。</p>
<ul>
<li>在conf目录下创建一个名为play.plugins的文件，在其中写入：</li>
</ul>
<p>400:com.github.cuipengfei.LiquibasePlugin</p>
<p>冒号前的400用来定义plugin的执行优先级，Play会由此决定何时执行该plugin。</p>
<p>冒号后是plugin的完全限定名。</p>
<ul>
<li>在你需要的conf文件中加入两行：</li>
</ul>
<p>liquibaseplugin=enabled</p>
<p>applyLiquibase.default=true</p>
<p>这样用来启用该plugin。</p>
<ul>
<li>在conf/liquibase/default/下创建一个modules.xml。</li>
</ul>
<p>在其中写入你的数据定义。（具体如何写，liquibase的官网有详细的介绍）</p>
<p>如果你用的数据库名字不是default，相应的替换就ok了。</p>
<p>这样，就大功告成了。</p>
<p>当你用sbt运行自动化测试时，liquibase会帮你创建数据。</p>
<p>当你在本地调试运行时，liquibase会帮你set up数据库。</p>
<p>当应用被部署到生产环境下去的时候，liquibase也可以帮你在第一次运行时进行数据的初创。</p>
]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>ScalaInAction</tag>
      </tags>
  </entry>
  <entry>
    <title>论“如果trait有方法实现，那么Java类就不能实现这个trait”这句话是错的</title>
    <url>/blog/2014/06/28/scala-java-interop-trait-with-impl/</url>
    <content><![CDATA[<p>最近还是在看郑大翻译的《Scala程序设计》，其中第十一章还有一句话：</p>
<blockquote>
<p>如果trait有方法实现，那么Java类就不能实现这个trait</p>
</blockquote>
<p>口说还是无凭，还是拍照为证：</p>
<p><img src="http://ww1.sinaimg.cn/large/8b1ece2agw1ehtqjokutnj21kw23u7wh.jpg" alt=""></p>
<p>我感觉这句话是错的，下面寻根究底地探索一下。</p>
<p>trait这个语言特性<a href="http://cuipengfei.me/blog/2013/10/13/scala-trait/" target="_blank" rel="noopener">前面的博文</a>讲过。</p>
<p>一个含有方法实现的trait会被编译成一个interface，还有一个含有实现的静态方法。</p>
<p>所有extends或者是with这个trait的Scala类，实际上都是implements了这个interface，在具体实现中调用了静态方法。</p>
<p>快速的简单回忆一下：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">HappyThoughts</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">whatAreYouThinking</span></span>(): <span class="type">Unit</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">" food :D "</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>定义一个含有方法实现的trait。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span> <span class="keyword">with</span> <span class="title">HappyThoughts</span></span></span></pre></td></tr></table></figure>

<p>然后让Dog去with这个trait。</p>
<p>之后就可以这样调用：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">Dog</span>().whatAreYouThinking()</span></pre></td></tr></table></figure>

<p>这样就能打印出food :D了。虽然Dog本身是空的，但是因为with了一个trait，它也拥有了一些行为。</p>
<p>再来看看反编译出的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">HappyThoughts</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">whatAreYouThinking</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">HappyThoughts</span>$<span class="title">class</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">whatAreYouThinking</span><span class="params">(HappyThoughts $<span class="keyword">this</span>)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="string">" food :D "</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> $init$(HappyThoughts $<span class="keyword">this</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>HappyThoughts就是上面这样的，一个interface，还有一个含有实现的静态方法。</p>
<p>Dog则是这样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span> <span class="keyword">implements</span> <span class="title">HappyThoughts</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">whatAreYouThinking</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    HappyThoughts<span class="class">.<span class="keyword">class</span>.<span class="title">whatAreYouThinking</span>(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Dog</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  	HappyThoughts<span class="class">.<span class="keyword">class</span>.$<span class="title">init</span>$(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>它implements了HappyThoughts，其实现则依赖于上面提到的静态方法。</p>
<p>Ok，足够清晰了。</p>
<p>这么一个trait，当真在Java中不可以利用吗？</p>
<p>写点代码试试看：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DogJ</span> <span class="keyword">implements</span> <span class="title">HappyThoughts</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="meta">@Override</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">whatAreYouThinking</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        HappyThoughts$<span class="class"><span class="keyword">class</span>.<span class="title">whatAreYouThinking</span>(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>基本照抄上面反编译的代码。这段Java代码是可以编译的，而且也可以运行，运行结果也是打印出了food :D。</p>
<p>这次，我就不去探寻旧版本的Scala是如何处理trait的了。我们只要知道当前版本（比如我用的2.10.4）的Scala中定义的含有方法实现的trait，拿到Java中依然是可用的就行了。虽说用起来有一点蹩脚，但终归是可用的。</p>
]]></content>
  </entry>
  <entry>
    <title>论“如果Scala类有方法接收闭包，这些方法在Java里就不可用，因为Java目前尚不支持闭包。”这句话是错的</title>
    <url>/blog/2014/06/27/scala-java-interop-closure/</url>
    <content><![CDATA[<p>最近在看郑大翻译的《Scala程序设计》，其中第十一章有一句话：</p>
<blockquote>
<p>如果Scala类有方法接收闭包，这些方法在Java里就不可用，因为Java目前尚不支持闭包。</p>
</blockquote>
<p>口说无凭，拍照为证：</p>
<p><img src="http://ww2.sinaimg.cn/large/8b1ece2agw1ehszbfbsj1j20xc18g17s.jpg" alt=""></p>
<p>当时看到这句话就感觉不对。因为JVM本身没有对函数式编程提供任何支持，所以无论是Java中常用的Guava，还是Scala，其对闭包的支持都是通过用类来包裹函数实现的。</p>
<p>如果说Java目前（其时Java 8还没面世）尚不支持闭包，那倒是还说得过去，因为毕竟是要用类包裹一层，不算真正的函数传递。</p>
<p>但是说如果Scala类有方法接收闭包，这些方法在Java里就不可用，那就不对了，包一层匿名内部类，我不还是能用吗？虽说不太好看，也不能说不能用啊。</p>
<p>为了验证一下，写点代码来试试吧。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">OnePluser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">plusOne</span></span>(func: () =&gt; <span class="type">Int</span>): <span class="type">Int</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    func() + <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>先定义一个Scala类，OnePluser，它有一个plusOne方法，接收一个函数，给函数的返回值加1，然后返回。非常简单。</p>
<p>在Scala里可以这么调用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">OnePluserCaller</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">callIt</span></span>(): <span class="type">Int</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">new</span> <span class="type">OnePluser</span>().plusOne(() =&gt; <span class="number">5</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>一样的简单，一个匿名函数传给它，这个匿名函数返回一个写死的5。这样最后的返回值会是6。</p>
<p>这段代码如果反编译成Java，会是这样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OnePluserCaller</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">callIt</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> OnePluser().plusOne(<span class="keyword">new</span> AbstractFunction0.mcI.sp() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">apply</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> apply$mcI$sp(); &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="keyword">public</span> <span class="keyword">int</span> apply$mcI$sp() &#123; <span class="keyword">return</span> <span class="number">5</span>; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>毫不出奇，调用plusOne的地方需要提供一个不接收参数，返回一个Int的函数，这个函数在Scala里是() =&gt; 5，编译出来就是一个AbstractFunction0的实例，其中的apply方法返回一个写死的5。可以想见，在plusOne中就会调用这个apply方法（已验证，非臆测）。</p>
<p>既然Scala的编译器可以用这种方式来实现函数的传递，那在Java代码中难道就不可以吗？</p>
<p>我们写点代码来验证一下吧：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> scala.runtime.AbstractFunction0;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OnePluserCallerJ</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">callIt</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> OnePluser().plusOne(<span class="keyword">new</span> AbstractFunction0&lt;Object&gt;() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="meta">@Override</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="function"><span class="keyword">public</span> Integer <span class="title">apply</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">return</span> <span class="number">5</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这段代码基本就是照上面的反编译结果照抄的，AbstractFunction0的定义在scala.runtime.AbstractFunction0里，在scala-library-xxx.jar里。xxx是版本号。</p>
<p>上面的代码可以编译，可以运行，而且也可以得到6这个返回值。</p>
<p>由此可以证明在当前的Scala版本下（我现在用的是2.10.4）作者的这段话是不成立的。</p>
<p>但是，当时呢？</p>
<p>我查了一下，这本书的出版日期是09年，然后查了一下Scala的版本，08年的版本是2.7.7。于是我下载了scala-library-2.7.7.jar。发现其中确实没有AbstractFunction0的定义，但是现在版本的AbstractFunction0是实现了Function0的，而Function0的定义在2.7.7中是有的。而当时如果在Java中写一个实现了Function0的匿名内部类，不也是可行的吗？</p>
<p>Ok，不关注那么老的事儿了，可以确定的一点是，在当前版本下，在Java中是可以调用Scala中定义的接收闭包的函数的。</p>
<p>THE END</p>
]]></content>
  </entry>
  <entry>
    <title>Scala + Play + sbt + Protractor = one build</title>
    <url>/blog/2014/06/22/scala-plus-play-plus-sbt-plus-protractor-equals-one-build/</url>
    <content><![CDATA[<p>我所在的项目的技术栈选用的是Play framework做后端API，前端用Angular JS。</p>
<p>因为用了Scala和Play，构建工具很自然用的就是sbt。</p>
<p>而由于前端用了Angular，所以functional test就选用了和Angular结合较好的protractor。</p>
<p>这一切看起来似乎很美好，一个无状态的后端，一个数据和UI双向绑定的前端。What could possibly go wrong？</p>
<p>一开始也确实如此，没什么问题。我们为了让functional test在CI上跑起来，写了一个脚本来把play dist打出的包部署到CI所在机器上，然后运行protractor。</p>
<p>这个脚本运行还算ok，偶尔有点小问题，修一修也就好了。</p>
<p>不过，这也就是说有两个因素可能会使得我们的CI挂掉，一个是用sbt跑的specs2的测试，一个是protractor的测试。而protractor的测试是基于我们自己所写的脚本的，与sbt没啥关系。</p>
<h3 id="麻烦来了"><a href="#麻烦来了" class="headerlink" title="麻烦来了"></a>麻烦来了</h3><p>上周五的show case，我们一个小时后就要给客户演示现有产品的示例，但是CI挂掉了，新的代码没法走到QA和UAT的环境，bug fix也过不去。</p>
<p>最后我们不得不修改jenkins的配置，把sbt test和protractor的test都暂时禁掉，才让最新的代码到了UAT上去。而这一切，是在show case之前一分钟才解决掉的。</p>
<p>事情总是这样的，出一两次小问题，修修改改就好，我们不会注意到其危害，不会想到其风险。直到琐碎的积累导致了严重的后果，我们才会正视问题的存在。而这个时候问题或许已经复杂和严重到不可修复或者是要花很大成本修复的地步了。保持一个健康的CI是如此。写代码的每一个细节也是如此。</p>
<p>还好，很幸运，我们的问题还没有那么严重，还来得及修。</p>
<p>在决定要修之后，先</p>
<h3 id="定义一下问题是啥"><a href="#定义一下问题是啥" class="headerlink" title="定义一下问题是啥"></a>定义一下问题是啥</h3><p>往简单里说，就是CI不稳定，动不动就随便挂。</p>
<p>说的再细一些，就是我们手写脚本去做部署和测试这件事算是重新发明了轮子。而这个轮子不如已有的经过打磨的轮子那么精巧细致，那么稳定好用。以至于我们的CI偶尔就要出格一次。</p>
<p>Ok，问题定义清楚了，那么想想解决方案吧。</p>
<p>但是，在提出具体的方案之前，先想想，如果把这个现时还未存在的解决方案作用在现有问题之上，会收获一个什么样的结果呢？</p>
<h3 id="验证标准"><a href="#验证标准" class="headerlink" title="验证标准"></a>验证标准</h3><p>基于以上所述，我想解决这个问题的方案要满足以下3点：</p>
<ol>
<li>能让CI重回稳定</li>
<li>一条命令行执行整个build</li>
<li>不要再自己造轮子了</li>
</ol>
<p>第1，2点毋庸赘言，这就是我们问题的核心。关于第3点，是因为我们没有时间精力，也实在没有必要造这个轮子，如果能找到现有的轮子能够解决问题，而且还比我们自己的木头胶皮轮子好用，那岂不妙哉？</p>
<p>于是，我要开始寻找一个能让CI重归稳定的神圣轮子了！让探险的旅途就此展开吧！</p>
<p>![图](<a href="http://static.comicvine.com/uploads/original/7/75497/2189954-aragorn247_screen.jpg" target="_blank" rel="noopener">http://static.comicvine.com/uploads/original/7/75497/2189954-aragorn247_screen.jpg</a> =400x300)</p>
<h3 id="开始寻找轮子"><a href="#开始寻找轮子" class="headerlink" title="开始寻找轮子"></a>开始寻找轮子</h3><p>我最初的想法是用play的test framework，其中已经集成了selenium，用来做functional test很是合适。但是由于我们基于protractor的测试数量已经不少了，全部重写成本较高，所以这个轮子就放弃了。</p>
<h3 id="残念，再看下一个轮子"><a href="#残念，再看下一个轮子" class="headerlink" title="残念，再看下一个轮子"></a>残念，再看下一个轮子</h3><p>再然后我想到的是自定义一个sbt的task，这个task依赖于sbt已有的run。</p>
<p>这样就能在我的task启动之前把play跑起来，而task本身运行protractor的测试，再之后则杀掉正在运行的play app。</p>
<p>看起来不错，但是有问题：</p>
<p>第一，sbt run跑起来后是不会自己退出的，它会维持play一直在待命的状态，这也就是说我自己的task根本就没机会执行。</p>
<p>第二，即便能找到方法让我自己定义的task和run同时跑起来，protractor运行完毕后还要关掉run，免得占用端口。这又是一件麻烦事儿。</p>
<p>于是，这第二个轮子也被我自己给枪毙掉了。</p>
<h3 id="再次残念，还有轮子吗？"><a href="#再次残念，还有轮子吗？" class="headerlink" title="再次残念，还有轮子吗？"></a>再次残念，还有轮子吗？</h3><p>会有的，总会有的，只要肯去找，还是会有的。</p>
<p>这次我想到，写sbt的task不成，那就写代码。我写个specs2测试，在case里用代码启动sbt run，然后再启动protractor，最后关闭sbt，总行了吧？</p>
<p>这样，确实是可以work的，而实际上我也把它做出来验证了可以work了，但是缺点很大。</p>
<p>第一，由于我们的specs2测试都是用sbt跑的，而在其中再启动sbt，相当于要开两个jvm，消耗很大。在我本地机器上可以压榨的只剩两位数的内存。</p>
<p>第二，在sbt已经编译好了产品代码和测试代码测试之后，再开始跑另一个sbt run，会导致sbt把代码重新编译一遍。而Scala的代码编译是很慢的。我试了一下，这两次启动sbt，两次编译所消耗的时间是四分钟左右。时间成本太高。CI的速度会被拉下来，受不了。</p>
<p>基于以上两点原因，我的第三个轮子也被我自己枪毙了。</p>
<h3 id="命途多舛啊，三次尝试都失败，以你为我要放弃了吗？哼"><a href="#命途多舛啊，三次尝试都失败，以你为我要放弃了吗？哼" class="headerlink" title="命途多舛啊，三次尝试都失败，以你为我要放弃了吗？哼~"></a>命途多舛啊，三次尝试都失败，以你为我要放弃了吗？哼<del>~</del></h3><p>最后，我结合第一次的尝试和第三次的尝试找到了一个满意的答案。</p>
<p>来看代码吧：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProtractorSpec</span> <span class="keyword">extends</span> <span class="title">Specification</span> <span class="keyword">with</span> <span class="title">NoTimeConversions</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="string">"my application"</span> should &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="string">"pass the protractor tests"</span> in &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">      running(<span class="type">TestServer</span>(<span class="number">9000</span>)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">val</span> resp = <span class="type">Await</span>.result(<span class="type">WS</span>.url(<span class="string">"http://localhost:9000"</span>).get(), <span class="number">2</span> seconds)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        resp.status === <span class="number">200</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        runProtractorTests === <span class="number">0</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">      &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="function"><span class="keyword">def</span> <span class="title">runProtractorTests</span></span>: <span class="type">Int</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="string">"protractor functional-test/config/ft.conf.js"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">      .run(getProcessIO)</span></pre></td></tr><tr><td class="code"><pre><span class="line">      .exitValue()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="function"><span class="keyword">def</span> <span class="title">getProcessIO</span></span>: <span class="type">ProcessIO</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">new</span> <span class="type">ProcessIO</span>(_ =&gt; (),</span></pre></td></tr><tr><td class="code"><pre><span class="line">      stdout =&gt; fromInputStream(stdout).getLines().foreach(println),</span></pre></td></tr><tr><td class="code"><pre><span class="line">      _ =&gt; ())</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>就只有这么一点点代码。</p>
<p>running和TestServer都是play的test framework所提供的API。顾名思义，其作用就是把play的app跑起来。</p>
<p>然后发一个get请求，assert它的response的status是200，以此来确保play真的是把server运行起来了的。</p>
<p>再然后运行shell脚本，把protractor跑起来。这里Scala会做implicit conversion，把字符串转换成ProcessBuilder，从而可以调用其run方法。</p>
<p>最后assert，protractor的shell脚本是返回了0的，意味着functional test跑成功了。如果protractor测试挂掉，返回了1，那么specs2的这个测试也会挂掉，从而挂掉整个build。而这，正是我想要的。</p>
<h3 id="这个解决方案合规吗？"><a href="#这个解决方案合规吗？" class="headerlink" title="这个解决方案合规吗？"></a>这个解决方案合规吗？</h3><p>检验一下吧。</p>
<p>由于server的启动和关闭都是有play的test framework的API负责的，比自己手写得脚本要稳定，所以符合了重归稳定性这一点。</p>
<p>由于用了specs2的测试，它可以跑在sbt里，所以符合一条命令跑build这一点。</p>
<p>整个解决方案只用了specs2和play的test framework，没有重新发明轮子，所以这一点也符合了。</p>
<p>除了符合最初定下的三条标准之外，还有额外的好处：functional test所跑到的代码会被纳入到测试覆盖率里面去。因为和其他specs2的测试一样，protractor的测试也在sbt jacoco:cover的监视下跑的，所以自然就纳入了coverage的范围。</p>
<h3 id="Takeaway"><a href="#Takeaway" class="headerlink" title="Takeaway"></a>Takeaway</h3><p>在解决这个问题之后，我想我会有三点takeaway：</p>
<ol>
<li><p>多尝试几种方案，不要随便放弃。即便想，也不要。</p>
</li>
<li><p>不要屈就于working solution，要相信一定存在你现在还没想到的更好的方式。</p>
</li>
<li><p>重复发明轮子总是会显得很诱人，因为它看起来可以非常直接而且准确的解决我们的问题。而实际上它常常是直接而且准确的解决我们的问题的现象。如果能找到现象产生的原因，干死这个原因，问题的解决或许会更彻底。</p>
</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>Desugar Scala(16) -- lower bound</title>
    <url>/blog/2014/06/21/desugar-scala-lower-bound/</url>
    <content><![CDATA[<p>Lower bound，不知道这个词的确切中文翻译是怎样的。我们直接看例子吧。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pair</span>[<span class="type">T</span>](<span class="params">val first: <span class="type">T</span>, val second: <span class="type">T</span></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">replaceFirst</span></span>[<span class="type">R</span> &gt;: <span class="type">T</span>](newFirst: <span class="type">R</span>): <span class="type">Pair</span>[<span class="type">R</span>] = <span class="keyword">new</span> <span class="type">Pair</span>[<span class="type">R</span>](newFirst, second)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们定义一个叫做Pair的类，其中可以包含两个元素，元素类型为泛型的T。</p>
<p>Pair类中有一个replaceFirst方法，用来把第二个元素和一个新的元素结合起来组成一个新的Pair。新的元素的类型是泛型的R。新组成的Pair的类型是Pair[R]。</p>
<p>到这里我们就要想了，一个T和一个R，它们俩怎么组成新的Pair呢？新的Pair的类型怎么能是Pair[R]呢？</p>
<p>replaceFirst的签名给我们说明了这一点。[R &gt;: T]。这种标记的含义是说R是T的基类。那么一个T和一个R自然可以组合成一个R的Pair了。</p>
<p>单是这样干说，有点不好理解，我们看一个例子：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vehicle</span> </span>&#123;&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Car</span> <span class="keyword">extends</span> <span class="title">Vehicle</span> </span>&#123;&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tank</span> <span class="keyword">extends</span> <span class="title">Vehicle</span> </span>&#123;&#125;</span></pre></td></tr></table></figure>

<p>汽车和坦克都是机动车。</p>
<p>然后我们可以这样使用它们：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> twoCars: <span class="type">Pair</span>[<span class="type">Car</span>] = <span class="keyword">new</span> <span class="type">Pair</span>(<span class="keyword">new</span> <span class="type">Car</span>(), <span class="keyword">new</span> <span class="type">Car</span>())</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> tankAndCar: <span class="type">Pair</span>[<span class="type">Vehicle</span>] = twoCars.replaceFirst(<span class="keyword">new</span> <span class="type">Tank</span>())</span></pre></td></tr></table></figure>

<p>首先我们用两辆汽车组成一个Pair，其类型为Pair[Car]。</p>
<p>然后我们用一辆坦克替代原来的Pair中的第一个元素，让坦克和第二辆车组成一个新的Pair。新的Pari的类型是Pair[Vehicle]。</p>
<p>这里有一点tricky。我们调用replaceFirst的时候传递的参数的类型是Tank，这是否意味着在这里R就是Tank呢？</p>
<p>不是的，因为很明显Tank不是Car的基类，然而Tank是一个（is a）Vehicle，Vehicle同时也是Car的基类。于是此处的R就是Vehicle。得到的新的Pair自然就是Pair[Vehicle]。</p>
<p>也就是说R会被什么具体类型替换呢？这取决于T和newFirst的类型。</p>
<p>如果newFirst的类型刚好是T的基类，那太好了，R就直接是newFirst的类型。如果newFirst的类型不是T的基类，那R就会是T和newFirst的类型的共同基类。</p>
<h3 id="这个东西挺麻烦的，它有啥用呢？"><a href="#这个东西挺麻烦的，它有啥用呢？" class="headerlink" title="这个东西挺麻烦的，它有啥用呢？"></a>这个东西挺麻烦的，它有啥用呢？</h3><p>保证类型安全，Java没有提供给我们的类型安全。</p>
<p>还是刚才的那段代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> twoCars: <span class="type">Pair</span>[<span class="type">Car</span>] = <span class="keyword">new</span> <span class="type">Pair</span>(<span class="keyword">new</span> <span class="type">Car</span>(), <span class="keyword">new</span> <span class="type">Car</span>())</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> tankAndCar: <span class="type">Pair</span>[<span class="type">Vehicle</span>] = twoCars.replaceFirst(<span class="keyword">new</span> <span class="type">Tank</span>())</span></pre></td></tr></table></figure>

<p>其中的第二行，Scala可以很聪明的推断出replaceFirst的返回值类型是Pair[Vehicle]。实际上，如果我们试图把tankAndCar声明为Pair[Tank]的话，会看到编译时错误。</p>
<p>而类似的代码在Java里则没有这么幸运了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PairJ</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> T first;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> T second;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">PairJ</span><span class="params">(T first, T second)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.first = first;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.second = second;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">first</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.first;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">second</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.second;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> &lt;R&gt; <span class="function">Pair&lt;R&gt; <span class="title">replaceFirst</span><span class="params">(R newFirst)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Pair(newFirst, second());</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>为了标明区别，我们这次称之为PairJ。到这里忍不住要小小的黑Java一下，21行代码，和Scala的3行是等价的：）</p>
<p>我们重点看一下replaceFirst在这里的声明，其中声明了一个泛型参数R，但是R和T是没有任何关系的。实际上，在Java中，我们无法表达方法的泛型参数和类型的泛型参数之间的关系。（其原因请参看<a href="http://www.angelikalanger.com/GenericsFAQ/FAQSections/TypeParameters.html#FAQ107" target="_blank" rel="noopener">这里</a>）</p>
<p>我们写出如下的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">PairJ&lt;Car&gt; twoCars = <span class="keyword">new</span> PairJ(<span class="keyword">new</span> Car(), <span class="keyword">new</span> Car());</span></pre></td></tr><tr><td class="code"><pre><span class="line">Tank actuallyACar = twoCars.replaceFirst(<span class="keyword">new</span> Tank()).second();</span></pre></td></tr></table></figure>

<p>先创建两辆车的Pair，然后把第一辆车替换成坦克。再把新组成的Pair里面的第二个元素（其类型是车）取出来，赋值给一个类型为坦克的变量。</p>
<p>如果我们编译这段代码，Java编译器会允许其通过。但是运行起来就会跑出类型转换异常。原因很明显，Car不能转换成Tank。</p>
<p>这个，就是刚才所说的类型安全性上的差异。</p>
<h3 id="等等，脱衣服的部分呢？"><a href="#等等，脱衣服的部分呢？" class="headerlink" title="等等，脱衣服的部分呢？"></a>等等，脱衣服的部分呢？</h3><p>之前的每一篇博客都会把Scala代码编译出的bytecode反编译成Java，来探索其语言特性是如何实现的。</p>
<p>而这一次颇为不同。之前的语言特性虽说Scala写起来比Java会简便一些，但是还没有超出Java的能力范围。多费点劲，用Java还是能做到。</p>
<p>而这一次，这种编译时类型安全检验的严格性，实在是在Java中无法表达的。这全靠了Scala编译器的功劳。</p>
<p>这就意味着，上面所定义的Pair这个类，如果你在Java中使用它，就会失去这种类型安全性。</p>
<p>Scala，这次算你牛，没扒掉你。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Desugar Scala(15) -- unapply和unapplySeq方法</title>
    <url>/blog/2014/06/14/scala-unapply-method/</url>
    <content><![CDATA[<p>实在想不到什么动词可以当做脱衣服来讲了，所以从现在开始这系列博文就叫做Desugar Scala了。除非哪天才思泉涌，又想到了新词：）</p>
<p>开始正文。</p>
<p>名字叫做unapply和unapplySeq的方法在Scala里也是有特殊含义的。</p>
<p>我们前面说过case class在做pattern match时很好用，而除case class之外，有unapply或unapplySeq方法的对象在pattern match时也有很好的应用场景。</p>
<p>比如这段代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Square</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">unapply</span></span>(z: <span class="type">Double</span>): <span class="type">Option</span>[<span class="type">Double</span>] = <span class="type">Some</span>(math.sqrt(z))</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们定义了一个unapply方法，用来计算平方根。<br>我们可以像调用普通方法一样的调用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> number: <span class="type">Double</span> = <span class="number">36.0</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="type">Square</span>.unapply(number)</span></pre></td></tr></table></figure>

<p>这样会得到36的平方根:6。实际上返回值是Some(6)。</p>
<p>上面的方式是对unapply的浪费，unapply真正的好处是这样的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> number: <span class="type">Double</span> = <span class="number">36.0</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">number <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">case</span> <span class="type">Square</span>(n) =&gt; println(<span class="string">s"square root of <span class="subst">$number</span> is <span class="subst">$n</span>"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">case</span> _ =&gt; println(<span class="string">"nothing matched"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这样我们无需显式调用unapply方法，而把是它用在pattern match中，让编译器替我们调用它。</p>
<p>当我们写下这段pattern match的代码时，编译器其实替我们做了好几件事：</p>
<ol>
<li>调用unapply，传入number</li>
<li>接收返回值并判断返回值是None，还是Some</li>
<li>如果是Some，则将其解开，并将其中的值赋值给n（就是case Square(n)中的n）</li>
</ol>
<p>这段代码反编译出来是这个样子的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> number = <span class="number">36.0</span>D;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   <span class="keyword">double</span> d1 = number;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   Option localOption = Square..MODULE$.unapply(d1);</span></pre></td></tr><tr><td class="code"><pre><span class="line">   <span class="comment">//调用unapply，传入number</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">   BoxedUnit localBoxedUnit;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   <span class="keyword">if</span> (localOption.isEmpty()) &#123;<span class="comment">//判断返回值是None</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">     Predef..MODULE$.println(<span class="string">"nothing matched"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">     localBoxedUnit = BoxedUnit.UNIT;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   <span class="keyword">else</span> &#123;<span class="comment">//判断返回值是Some</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">     <span class="keyword">double</span> n = BoxesRunTime.unboxToDouble(localOption.get());</span></pre></td></tr><tr><td class="code"><pre><span class="line">     <span class="comment">//将Some解开，并将其中的值赋值给n</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">     Predef..MODULE$.println(<span class="keyword">new</span> StringContext(Predef..MODULE$.wrapRefArray((Object[]) <span class="keyword">new</span> String[] &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="string">"square root of "</span>, <span class="string">" is "</span>, <span class="string">""</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">     &#125;) ).s(Predef..MODULE$.genericWrapArray(<span class="keyword">new</span> Object[] &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       BoxesRunTime.boxToDouble(number), BoxesRunTime.boxToDouble(n)</span></pre></td></tr><tr><td class="code"><pre><span class="line">     &#125;)));</span></pre></td></tr><tr><td class="code"><pre><span class="line">     localBoxedUnit = BoxedUnit.UNIT;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;</span></pre></td></tr></table></figure>

<p>如果没有unapply方法和pattern match语法之间的这种结合，我们自己写代码要写成什么样子呢？</p>
<p>或许会比上面反编译的代码简单一些，但是显式地调用开平方的方法，用if else来判断Option，以及将真正的返回值从Option里面解出来这三件事是免不掉的。</p>
<p>unapplySeq和unapply的作用很是类似，例如这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Names</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">unapplySeq</span></span>(str: <span class="type">String</span>): <span class="type">Option</span>[<span class="type">Seq</span>[<span class="type">String</span>]] = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (str.contains(<span class="string">","</span>)) <span class="type">Some</span>(str.split(<span class="string">","</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">else</span> <span class="type">None</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们定义一个unapplySeq方法，用逗号作为分隔符来把字符串拆开。</p>
<p>然后我们可以这样应用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> namesString = <span class="string">"xiao ming,xiao hong,tom"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">namesString <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="type">Names</span>(first, second, third) =&gt; &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">"the string contains three people's names"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">s"<span class="subst">$first</span> <span class="subst">$second</span> <span class="subst">$third</span>"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> _ =&gt; println(<span class="string">"nothing matched"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>与上面的例子很是类似，不过编译器在这里替我们做的事情更多了：</p>
<ol>
<li>调用unapplySeq，传入namesString</li>
<li>接收返回值并判断返回值是None，还是Some</li>
<li>如果是Some，则将其解开</li>
<li>判断解开之后得到的sequence中的元素的个数是否是三个</li>
<li>如果是三个，则把三个元素分别取出，赋值给first，second和third</li>
</ol>
<p>如果没有unapplySeq方法和pattern match语法之间的这种结合，我们自己写代码来做这五件事会显得很是繁琐。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>抹掉Scala的糖衣(14) -- update method</title>
    <url>/blog/2014/06/12/scala-update-method/</url>
    <content><![CDATA[<p>在Scala中，名字叫做update的方法是有特殊作用的。</p>
<p>比如：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> scores = <span class="keyword">new</span> scala.collection.mutable.<span class="type">HashMap</span>[<span class="type">String</span>, <span class="type">Int</span>]</span></pre></td></tr><tr><td class="code"><pre><span class="line">scores(<span class="string">"Bob"</span>) = <span class="number">100</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> bobsScore = scores(<span class="string">"Bob"</span>)</span></pre></td></tr></table></figure>

<p>以上三行代码，我们创建了一个可变的map来存储得分情况，然后我们记录了Bob的得分是100分，最后我们又把Bob的分数取出来了。</p>
<p>这三行代码看似平淡无奇，实则暗藏了一点点玄机。</p>
<p>第二行实际是调用了HashMap的update方法。</p>
<p>第三行实际是调用了HashMap的apply方法。</p>
<p>我们可以把上面的代码改写成下面的等价形式：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> scores = <span class="keyword">new</span> scala.collection.mutable.<span class="type">HashMap</span>[<span class="type">String</span>, <span class="type">Int</span>]</span></pre></td></tr><tr><td class="code"><pre><span class="line">scores.update(<span class="string">"Bob"</span>, <span class="number">100</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> bobsScore = scores.apply(<span class="string">"Bob”)</span></span></pre></td></tr></table></figure>

<p>虽然等价，但是可读性却降低了一些。</p>
<p>apply方法我们之前讲过，就不再赘述。</p>
<p>update方法也不太复杂，它的规则就是：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">x(y) = z</span></pre></td></tr></table></figure>

<p>这样的代码会被编译为：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">x.update(y, z)</span></pre></td></tr></table></figure>

<p>这次博文名字虽然以抹掉糖衣开头，实则有点名不符实，因为这个语言特性过于简单，糖衣很薄，一抹就透。</p>
<p>这次的目的主要是介绍一个update方法的适用场景。</p>
<p>我们来看用来修改某个人地址的一段代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AddressChanger</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">update</span></span>(name: <span class="type">String</span>, age: <span class="type">Int</span>, newAddress: <span class="type">String</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">s"changing address of <span class="subst">$name</span>, whose age is <span class="subst">$age</span> to <span class="subst">$newAddress</span>"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">//actually change the address</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们可以这样来调用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> changer = <span class="keyword">new</span> <span class="type">AddressChanger</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">changer.update(<span class="string">"xiao ming"</span>, <span class="number">23</span>, <span class="string">"beijing"</span>)</span></pre></td></tr></table></figure>

<p>或者，我们也可以这样来调用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> addressOf = <span class="keyword">new</span> <span class="type">AddressChanger</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">addressOf(name = <span class="string">"xiao ming"</span>, age = <span class="number">23</span>) = <span class="string">"beijing"</span></span></pre></td></tr></table></figure>

<p>这两段代码是等价的。</p>
<p>比较一下，前一种用法显得中规中矩，没什么特别好的，也没啥特大的毛病。</p>
<p>可是后一种用法就不同了，读起来很通顺，有读英语语句的感觉：把名字叫做小明，年龄23岁的人的地址改为北京。</p>
<p>如果再给AddressChanger加上一个apply方法，我们还可以写这样的代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> currentAddress = addressOf(name = <span class="string">"xiao ming"</span>, age = <span class="number">23</span>)</span></pre></td></tr></table></figure>

<p>这样，读取和更新的代码都看起来非常自然。</p>
<p>如果我们把这两段代码连起来看：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> currentAddress = addressOf(name = <span class="string">"xiao ming"</span>, age = <span class="number">23</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">addressOf(name = <span class="string">"xiao ming"</span>, age = <span class="number">23</span>) = <span class="string">"beijing"</span></span></pre></td></tr></table></figure>

<p>感觉甚好。</p>
<p>addressOf(name = “xiao ming”, age = 23)可以看做一个整体，它就如同一个可读可写的属性。</p>
<p>我们把它放到赋值语句的右侧，就能取到小明的当前住址。</p>
<p>我们把它放到赋值语句的左侧，就能修改小明的住址。</p>
<p>apply和update都是蛮简单的语言特性，但是加以合适的应用，却能得到可读性极强的代码。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>除去Scala的糖衣(13) -- default parameter value</title>
    <url>/blog/2014/06/09/desugar-scala-default-parameter-value/</url>
    <content><![CDATA[<p>好久没有写博客了，上一次更新竟然是一月份。</p>
<p>说工作忙都是借口，咋有空看美剧呢。</p>
<p>这半年荒废掉博客说到底就是懒，惯性的懒惰。写博客这事儿，一丢掉就很久捡不起来。</p>
<p>闲话到此为止，下面进入正题。</p>
<p>Default parameter value，默认参数值。<br>这个很容易理解，给参数一个默认值，如果调用者不显式指明参数值，则使用默认值。如果显式指明了，那就用显式指明的值。</p>
<p>举个例子：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span></span>(name: <span class="type">String</span> = <span class="string">"world"</span>) = println(<span class="string">"hello "</span> + name)</span></pre></td></tr></table></figure>

<p>这个函数，如果我们不给它传参数，它就会打印hello world。就像这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">hello()</span></pre></td></tr></table></figure>

<p>如果给了参数：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">hello(<span class="string">"everybody"</span>)</span></pre></td></tr></table></figure>

<p>则打印hello everybody。</p>
<p>这个语言特性都有哪些应用场景呢？</p>
<p>它经常用来避免过多的重载。一个很常见很典型的例子就是构造函数重载。</p>
<p>在Java中，为了让调用者能够比较容易的创建某个类的实例，我们通常会提供几个参数列表比较短的构造函数。而这些构造函数存在的唯一意义就是为了写死某几个参数值。而在Scala中，有了这个语言特性，我们就无需那么麻烦了。</p>
<p>那这个语言特性是如何实现的呢？实际上简单的一塌糊涂。</p>
<p>这样一段代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Greeter</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">hello</span></span>(name: <span class="type">String</span> = <span class="string">"world"</span>) = println(<span class="string">"hello "</span> + name)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AnotherClass</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">new</span> <span class="type">Greeter</span>().hello()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们的Greeter类含有前面提到过的hello方法。在AnotherClass里调用了hello，并且没有显式指明参数值。</p>
<p>上面的Scala代码生成的bytecode反编译成Java是这样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Greeter</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hello</span><span class="params">(String name)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Predef..MODULE$.println(<span class="keyword">new</span> StringBuilder().append(<span class="string">"hello "</span>).append(name).toString());</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> String hello$<span class="keyword">default</span>$<span class="number">1</span>() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="string">"world"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AnotherClass</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">AnotherClass</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Greeter qual$<span class="number">1</span> = <span class="keyword">new</span> Greeter();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        String x$<span class="number">1</span> = qual$<span class="number">1</span>.hello$<span class="keyword">default</span>$<span class="number">1</span>();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qual$<span class="number">1</span>.hello(x$<span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以看到，我们所定义的hello方法反编译出来看起来很普通，就是个接受一个参数的方法。</p>
<p>而在Greeter类中，编译器为我们加入了另一个方法hello$default$1，这个方法就是返回一个写死的字符串，其值为world。</p>
<p>在AnotherClass中调用hello时，写死的字符串被取到，然后传进了hello里。</p>
<p>这样，被调用者提供了参数的默认值，调用者在调用时取得该值，然后传入方法。</p>
<h3 id="题外话"><a href="#题外话" class="headerlink" title="题外话"></a>题外话</h3><p>到这里我不禁联想起C#中的默认参数值的实现方式。</p>
<p>在C#中，默认参数的值会被编译成调用者的一个常量，而不是像Scala一样的由被调用者提供。</p>
<p>这样看起来貌似没啥区别，不就是写死的值换个地方吗？</p>
<p>其实不然，如果被调用者在A程序集内，调用者在B程序集内，那么A更新时，B就必须重新编译才能得到最新的默认值。也就是说，如果当前部署环境中同时存在A和B，而后我们拿一个新版的A来替换老的，这时B仍然在传递老的默认参数值给A。这样就会造成一些看似很诡异的行为偏差。</p>
<p>如果对C#的默认参数值的实现有兴趣，请看<a href="http://www.cnblogs.com/cuipengfei/archive/2011/04/13/2014325.html" target="_blank" rel="noopener">我很久很久之前写的博客</a></p>
<p>现在想来，C#这一语言特性的设计者为什么要把它设计成如此容易出错的样子呢？</p>
<p>思而不得其解。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>褪去Scala的糖衣(12) -- implicit function（补）</title>
    <url>/blog/2014/01/05/more-on-scala-implicit-function/</url>
    <content><![CDATA[<p>上次<a href="http://cuipengfei.me/blog/2014/01/01/desugar-scala-10/" target="_blank" rel="noopener">博客谈到了implicit function</a>，但是漏掉了一些东西，今天补上。</p>
<p>由于上次已经讲过implicit function的实现细节，这次就不再重复了。今天只补充上次漏掉了的implicit function的一种很好的实践。</p>
<p>先看一段specs2的测试代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.specs2.mutable._</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HelloWorldSpec</span> <span class="keyword">extends</span> <span class="title">Specification</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="string">"The 'Hello world' string"</span> should &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="string">"contain 11 characters"</span> in &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      <span class="string">"Hello world"</span> must have size <span class="number">11</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们试着理解这个测试代码在做什么的时候，无须多少思考，因为它和人类语言一样的亲近和自然。但是如果我们想知道specs2如何做到这一点时，就有点费解了。</p>
<p>我们知道xObject yMethod zParameter的写法是一个语法糖，它和xObject.yMethod(zParameter)是一样的。也就是说should和in都是方法名。于是，问题来了，should和in前面是个String啊，String上哪有这两个方法的定义？</p>
<p>想必答案你已经猜到了，specs2定义了一些implicit functions来把String转换成能够调用should和in的对象。至于是哪些对象，因为涉及到了specs2的细节，我们暂不讨论。我们试着模仿specs2来写出一段类似人类语言的代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span>(<span class="params">name: <span class="type">String</span></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">eat</span></span>(food: <span class="type">String</span>) = println(<span class="string">"I just ate "</span> + food)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>定义一个Person类，Person有一个名字，只会做一件事：吃。</p>
<p>然后我们可以这样调用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">Person</span>(<span class="string">"Xiao ming"</span>).eat(<span class="string">"steamed dumplings"</span>)</span></pre></td></tr></table></figure>

<p>我们让小明吃点蒸饺。或者也可以这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">Person</span>(<span class="string">"Xiao ming"</span>) eat <span class="string">"boiled dumplings"</span></span></pre></td></tr></table></figure>

<p>再煮点饺子让小明吃。这就是我们前面提到过的xObject yMethod zParameter的写法了。让小明吃点东西还得new一个Person出来，这太麻烦了，我们定义一个implicit function：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">implicit</span> <span class="function"><span class="keyword">def</span> <span class="title">stringToPerson</span></span>(name: <span class="type">String</span>) = <span class="keyword">new</span> <span class="type">Person</span>(name)</span></pre></td></tr></table></figure>

<p>这样就可以把String转换成Person了。然后就可以像这样写：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="string">"Xiao ming"</span> eat <span class="string">"more dumplings"</span></span></pre></td></tr></table></figure>

<p>虽然我们写的是“Xiao ming”，但是编译器会把stringToPerson的调用加上去，然后才调用eat方法。</p>
<p>这行代码看起来已经很接近人类语言了。</p>
<p>连吃三顿饺子，明哥，您饱了吧？</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>剥掉Scala的糖衣(11) -- structural types</title>
    <url>/blog/2014/01/02/desugar-scala-11/</url>
    <content><![CDATA[<p>Structural types，中文怎么翻译不确定。我们可以用它来实现类似于鸭子类型的效果。为什么说是“类似”鸭子类型呢？稍后会说到它和鸭子类型的区别。</p>
<p>举一个例子，看看它都可以做什么：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">makeNoise</span></span>(quacker: &#123;<span class="function"><span class="keyword">def</span> <span class="title">quack</span></span>(): <span class="type">String</span>&#125;) = quacker.quack</span></pre></td></tr></table></figure>

<p>声明一个方法，叫做makeNoise，接受什么类型的参数呢？不做严格限制，我们只声明说参数必须有一个叫做quack的方法，该quack方法返回值类型为String。然后在makeNoise方法内调用quack方法。请注意我们并没有声明一个含有quack方法签名的接口或者类，我们仅仅是在声明参数的同时声明我们期待参数含有什么样的成员。</p>
<p>然后我们声明一个Duck类：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Duck</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">quack</span></span>() = <span class="string">"real quack"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这样就可以调用makeNoise方法了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">makeNoise(<span class="keyword">new</span> <span class="type">Duck</span>)</span></pre></td></tr></table></figure>

<p>或者再声明一个NotADuck类：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NotADuck</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">quack</span></span>() = <span class="string">"fake quack"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>也可以把它传给makeNoise方法：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">makeNoise(<span class="keyword">new</span> <span class="type">NotADuck</span>)</span></pre></td></tr></table></figure>

<p>甚至是匿名对象也可以：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">makeNoise(<span class="keyword">new</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">quack</span></span>() = <span class="string">"anonymous quack"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;)</span></pre></td></tr></table></figure>

<p>如果我们把Duck的quack方法改个名字：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Duck</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">quackRenamed</span></span>() = <span class="string">"real quack"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>那么编译器就会对下面这行代码给出错误：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">makeNoise(<span class="keyword">new</span> <span class="type">Duck</span>)</span></pre></td></tr></table></figure>

<p>type mismatch, found : hello.Duck, required: AnyRef{def quack(): String}</p>
<p>是做了编译时类型检查的。</p>
<p>然后我们反编译代码，看看它是如何实现的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> Class[] reflParams$Cache1;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> SoftReference reflPoly$Cache1;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Hello.reflParams$Cache1 = <span class="keyword">new</span> Class[<span class="number">0</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Hello.reflPoly$Cache1 = <span class="keyword">new</span> SoftReference((T)<span class="keyword">new</span> EmptyMethodCache());</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Method reflMethod$Method1(<span class="keyword">final</span> Class x$<span class="number">1</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    MethodCache methodCache1 = Hello.reflPoly$Cache1.get();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (methodCache1 == <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        methodCache1 = (MethodCache)<span class="keyword">new</span> EmptyMethodCache();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Hello.reflPoly$Cache1 = <span class="keyword">new</span> SoftReference((T)methodCache1);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Method method1 = methodCache1.find(x$<span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (method1 != <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> method1;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    method1 = ScalaRunTime$.MODULE$.ensureAccessible(x$<span class="number">1</span>.getMethod(<span class="string">"quack"</span>, Hello.reflParams$Cache1));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Hello.reflPoly$Cache1 = <span class="keyword">new</span> SoftReference((T)methodCache1.add(x$<span class="number">1</span>, method1));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> method1;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">makeNoise</span><span class="params">(<span class="keyword">final</span> Object quacker)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">final</span> Object invoke;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        invoke = reflMethod$Method1(quacker.getClass()).invoke(quacker, <span class="keyword">new</span> Object[<span class="number">0</span>]);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">catch</span> (InvocationTargetException ex) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">throw</span> ex.getCause();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> (String)invoke;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们可以看到，makeNoise方法的参数类型被编译成了Object。方法内部通过反射去调用quack方法。</p>
<p>再仔细看一下，方法内做了个catch，如果找不到quack方法就把异常抛出来。我们刚才不是看到有编译时类型检查吗？怎么会找不到quack方法呢？</p>
<p>其实找不到quack方法的情况还是会存在的。假如我们把以上代码打成jar包供别人调用，那别人看到的你这个方法要的是Object啊，随便传一个什么东西进来都可以。如果传入的参数没有quack方法，那自然就会有异常了。这也是一个很好的信号，告诉我们说这个语言特性不适合用在public API中。</p>
<p>刚开始时提到过，这个语言特性不能叫做鸭子类型，为什么呢？我们看两个真正鸭子类型的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">makeNoise</span>(<span class="params">quacker</span>)</span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">return</span> quacker.quack()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">makeNoise(&#123;<span class="attr">quack</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="keyword">return</span> <span class="string">"quack quack"</span>&#125;&#125;)</span></pre></td></tr></table></figure>

<p>上面的是JavaScript的，下面的是C#的。</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">Duck</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> String <span class="title">Quack</span> (<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		<span class="keyword">return</span> <span class="string">"quack quack"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">Hello</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> String <span class="title">MakeNoise</span> (<span class="params"><span class="keyword">dynamic</span> quacker</span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		<span class="keyword">return</span> quacker.Quack ();</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">MainClass</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span> (<span class="params"><span class="keyword">string</span>[] args</span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		Console.Out.WriteLine (<span class="keyword">new</span> Hello ().MakeNoise (<span class="keyword">new</span> Duck ()));</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>在上面两段代码中，如果我们把quack或者Quack改个名字的话，都会导致运行失败。而且在声明参数的时候也没有指明我们期待有一个quack或者Quack成员的存在。所以说Scala的这个语言特性不能称之为鸭子类型的原因在于两点：</p>
<ul>
<li>Scala做了编译时类型安全检查</li>
<li>声明参数时显式的指明了期待的成员</li>
</ul>
<p>存疑：虽然上面的C#例子看起来是鸭子类型，但是C#的wiki页面上的typing discipline一项里并没有列出duck typing。原因不明。</p>
<p>=======================================</p>
<p>后记：C#的wiki页面没有把duck typing列为其typing discipline的一项。但是，duck typing的wiki页面上又用C#举了例子。互相矛盾啊，于是我在stackoverflow上问了个问题，引来了C#编译器的程序员之一Eric Lippert，他回答问题之后还写了篇博客来表示对duck typing这个名词的困惑。看完之后我表示更晕了。</p>
<p>所以请不要把上面我说的关于duck typing的东西当真，随便瞄一眼就好了。duck typing是一个没有清晰定义的名词，在我们能够共同认同它的某一种特定的definition之前去讨论它是无谓的。</p>
<p>下面把链接列出：</p>
<p><a href="http://en.wikipedia.org/wiki/C_Sharp_programming_language" target="_blank" rel="noopener">C#的wiki</a>  （typing discipline在页面右侧的那个表里）</p>
<p><a href="http://en.wikipedia.org/wiki/Duck_typing" target="_blank" rel="noopener">Duck typing的wiki</a></p>
<p><a href="http://ericlippert.com/2014/01/02/what-is-duck-typing/" target="_blank" rel="noopener">Eric Lippert的博客</a></p>
<p>以上链接，建议别点  :)</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>褪去Scala的糖衣(10) -- implicit function</title>
    <url>/blog/2014/01/01/desugar-scala-10/</url>
    <content><![CDATA[<p>Implicit function，中文或许应该叫做隐式函数吧。主要用来作隐式类型转换。例子：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Duck</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">makeDuckNoise</span></span>() = <span class="string">"gua gua"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Chicken</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">makeChickenNoise</span></span>() = <span class="string">"ge ge"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ducken</span>(<span class="params">chicken: <span class="type">Chicken</span></span>) <span class="keyword">extends</span> <span class="title">Duck</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">override</span> <span class="function"><span class="keyword">def</span> <span class="title">makeDuckNoise</span></span>() = chicken.makeChickenNoise()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>三个类，鸭子，鸡，还有伪装成鸭子的鸡。如果有这么一个函数：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">giveMeADuck</span></span>(duck: <span class="type">Duck</span>) = duck.makeDuckNoise()</span></pre></td></tr></table></figure>

<p>该函数要求我们给它提供一只鸭子，我们可以这么调用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">giveMeADuck(<span class="keyword">new</span> <span class="type">Duck</span>)</span></pre></td></tr></table></figure>

<p>要鸭子就给鸭子，没问题。或者是也可以这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">giveMeADuck(<span class="keyword">new</span> <span class="type">Ducken</span>(<span class="keyword">new</span> <span class="type">Chicken</span>))</span></pre></td></tr></table></figure>

<p>把一只鸡伪装成鸭子给它，也没问题。但是如果直接把鸡给它就不行了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">giveMeADuck(<span class="keyword">new</span> <span class="type">Chicken</span>)</span></pre></td></tr></table></figure>

<p>编译器会给出type mismatch的错误。</p>
<p>我们已经有一个Ducken类可以用来把鸡化装成鸭子了，那有没有一种方法可以让我们鬼鬼祟祟的就把化妆这件事儿做了？那就不用每次都明火执仗的了。</p>
<p>答案就是用implicit function：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">implicit</span> <span class="function"><span class="keyword">def</span> <span class="title">chickenToDuck</span></span>(chicken: <span class="type">Chicken</span>) = <span class="keyword">new</span> <span class="type">Ducken</span>(chicken)</span></pre></td></tr></table></figure>

<p>它的用法很简单，给函数加上implicit这个修饰符，函数的参数是鸡，返回类型是鸭子 （或者是任何鸭子的子类都行）。</p>
<p>然后这行代码就可以编译了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">giveMeADuck(<span class="keyword">new</span> <span class="type">Chicken</span>)</span></pre></td></tr></table></figure>

<p>来看一下反编译的结果：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">giveMeADuck</span><span class="params">(Duck duck)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> duck.makeDuckNoise();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Ducken <span class="title">chickenToDuck</span><span class="params">(Chicken chicken)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Ducken(chicken);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>giveMeADuck和chickenToDuck都被编译成了中规中矩的方法。giveMeADuck(new Chicken)则被编译成了这个样子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">giveMeADuck(<span class="keyword">this</span>.chickenToDuck(<span class="keyword">new</span> Chicken()));</span></pre></td></tr></table></figure>

<p>一点意外都没有，implicit在反编译后无影无踪，它就是很纯的一点点糖。</p>
<p>我意淫一下：编译器在发现我们给它的chicken和想要的duck之间存在type mismatch的时候先不着急给出错误，看看有没有标为implicit的函数可以把chicken变成duck或者其子类，如果有则调用该方法，没有再给出错误。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>今天是二零一四年的第一天，回忆一下二零一三年都干了些什么，然后再回忆一下没干什么</title>
    <url>/blog/2014/01/01/my-2013/</url>
    <content><![CDATA[<h2 id="干了什么"><a href="#干了什么" class="headerlink" title="干了什么"></a>干了什么</h2><p><strong>上半年：</strong></p>
<p>看了十几本书；</p>
<p>写了几篇博客；</p>
<p>在github上做了一个豆瓣读书的插件；</p>
<p>肇始了一个十万个为什么的活动，自始至终只有我一个人参与，然后就挂掉了；</p>
<p>做了三章的SICP，然后卡住做不动了；</p>
<p>乏善可陈。</p>
<p>上面列的最后两项的价值最大，但都没做完。究其原因：</p>
<ul>
<li>坚持不够</li>
<li>attention span太短</li>
</ul>
<p>这两项在下半年做的事儿里address到了。</p>
<p><strong>下半年：</strong></p>
<p>又看了十几本书（量与上半年持平）；</p>
<p>又写了几篇博客（量较上半年有增）；</p>
<p>跟完了Coursera上的Functional programming principles in Scala的课，拿到了Statement of Accomplishment with distinction；</p>
<p>键盘速度到了60wpm左右；</p>
<p>找到了坚持和attention span问题的解决方案；</p>
<p>有善可陈。</p>
<p>善在最后一项。解决方案是这样的：</p>
<ul>
<li>戒掉微博，规避碎片化娱乐化的信息。（还没戒掉的是美剧）</li>
<li>把番茄钟用到自己的时间中去。track用在“正经事儿”上的时间。</li>
</ul>
<p>不细说了，下面说没干什么。</p>
<h2 id="没干什么"><a href="#没干什么" class="headerlink" title="没干什么"></a>没干什么</h2><p>我的情商和people skill很低，低的你都猜不到哦亲。</p>
<p>我可以预感到这个东西即将成为我的bottle neck。但是我二零一三年并没有在上面做太多事情。</p>
<p>读了几本我认为会有帮助的书，一些被我认为是不知所云，一些促使我得出了死硬的理性派结论。</p>
<p>二零一四年需要在这个领域积累知识，然后期待量变带来的质变。</p>
]]></content>
      <tags>
        <tag>me</tag>
      </tags>
  </entry>
  <entry>
    <title>掀开Scala的糖衣(9) -- function composition</title>
    <url>/blog/2013/12/30/desugar-scala-9/</url>
    <content><![CDATA[<p>Function composition，顾名思义，就是函数的组合。直接举例：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sayHi</span></span>(name: <span class="type">String</span>) = <span class="string">"Hi, "</span> + name</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sayBye</span></span>(str: <span class="type">String</span>) = str + <span class="string">", bye"</span></span></pre></td></tr></table></figure>

<p>两个方法，一个说你好，一个说再见。然后我们创建很多个人名：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> names = <span class="type">List</span>(<span class="string">"world"</span>, <span class="string">"tom"</span>, <span class="string">"xiao ming"</span>)</span></pre></td></tr></table></figure>

<p>我们希望对List中的每个人都说你好然后说再见：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">names.map(sayHi).map(sayBye)</span></pre></td></tr></table></figure>

<p>好，这样我们的目的实现了。但是，我们调用了两次map，会对整个List遍历两次。我们希望只遍历一次：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">names.map &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  name =&gt; sayBye(sayHi(name))</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>嗯，这样可以了，但是看起来有一点点不爽。按照从左到右的阅读习惯，我们先看到了sayBye，然后才看到sayHi，而且括号还是包了两层。需要想那么一小下才能明白：按照eager evaluation的规则，先运行sayHi，然后把结果传入sayBye，最后得到一个我们想要的结果。</p>
<p>其实我们真正想要的是一个链式操作，一个pipe：把数据用某种操作进行处理，然后把处理后的结果传递给第二个操作继续处理。类似于这样：a.pipe(b)，或者是这样：a | b。</p>
<p>而Scala的function composition正是做这件事的：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">names.map(sayHi _ andThen sayBye)</span></pre></td></tr></table></figure>

<p>从左到右，当成英语来读：先sayHi然后再sayBye，清晰明了。</p>
<p>这个andThen并不是什么神奇的语言关键字。它其实只是定义在Function1上的一个方法而已。我们来看看反编译的结果就知道了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="type">List</span> names =</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="type">List</span>$.<span class="type">MODULE</span>$.apply((<span class="type">Seq</span>)<span class="type">Predef</span>$.<span class="type">MODULE</span>$.wrapRefArray(</span></pre></td></tr><tr><td class="code"><pre><span class="line">(<span class="type">Object</span>[])<span class="keyword">new</span> <span class="type">String</span>[] &#123; <span class="string">"world"</span>, <span class="string">"tom"</span>, <span class="string">"xiao ming"</span> &#125;));</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> (<span class="type">List</span>)names.map(((<span class="type">Function1</span>)<span class="keyword">new</span> <span class="type">Serializable</span>() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    public static <span class="keyword">final</span> long serialVersionUID = <span class="number">0</span>L;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    public <span class="keyword">final</span> <span class="type">String</span> apply(<span class="keyword">final</span> <span class="type">String</span> name) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="type">Hello</span>.<span class="keyword">this</span>.sayHi(name);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;).andThen((<span class="type">Function1</span>)<span class="keyword">new</span> <span class="type">Serializable</span>() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    public static <span class="keyword">final</span> long serialVersionUID = <span class="number">0</span>L;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    public <span class="keyword">final</span> <span class="type">String</span> apply(<span class="keyword">final</span> <span class="type">String</span> str) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="type">Hello</span>.<span class="keyword">this</span>.sayBye(str);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;), <span class="type">List</span>$.<span class="type">MODULE</span>$.canBuildFrom());</span></pre></td></tr></table></figure>

<p>由于反编译工具的原因，这个反编译的结果并不是合法的Java代码，我们凑合着看一下。</p>
<p>我们看到，sayHi和sayBye都是被包到了Functoin1里面，调一下第一个Function1的andThen方法，把第二个Function1传进去，会返回一个新的Function1。这个返回的新的Function1就是我们想要的链式操作了。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>揭开Scala的糖衣(8) -- pattern matching</title>
    <url>/blog/2013/12/29/desugar-scala-8/</url>
    <content><![CDATA[<p>Pattern matching是Scala中很好用的一个语言特性。先举一个最简单的例子：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> number = <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">number <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="number">1</span> =&gt; doSomething()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="number">2</span> =&gt; doSomethingElse()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> _ =&gt; doDefault()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个代码和我们熟悉的switch case看起来很像，其实，这段代码反编译之后和Java的switch case确实就是一样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> number = <span class="number">1</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i = number; <span class="keyword">switch</span> (i)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">default</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">  doDefault(); <span class="keyword">break</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="number">2</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">  doSomethingElse(); <span class="keyword">break</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="number">1</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">  doSomething();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>但是和Java的switch case不一样的是，Scala的pattern matching作为一个expression是可以evaluate一个值出来的，我们把上面的代码改一下，让doSomething,doSomethingElse和doDefault都返回点东西：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> number = <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> result = number <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="number">1</span> =&gt; doSomething()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="number">2</span> =&gt; doSomethingElse()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> _ =&gt; doDefault()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这样，result就承接了能够match上的那个case的返回值。而无需像普通的swtich case一样在每个case中给result赋值。</p>
<p>单是这样看，pattern matching的魅力还不算怎么大，我们再看一下下面这个例子：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Animal</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Cat</span>(<span class="params">name: <span class="type">String</span></span>) <span class="keyword">extends</span> <span class="title">Animal</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="title">case</span> <span class="title">class</span> <span class="title">Dog</span>(<span class="params">name: <span class="type">String</span></span>) <span class="keyword">extends</span> <span class="title">Animal</span></span></span></pre></td></tr></table></figure>

<p>首先声明几个case classes。这些case classes会被编译成一些比较复杂的classes，我们暂时不去关心。</p>
<p>然后看一下如何match类型及其属性：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> animal = createAnimal</span></pre></td></tr><tr><td class="code"><pre><span class="line">animal <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="type">Dog</span>(anyName) =&gt; <span class="string">"this is a dog"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="type">Cat</span>(<span class="string">"kitty"</span>) =&gt; <span class="string">"this is a cat named kitty"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> _ =&gt; <span class="string">"other animal"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这段代码很容易懂，如果创建出来的animal是狗的话，无论它的名字叫什么，我们都返回this is a dog，如果是一只名叫kitty的猫，则返回this is a cat named kitty。如果都不是的话，则返回other animal。</p>
<p>很简单的几行代码，就做出了类型判断而且还有属性判断。</p>
<p>如果没有pattern matching，那么就要写if去判断类型，如果类型符合还要做类型转换，然后把转换后的变量中的属性取出来，再然后才能对属性的值做判断，最后才能返回点东西……</p>
<p>类型判断，类型转换，取属性，属性值判断，返回值。这么五件事我们用这样一行代码就都解决了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="type">Cat</span>(<span class="string">"kitty"</span>) =&gt; <span class="string">"this is a cat named kitty"</span></span></pre></td></tr></table></figure>

<p>这样的Scala代码会被编译成什么样呢？其实就是我们上面描述的很复杂的样子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Animal animal;</span></pre></td></tr><tr><td class="code"><pre><span class="line">String string;</span></pre></td></tr><tr><td class="code"><pre><span class="line">Animal animal2 = (animal = <span class="keyword">this</span>.createAnimal());</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ((animal2 <span class="keyword">instanceof</span> Dog)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="string">"this is a dog"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!((animal2 <span class="keyword">instanceof</span> Cat))) <span class="keyword">return</span> <span class="string">"other animal"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">Cat cat = (Cat)(animal2);</span></pre></td></tr><tr><td class="code"><pre><span class="line">String string2 = (string = cat.name());</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="string">"kitty"</span> == <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (string2 == <span class="keyword">null</span>) <span class="keyword">return</span> <span class="string">"this is a cat named kitty"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="string">"other animal"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125; <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!(<span class="string">"kitty"</span>.equals(string2))) <span class="keyword">return</span> <span class="string">"other animal"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="string">"this is a cat named kitty"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这段反编译出来的代码不很可读。我们就凑合着粗看一下。里面和我们前面说的一样，都是if else，类型判断，转型，判等……</p>
<p>当然，用反编译工具给出的Java代码和上面的Scala代码作比较并不公平。我们自己把它写一遍：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Animal animal = createAnimal();</span></pre></td></tr><tr><td class="code"><pre><span class="line">String result = <span class="string">"other animal"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (animal <span class="keyword">instanceof</span> Dog) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    result = <span class="string">"this is a dog"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (animal <span class="keyword">instanceof</span> Cat) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Cat cat = (Cat) animal;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (cat.name() == <span class="string">"kitty"</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        result = <span class="string">"this is a cat named kitty"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> result;</span></pre></td></tr></table></figure>

<p>这个样子再和上面的Scala代码比较，可以看到Scala编译器帮我们省掉了局部变量，类型判断和判等这些噪音。</p>
<p>Pattern matching还有很多其他用法，比如用来match tuple：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> hostPort = (<span class="string">"localhost"</span>, <span class="number">80</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">hostPort <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> (<span class="string">"localhost"</span>, port) =&gt; <span class="string">"this is localhost address"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> (host, port) =&gt; <span class="string">"some other address"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>或者是用来match option：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> map = <span class="type">Map</span>(<span class="number">1</span> -&gt; <span class="string">"one"</span>, <span class="number">2</span> -&gt; <span class="string">"two"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">map.get(<span class="number">1</span>) <span class="keyword">match</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="type">Some</span>(str) =&gt; <span class="string">"get something from map: "</span> + str</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">case</span> <span class="type">None</span> =&gt; <span class="string">"no result"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>Scala标准库中的Map的get方法的返回类型是Option，如果能够get到东西则返回Some，其中包着get到的值。如果get不到东西，则返回一个None。</p>
<p>由于Tuple和Option本身也是case class，所以上面的两段代码反编译出来和上面的Java代码是大同小异的。就不再赘述了。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>剥掉Scala的糖衣(7) -- apply method</title>
    <url>/blog/2013/12/29/desugar-scala-7/</url>
    <content><![CDATA[<p>apply method是一个很简单的语言特性。如果一个class或者是object有一个主要的方法，那么与其每次显式的调用这个主要的方法，还不如隐式调用。举个例子：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Kettle</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">boil</span></span>(water: <span class="type">Water</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    water.isWarm = <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    water</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>一个水壶的主要作用就是烧开水，于是我们每次都要调用boil方法来烧开水:</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> kettle: <span class="type">Kettle</span> = <span class="keyword">new</span> <span class="type">Kettle</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">kettle.boil(<span class="keyword">new</span> <span class="type">Water</span>())</span></pre></td></tr></table></figure>

<p>如果要把它改写成apply method的方式，只需要给boil改个名字就好了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Kettle</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">apply</span></span>(water: <span class="type">Water</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    water.isWarm = <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    water</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>然后需要烧开水时，就只需把水倒进壶里就好了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> kettle: <span class="type">Kettle</span> = <span class="keyword">new</span> <span class="type">Kettle</span>()</span></pre></td></tr><tr><td class="code"><pre><span class="line">kettle(<span class="keyword">new</span> <span class="type">Water</span>())</span></pre></td></tr></table></figure>

<p>这个语言特性的实现很简单，不用说也可以猜到，无非就是把kettle(water)编译成kettle.apply(water)。反编译一下，Kettle class的定义毫无出奇之处：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Kettle</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> Water <span class="title">apply</span><span class="params">(Water water)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    water.isWarm_$eq(<span class="keyword">true</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> water;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>烧水的代码被编译成了这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Kettle kettle = <span class="keyword">new</span> Kettle();</span></pre></td></tr><tr><td class="code"><pre><span class="line">kettle.apply(<span class="keyword">new</span> Water());</span></pre></td></tr></table></figure>

<p>我们刚开始说过apply method也可以用在object里。下面举个例子，我们把Kettle烧水的能力移到它的companion object里面去：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Kettle</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">apply</span></span>(water: <span class="type">Water</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    water.isWarm = <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    water</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>然后烧水的时候就可以这样调用：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">Kettle</span>(<span class="keyword">new</span> <span class="type">Water</span>())</span></pre></td></tr></table></figure>

<p>反编译出来的结果大同小异，就不再赘述了，唯一的区别就是apply变成了静态方法。</p>
<p>上面这个水壶烧水的例子并不是最佳实践的作法。apply method的一个最佳实践是用来做工厂。比如Scala标准库中的List就提供了apply方法来给我们创建List：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">List</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="type">List</span>(<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>)</span></pre></td></tr></table></figure>

<p>或者是Map也有类似的用法：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="type">Map</span>(<span class="number">1</span> -&gt; <span class="string">"a"</span>, <span class="number">2</span> -&gt; <span class="string">"b"</span>, <span class="number">3</span> -&gt; <span class="string">"c"</span>)</span></pre></td></tr></table></figure>

<p>以上的两段代码并不是在调用List和Map的constructor，而是在调用List和Map的companion objects的apply方法。</p>
<p>Map的创建可以用apply method，而Map最常用的一个方法就是通过key来取得value，这个也有apply method来做：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> map = <span class="type">Map</span>(<span class="number">1</span> -&gt; <span class="string">"a"</span>, <span class="number">2</span> -&gt; <span class="string">"b"</span>, <span class="number">3</span> -&gt; <span class="string">"c"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">map(<span class="number">2</span>)</span></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>褪去Scala的糖衣(6) -- partial application</title>
    <url>/blog/2013/12/25/desugar-scala-6/</url>
    <content><![CDATA[<p>这篇博客介绍一下Scala中的partial application，局部应用，或者叫做柯里化。</p>
<p>所谓柯里化就是指把一个接受多个参数的函数的一部分参数写死，剩下的一部分由调用者提供。</p>
<p>用Java代码来表述，大概可以写成这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">greet</span><span class="params">(String greeting, String name)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> greeting + <span class="string">" "</span> + name;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">sayHello</span><span class="params">(String name)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> greet(<span class="string">"Hello"</span>, name);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">greetXiaoMing</span><span class="params">(String greeting)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> greet(greeting, <span class="string">"Xiao Ming"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>greet用来给某个不确定的人打个不确定的招呼。</p>
<p>sayHello用来给某个不确定的人说一句固定的Hello。</p>
<p>greetXiaoMing用来给一个固定的人小明打一个不确定的招呼。</p>
<p>如果用Scala来表达同样的含义的话，可以这样写：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">greet</span></span>(greeting: <span class="type">String</span>, name: <span class="type">String</span>) = greeting + <span class="string">" "</span> + name</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sayHello</span> </span>= greet(<span class="string">"hello"</span>, _: <span class="type">String</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">greetXiaoMing</span> </span>= greet(_: <span class="type">String</span>, <span class="string">"Xiao Ming"</span>)</span></pre></td></tr></table></figure>

<p>其实比Java代码也简单不了多少。只是把暂时不确定的参数用下划线指代出来。</p>
<p>然后我们就可以在稍后需要调用它们的时候再把参数传入：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">sayHello(<span class="string">"world"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">greetXiaoMing(<span class="string">"Ni Hao"</span>)</span></pre></td></tr></table></figure>

<p>然后我们看一下这个语言特性是怎么实现的呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">greet</span><span class="params">(String greeting, String name)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> StringBuilder().append(greeting).append(<span class="string">" "</span>).append(name).toString();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function1&lt;String, String&gt; <span class="title">sayHello</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> AbstractFunction1() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> String <span class="title">apply</span><span class="params">(String x$<span class="number">1</span>)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">return</span> Hello.<span class="keyword">this</span>.greet(<span class="string">"hello"</span>, x$<span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function1&lt;String, String&gt; <span class="title">greetXiaoMing</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> AbstractFunction1() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> String <span class="title">apply</span><span class="params">(String x$<span class="number">2</span>)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">return</span> Hello.<span class="keyword">this</span>.greet(x$<span class="number">2</span>, <span class="string">"Xiao Ming"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以看到sayHello和greetXiaoMing并不是返回String的，它们返回的是Function1 of String, String。也就是说直接调用它们俩是得不到我们想要的结果的，必须把这个Function1上的apply再调一下才行。实际上正是如此，这段代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">sayHello(<span class="string">"world"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">greetXiaoMing(<span class="string">"Ni Hao"</span>)</span></pre></td></tr></table></figure>

<p>会被编译成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sayHello().apply(<span class="string">"world"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">greetXiaoMing().apply(<span class="string">"Ni Hao"</span>);</span></pre></td></tr></table></figure>

<p>除此之外，partial application还可以有另一种稍微另类一些的写法：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">greet</span></span>(greeting: <span class="type">String</span>)(name: <span class="type">String</span>) = greeting + <span class="string">" "</span> + name</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sayHello</span> </span>= greet(<span class="string">"hello"</span>)(_)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">greetXiaoMing</span> </span>= greet(_: <span class="type">String</span>)(<span class="string">"Xiao Ming"</span>)</span></pre></td></tr></table></figure>

<p>反编译的结果和上面的代码是完全一致的。</p>
<p>我不太清楚这种写法存在的意义是不是仅仅起一个向外界宣称“我这个函数之所以出现就是给你局部应用的，不要一下子把两个参数都给我”的作用。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>剥开Scala的糖衣(5) -- lazy</title>
    <url>/blog/2013/12/24/desugar-scala-5/</url>
    <content><![CDATA[<p>Scala中的lazy关键字是实现延迟加载的好帮手。</p>
<p>在Java中想要做到延迟加载，常规的做法是大抵是这样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> String str = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getStr</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (str == <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        str = getStrFromWebService();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> str;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>以这种方式来保证web service不会被无谓的重复请求。</p>
<p>C#中则可以使用Lazy of T来实现类似的事:</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Lazy&lt;String&gt; str = <span class="keyword">new</span> Lazy&lt;<span class="keyword">string</span>&gt; (() =&gt; GetStrFromWebService ());</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> String Str</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">get</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		<span class="keyword">return</span> str.Value;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>Lazy of T保证传入其中的Func只执行一次。</p>
<p>（其实，Java也可以使用Guava中的memoize来实现类似的事情）</p>
<p>要么自己写代码，要么通过库来实现。</p>
<p>而Scala则在语言级别给出了解决方案：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">lazy</span> <span class="keyword">val</span> str = getStrFromWebService()</span></pre></td></tr></table></figure>

<p>仅此一行。</p>
<p>只要用lazy关键字修饰一下str，延迟执行的事就搞定了。</p>
<p>其实Scala编译器做的事情和我们手动做的区别不大：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> String str;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">boolean</span> bitmap$<span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> String str$lzycompute()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  	<span class="keyword">if</span> (!<span class="keyword">this</span>.bitmap$<span class="number">0</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.str = getStrFromWebService();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.bitmap$<span class="number">0</span> = <span class="keyword">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  	<span class="keyword">return</span> <span class="keyword">this</span>.str;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">str</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>.bitmap$<span class="number">0</span> ? <span class="keyword">this</span>.str : str$lzycompute();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>你看编译器多热心，还加了锁哦。</p>
<p>小小总结一下：</p>
<p>对于这样一个表达式：<br>lazy val t:T = expr<br>无论expr是什么东西，字面量也好，方法调用也好。Scala的编译器都会把这个expr包在一个方法中，并且生成一个flag来决定只在t第一次被访问时才调用该方法。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>去掉Scala的糖衣(4) -- type aliase</title>
    <url>/blog/2013/12/23/desugar-scala-4/</url>
    <content><![CDATA[<p>Scala中有一个type关键字，用来给类型或者是操作起别名，用起来很是方便。</p>
<p>比如这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">People</span> </span>= <span class="type">List</span>[<span class="type">Person</span>]</span></pre></td></tr></table></figure>

<p>这样就是给List[Person]（方括号是Scala的类型参数的写法）声明了一个别名，叫做People。</p>
<p>接下来就可以这样使用它：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">teenagers</span></span>(people: <span class="type">People</span>): <span class="type">People</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  people.filter(person =&gt; person.age &lt; <span class="number">20</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个代码编译之后没有什么神奇的，仅仅是把所有出现People这个字眼的地方都用List of Person替代了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> List&lt;Person&gt; <span class="title">teenagers</span><span class="params">(List&lt;Person&gt; people)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">return</span> (List)people.filter(<span class="keyword">new</span> AbstractFunction1() &#123; <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">apply</span><span class="params">(Person person)</span> </span>&#123; <span class="keyword">return</span> person.age() &lt; <span class="number">20</span>; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这种给类型一个别名的特性只是一个小糖豆，不太甜，真正有趣的是给一类操作命名（联想C#中定义delegate）。</p>
<p>比如这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">PersonPredicate</span> </span>= <span class="type">Person</span> =&gt; <span class="type">Boolean</span></span></pre></td></tr></table></figure>

<p>接受一个Person，返回一个Boolean，我们把这一类用来判断一个人是否符合某个条件的操作统称为PersonPredicate。</p>
<p>然后我们可以定义以下predicate：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> teenagerPred: <span class="type">PersonPredicate</span> = person =&gt; person.age &lt; <span class="number">20</span></span></pre></td></tr></table></figure>

<p>然后前面写过的teenagers方法就可以这样重新定义：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">teenagers</span></span>(people: <span class="type">People</span>): <span class="type">People</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  people.filter(teenagerPred)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>按照这个思路下去，我们就可以开始composite functions了。比如说，我们跟人收税，就可以这么做：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">Tax</span> </span>= <span class="type">Person</span> =&gt; <span class="type">Double</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> incomeTax: <span class="type">Tax</span> = person =&gt; person.income * <span class="number">5</span> / <span class="number">100</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> kejuanzaTax: <span class="type">Tax</span> = person =&gt; person.income * <span class="number">20</span> / <span class="number">100</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">giveMeYourMoney</span></span>(p: <span class="type">Person</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  calculateTax(p, <span class="type">List</span>(incomeTax, kejuanzaTax))</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculateTax</span></span>(person: <span class="type">Person</span>, taxes: <span class="type">List</span>[<span class="type">Tax</span>]): <span class="type">Double</span> = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  taxes.foldLeft(<span class="number">0</span>d) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    (acc, curTax) =&gt; acc + curTax(person)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>从一个人那里拿到钱，这种操作，我们称之为Tax。然后定义个税和苛捐杂税，或者也可以有任意多的税种。</p>
<p>然后就可以把任意的几个税种放在一个List里面，和calculateTax去composite了。</p>
<p>当然，没有type这个关键字，我们也可以composite functions。只不过就得写成这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> teenagerPred: (<span class="type">Person</span>) =&gt; <span class="type">Boolean</span> = person =&gt; person.age &lt; <span class="number">20</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">incomeTax</span></span>: (<span class="type">Person</span>) =&gt; <span class="type">Double</span> = person =&gt; person.income * <span class="number">5</span> / <span class="number">100</span></span></pre></td></tr></table></figure>

<p>看着稍微有点眼花。</p>
<p>这种用type关键字给一种操作命名的代码反编译之后是这样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function1&lt;Person, Object&gt; <span class="title">teenagerPred</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> AbstractFunction1() &#123; <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">apply</span><span class="params">(Person person)</span> </span>&#123; <span class="keyword">return</span> person.age() &lt; <span class="number">20</span>; &#125;  &#125; ;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function1&lt;Person, Object&gt; <span class="title">incomeTax</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> AbstractFunction1() &#123; <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">double</span> <span class="title">apply</span><span class="params">(Person person)</span> </span>&#123; <span class="keyword">return</span> person.income() * <span class="number">5</span> / <span class="number">100</span>; &#125;  &#125; ;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function1&lt;Person, Object&gt; <span class="title">kejuanzaTax</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">return</span> <span class="keyword">new</span> AbstractFunction1() &#123; <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">double</span> <span class="title">apply</span><span class="params">(Person person)</span> </span>&#123; <span class="keyword">return</span> person.income() * <span class="number">20</span> / <span class="number">100</span>; &#125; &#125; ;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以看到所有这种接受一个参数，返回一个值的操作都是Function1&lt;Person, Object&gt;。</p>
<p>推测一下，接受两个参数，返回一个值的是不是该叫做Function2呢？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">TwoToOne</span> </span>= (<span class="type">String</span>, <span class="type">Int</span>) =&gt; <span class="type">Double</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">twoToOneImpl</span></span>: <span class="type">TwoToOne</span> = (str, i) =&gt; <span class="number">1</span></span></pre></td></tr></table></figure>

<p>反编译之后，果不其然：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function2&lt;String, Object, Object&gt; <span class="title">twoToOneImpl</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> Hello..anonfun.twoToOneImpl<span class="number">.1</span>(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>那不接收参数，只有返回值的呢？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">NoInJustOut</span> </span>= () =&gt; <span class="type">String</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">noInJustOutImpl</span></span>: <span class="type">NoInJustOut</span> = () =&gt; <span class="string">"hello world"</span></span></pre></td></tr></table></figure>

<p>反编译之后，其实是变成了Function0 of String:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Function0&lt;String&gt; <span class="title">noInJustOutImpl</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> Hello..anonfun.noInJustOutImpl<span class="number">.1</span>(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>到这里，我们可以总结一下type alia这个糖衣：</p>
<p>一个类型的type alias，类似于这样的：type t = x。编译器将在所有使用到t的地方把t替换为x。</p>
<p>对于一种操作的type alias，编译器将会根据参数列表和返回值类型的不同将其替换为对应的Function0,Function1,Function2 …… 一直到Function22。</p>
<p>如果我们真的定义一个超过二十二个参数的操作会如何呢？</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">type</span> <span class="title">twentyThree</span> </span>= (</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="type">String</span>, <span class="type">String</span>, <span class="type">String</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  ) =&gt; <span class="type">String</span></span></pre></td></tr></table></figure>

<p>Scala编译器会直接告诉我们：<br>type Function23 is not a member of package scala</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala中的语言特性是如何实现的(3) -- trait</title>
    <url>/blog/2013/10/13/scala-trait/</url>
    <content><![CDATA[<p>我在Coursera上跟了一门叫做<a href="https://www.coursera.org/course/progfun" target="_blank" rel="noopener">Functional Programming Principles in Scala</a>的课程，是由Scala的作者Martin Odersky讲授的。其中第三周的作业中使用到了Scala的trait这个语言特性。</p>
<p>我以前熟知的语言都没有类似的特性（Ruby的mixin和Scala的trait很像，但是Ruby我不熟），所以这周的博客就分析一下这个语言特性是如何实现的。</p>
<h3 id="trait"><a href="#trait" class="headerlink" title="trait"></a>trait</h3><p>在讲trait的实现机制之前，先看一个使用trait的例子。<br>假设我们有以下几个类：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Plant</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">photosynthesis</span> </span>= println(<span class="string">"Oh, the sunlight!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rose</span> <span class="keyword">extends</span> <span class="title">Plant</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">smell</span> </span>= println(<span class="string">"Good!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">makePeopleHappy</span> </span>= println(<span class="string">"People like me"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ruderal</span> <span class="keyword">extends</span> <span class="title">Plant</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">grow</span> </span>= println(<span class="string">"I take up all the space!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Animal</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">move</span> </span>= println(<span class="string">"I can move!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">bark</span> </span>= println(<span class="string">"Woof!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">makePeopleHappy</span> </span>= println(<span class="string">"People like me"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Snake</span> <span class="keyword">extends</span> <span class="title">Animal</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">bite</span> </span>= println(<span class="string">"I am poisonous!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>植物家族有玫瑰和杂草。</p>
<p>动物家族有狗和毒蛇。</p>
<p>仔细观察可以发现，玫瑰和狗有一个共同的行为，它们都可以取悦人类，这个行为是用完全一样的代码实现的。</p>
<p>如何把Rose和Dog中的重复代码消除掉呢？有一种潜在的解决方案：<br>把makePeopleHappy提取到一个类中去，让植物和动物都继承自它。</p>
<p>这么做虽然消除了重复代码但有两个明显的缺点：</p>
<ol>
<li>植物和动物继承自同一个类，不太合理</li>
<li>杂草和毒蛇也具有了取悦于人的能力，也不太合理</li>
</ol>
<p>这时我们就可以使用trait，它没有上面提到的两个缺点。</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">PeoplePleaser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">makePeopleHappy</span> </span>= println(<span class="string">"People like me"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rose</span> <span class="keyword">extends</span> <span class="title">Plant</span> <span class="keyword">with</span> <span class="title">PeoplePleaser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">smell</span> </span>= println(<span class="string">"Good!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span> <span class="keyword">with</span> <span class="title">PeoplePleaser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">bark</span> </span>= println(<span class="string">"Woof!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>我们定义一个trait，把makePeopleHappy置于其中，让Rose和Dog都with这个trait。然后就可以写这样的代码来调用它们了：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">Rose</span>().makePeopleHappy</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="type">Dog</span>().makePeopleHappy</span></pre></td></tr></table></figure>

<p>这样我们就解决了重复代码的问题，而且没有触及已存在的继承关系。</p>
<p>现在看看trait的实现机制吧，我们开始反编译！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">interface</span> <span class="title">PeoplePleaser</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">PeoplePleaser</span>$<span class="title">class</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">(PeoplePleaser $<span class="keyword">this</span>)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="string">"People like me"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> $init$(PeoplePleaser $<span class="keyword">this</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Rose</span> <span class="keyword">extends</span> <span class="title">Plant</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class">  <span class="keyword">implements</span> <span class="title">PeoplePleaser</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser$<span class="class"><span class="keyword">class</span>.<span class="title">makePeopleHappy</span>(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">smell</span><span class="params">()</span> </span>&#123; Predef..MODULE$.println(<span class="string">"Good!"</span>); &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Rose</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser<span class="class">.<span class="keyword">class</span>.$<span class="title">init</span>$(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class">  <span class="keyword">implements</span> <span class="title">PeoplePleaser</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser$<span class="class"><span class="keyword">class</span>.<span class="title">makePeopleHappy</span>(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bark</span><span class="params">()</span> </span>&#123; Predef..MODULE$.println(<span class="string">"Woof!"</span>); &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Dog</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser<span class="class">.<span class="keyword">class</span>.$<span class="title">init</span>$(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>真相大白了，PeoplePleaser被编译成了一个接口加一个抽象类。Rose和Dog实现这个接口，并通过调用抽象类中的静态方法来实现了makePeopleHappy。</p>
<p>很有趣的一点是Rose和Dog在调用静态方法时都把this传了进去，为什么呢？我们把原来的代码改成这样来看：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">PeoplePleaser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> moreMessage = <span class="string">""</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">makePeopleHappy</span> </span>= println(<span class="string">"People like me. "</span> + moreMessage)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rose</span> <span class="keyword">extends</span> <span class="title">Plant</span> <span class="keyword">with</span> <span class="title">PeoplePleaser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">override</span> <span class="keyword">val</span> moreMessage = <span class="string">"Because I smell nice."</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">smell</span> </span>= println(<span class="string">"Good!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span> <span class="keyword">with</span> <span class="title">PeoplePleaser</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">override</span> <span class="keyword">val</span> moreMessage = <span class="string">"Because I fetch balls."</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">bark</span> </span>= println(<span class="string">"Woof!"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>我们给makePeopleHappy加上一段额外的信息。<br>现在再次反编译。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">interface</span> <span class="title">PeoplePleaser</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> objsets$PeoplePleaser$_setter_$moreMessage_$eq(String paramString);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title">moreMessage</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">PeoplePleaser</span>$<span class="title">class</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">(PeoplePleaser $<span class="keyword">this</span>)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="keyword">new</span> StringBuilder()</span></pre></td></tr><tr><td class="code"><pre><span class="line">    .append(<span class="string">"People like me. "</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    .append($<span class="keyword">this</span>.moreMessage()).toString());</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> $init$(PeoplePleaser $<span class="keyword">this</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    $<span class="keyword">this</span>.objsets$PeoplePleaser$_setter_$moreMessage_$eq(<span class="string">""</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Rose</span> <span class="keyword">extends</span> <span class="title">Plant</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class">  <span class="keyword">implements</span> <span class="title">PeoplePleaser</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> String moreMessage;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> objsets$PeoplePleaser$_setter_$moreMessage_$eq(String x$<span class="number">1</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser$<span class="class"><span class="keyword">class</span>.<span class="title">makePeopleHappy</span>(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">moreMessage</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="keyword">this</span>.moreMessage; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">smell</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="string">"Good!"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Rose</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser<span class="class">.<span class="keyword">class</span>.$<span class="title">init</span>$(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.moreMessage = <span class="string">"Because I smell nice."</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class">  <span class="keyword">implements</span> <span class="title">PeoplePleaser</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> String moreMessage;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> objsets$PeoplePleaser$_setter_$moreMessage_$eq(String x$<span class="number">1</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">makePeopleHappy</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser$<span class="class"><span class="keyword">class</span>.<span class="title">makePeopleHappy</span>(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">moreMessage</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="keyword">this</span>.moreMessage; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bark</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="string">"Woof!"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Dog</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    PeoplePleaser<span class="class">.<span class="keyword">class</span>.$<span class="title">init</span>$(<span class="title">this</span>)</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.moreMessage = <span class="string">"Because I fetch balls."</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>现在就清楚了，抽象类中的静态方法可能会依赖于各个实例不同的状态，所以需要把this传递进去。<br>这样我们才能够给makePeopleHappy加上一段额外的信息。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>为什么必须是final的呢？</title>
    <url>/blog/2013/06/22/why-does-it-have-to-be-final/</url>
    <content><![CDATA[<h2 id="一个谜团"><a href="#一个谜团" class="headerlink" title="一个谜团"></a>一个谜团</h2><p>如果你用过类似guava这种“伪函数式编程”风格的library的话，那下面这种风格的代码对你来说应该不陌生：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">tryUsingGuava</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> expectedLength = <span class="number">4</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Iterables.filter(Lists.newArrayList(<span class="string">"123"</span>, <span class="string">"1234"</span>), <span class="keyword">new</span> Predicate&lt;String&gt;() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="meta">@Override</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">apply</span><span class="params">(String str)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">return</span> str.length() == expectedLength;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这段代码对一个字符串的list进行过滤，从中找出长度为4的字符串。看起来很是平常，没什么特别的。</p>
<p>但是，声明expectedLength时用的那个final看起来有点扎眼，把它去掉试试：</p>
<blockquote>
<p>error: local variable expectedLength is accessed from within inner class; needs to be declared final</p>
</blockquote>
<p>结果Java编译器给出了如上的错误，看起来匿名内部类只能够访问final的局部变量。但是，<strong>为什么呢？其他的语言也有类似的规定吗？</strong></p>
<p>在开始用其他语言做实验之前我们先把问题简化一下，不要再带着guava了，我们去除掉噪音，把问题归结为：</p>
<p><strong>为什么Java中的匿名内部类只可以访问final的局部变量呢？其他语言中的匿名函数也有类似的限制吗？</strong></p>
<h2 id="Scala中有类似的规定吗？"><a href="#Scala中有类似的规定吗？" class="headerlink" title="Scala中有类似的规定吗？"></a>Scala中有类似的规定吗？</h2><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tryAccessingLocalVariable</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">var</span> number = <span class="number">123</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  println(number)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">var</span> lambda = () =&gt; &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    number = <span class="number">456</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(number)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  lambda.apply()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  println(number)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>上面的Scala代码是合法的，number变量是声明为var的，不是val（类似于Java中的final）。而且在匿名函数中可以修改number的值。</p>
<p>看来<strong>Scala中没有类似的规定</strong>。</p>
<h2 id="C-中有类似的规定吗？"><a href="#C-中有类似的规定吗？" class="headerlink" title="C#中有类似的规定吗？"></a>C#中有类似的规定吗？</h2><figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">tryUsingLambda</span> (<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">int</span> number = <span class="number">123</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Console.WriteLine (number);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	Action action = () =&gt; &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		number = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		Console.WriteLine (number);</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	action ();</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Console.WriteLine (number);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这段C#代码也是合法的，number这个局部变量在lambda表达式内外都可以访问和赋值。</p>
<p>看来<strong>C#中也没有类似的规定</strong>。</p>
<h2 id="分析谜团"><a href="#分析谜团" class="headerlink" title="分析谜团"></a>分析谜团</h2><p>三门语言中只有Java有这种限制，那我们分析一下吧。先来看一下Java中的匿名内部类是如何实现的：</p>
<p>先定义一个接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyInterface</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>然后创建这个接口的匿名子类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TryUsingAnonymousClass</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">useMyInterface</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">final</span> Integer number = <span class="number">123</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(number);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        MyInterface myInterface = <span class="keyword">new</span> MyInterface() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="meta">@Override</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                System.out.println(number);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        myInterface.doSomething();</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(number);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个匿名子类会被编译成一个单独的类，反编译的结果是这样的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TryUsingAnonymousClass</span>$1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class">        <span class="keyword">implements</span> <span class="title">MyInterface</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> TryUsingAnonymousClass <span class="keyword">this</span>$<span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Integer paramInteger;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    TryUsingAnonymousClass$<span class="number">1</span>(TryUsingAnonymousClass <span class="keyword">this</span>$<span class="number">0</span>, Integer paramInteger) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.<span class="keyword">this</span>$<span class="number">0</span> = <span class="keyword">this</span>$<span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.paramInteger = paramInteger;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(<span class="keyword">this</span>.paramInteger);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以看到名为number的局部变量是作为构造方法的参数传入匿名内部类的（以上代码经过了手动修改，真实的反编译结果中有一些不可读的命名）。</p>
<p>如果Java允许匿名内部类访问非final的局部变量的话，那我们就可以在TryUsingAnonymousClass$1中修改paramInteger，但是这不会对number的值有影响，因为它们是不同的reference。</p>
<p>这就会造成数据不同步的问题。</p>
<p>所以，<strong>谜团解开了：Java为了避免数据不同步的问题，做出了匿名内部类只可以访问final的局部变量的限制。</strong></p>
<p>但是，新的谜团又出现了：</p>
<h2 id="Scala和C-为什么没有类似的限制呢？它们是如何处理数据同步问题的呢？"><a href="#Scala和C-为什么没有类似的限制呢？它们是如何处理数据同步问题的呢？" class="headerlink" title="Scala和C#为什么没有类似的限制呢？它们是如何处理数据同步问题的呢？"></a>Scala和C#为什么没有类似的限制呢？它们是如何处理数据同步问题的呢？</h2><p>上面出现过的那段Scala代码中的lambda表达式会编译成这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">TryUsingAnonymousClassInScala</span>$$<span class="title">anonfun</span>$1 <span class="keyword">extends</span> <span class="title">AbstractFunction0</span>.<span class="title">mcV</span>.<span class="title">sp</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class">        <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">0L</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> IntRef number$<span class="number">2</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">apply</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        apply$mcV$sp();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> apply$mcV$sp() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.number$<span class="number">2</span>.elem = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Predef..MODULE$.println(BoxesRunTime.boxToInteger(<span class="keyword">this</span>.number$<span class="number">2</span>.elem));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">public</span> TryUsingAnonymousClassInScala$$anonfun$<span class="number">1</span>(TryUsingAnonymousClassInScala $outer, IntRef number$<span class="number">2</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">this</span>.number$<span class="number">2</span> = number$<span class="number">2</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以看到number也是通过构造方法的参数传入的，但是与Java的不同是这里的number不是直接传入的，是被IntRef包装了一层然后才传入的。对number的值修改也是通过包装类进行的：this.number$2.elem = 456;</p>
<p>这样就保证了lambda表达式内外访问到的是同一个对象。</p>
<p>再来看看C#的处理方式，反编译一下，发现C#编译器生成了如下的一个类：</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line">private sealed class &lt;tryUsingLambda&gt;c__AnonStorey0</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">internal</span> <span class="keyword">int</span> number;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">internal</span> <span class="keyword">void</span> &lt;&gt;m__0 ()</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		<span class="keyword">this</span>.number = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		Console.WriteLine (<span class="keyword">this</span>.number);</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>把number包装在这个类内，这样就保证了lambda表达式内外使用的都是同一个number，即便重新赋值也可以保证内外部的数据是同步的。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Scala和C#的编译器通过把局部变量包装在另一个对象中，来实现lambda表达式内外的数据同步。</p>
<p>而Java的编译器由于未知的原因（怀疑是为了图省事儿？）没有做包装局部变量这件事儿，于是就只好强制用户把局部变量声明为final才能在匿名内部类中使用来避免数据不同步的问题。</p>
]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala中的语言特性是如何实现的(2)</title>
    <url>/blog/2013/05/11/scala-language-features-2/</url>
    <content><![CDATA[<p><a href="http://cuipengfei.me/blog/2013/05/05/how-are-scala-language-features-implemented/" target="_blank" rel="noopener">上篇博文</a>的末尾留了三个问题，现在自问自答一下。</p>
<h3 id="在Scala中被声明为val的v4为什么在反编译的Java中不是final的呢？"><a href="#在Scala中被声明为val的v4为什么在反编译的Java中不是final的呢？" class="headerlink" title="在Scala中被声明为val的v4为什么在反编译的Java中不是final的呢？"></a>在Scala中被声明为val的v4为什么在反编译的Java中不是final的呢？</h3><p>在方法中声明局部变量时，如果用Scala的val关键字（或者是Java中的final）来修饰变量，则代表着此变量在赋过初始值之后不可以再被重新赋值。这个val或者final只是给编译器用的，编译器如果发现你给此变量重新赋值会抛出错误。</p>
<p>而bytecode不具备表达一个局部变量是immutable的能力，也就是说对于JVM来说，不存在不可变的局部变量这个概念。所以v4在反编译之后，就和普通的局部变量无异了。</p>
<h3 id="在Scala中被声明为val的v2为什么在反编译的C-中不是readonly的呢？"><a href="#在Scala中被声明为val的v2为什么在反编译的C-中不是readonly的呢？" class="headerlink" title="在Scala中被声明为val的v2为什么在反编译的C#中不是readonly的呢？"></a>在Scala中被声明为val的v2为什么在反编译的C#中不是readonly的呢？</h3><p>这是个挺tricky的问题，我试着解释一下。Scala .NET是基于IKVM实现的，IKVM可以把Java bytecode翻译为CIL。<br>所以Scala编译为CIL的过程实际是这样的：</p>
<p>Scala —–Scala编译器—–&gt; bytecode —–IKVM—–&gt; CIL</p>
<p>Scala编译器编译出的bytecode实际是用final修饰了v2的，但是bytecode中的final和CIL中的initonly（对应C#的readonly）是不一样的。</p>
<p>Java中，final实例变量定义的时候，可以先声明，而不给初值，然后我们可以在任何一个方法中给它赋初值。这提供了更大的灵活性，一个Java类中的final成员可以依对象而不同，却保持其immutable的特征。</p>
<p>而CIL的initonly则要严格一点，CLI标准（ECMA-334）这样描述：</p>
<blockquote>
<p>initonly marks fields which are constant after they are initialized. These fields shall only be mutated inside a constructor. If the field is a static field, then it shall be mutated only inside the type initializer of the type in which it was declared. If it is an instance field, then it shall be mutated only in one of the instance constructors of the type in which it was defined. It shall not be mutated in any other method or in any other constructor, including constructors of derived classes.</p>
</blockquote>
<p>可见，一个initonly的成员，不是随便在哪儿都可以赋初值的。由于这点不同IKVM就没有直接把final翻译成initonly。如果想让v2在C#代码中变成readonly的，可以给IKVM加上strictfinalfieldsemantics这个参数。</p>
<h3 id="为什么反编译出来的C-代码中的实例级公开方法都是标有override的呢？"><a href="#为什么反编译出来的C-代码中的实例级公开方法都是标有override的呢？" class="headerlink" title="为什么反编译出来的C#代码中的实例级公开方法都是标有override的呢？"></a>为什么反编译出来的C#代码中的实例级公开方法都是标有override的呢？</h3><p>这个问题还没搞明白。</p>
<p>但是有个有趣的现象，如果用Scala .NET来编译Scala源码，编译出的实例级方法都是标有override的；而如果先把Scala代码编译为.class然后再用IKVM把.class文件转换为CIL的话，方法则是标有virtual的。我猜这可能和Java中的方法默认是可以被overirde的有关。</p>
<p>下面开始正文，前面填坑用了不少篇幅，所以这次只分析一个语言特性：Scala中的constructor。</p>
<p>##Constructor</p>
<p>Scala中可以在声明class的同时声明一个constructor，比如这样：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ScalaConstructorExample</span>(<span class="params">val x: <span class="type">Double</span>, y: <span class="type">String</span></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  println(x + y)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>构造函数接收两个参数x和y，然后把x和y拼在一起打印出来。反编译为Java：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ScalaConstructorExample</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">double</span> x;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">x</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.x;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ScalaConstructorExample</span><span class="params">(<span class="keyword">double</span> x, String y)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="keyword">new</span> StringBuilder().append(x).append(y).toString());</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>可以发现编译器给标为val的x生成了一个getter，很方便的语法糖。而直接写在类内的打印语句则被放到了构造函数内。下面是反编译为C#的代码：</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">ScalaConstructorExample</span> : <span class="title">ScalaObject</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">private</span> <span class="keyword">double</span> x = x;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">double</span> <span class="title">x</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>.x;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">ScalaConstructorExample</span>(<span class="params"><span class="keyword">double</span> x, <span class="keyword">string</span> y</span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		Predef$.MODULE$.println(<span class="keyword">new</span> StringBuilder().Append(x).Append(y).ToString());</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>和Java代码基本无异。比较一下，Scala用3行代码表达的含义，Java和C#要用14行才行。</p>
<p>现在加一个重载的构造函数：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ScalaConstructorExample</span>(<span class="params">val x: <span class="type">Double</span>, y: <span class="type">String</span></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  println(x + y)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">this</span></span>(x: <span class="type">Double</span>) = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  	<span class="keyword">this</span>(x, <span class="string">"hello"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个构造函数给了y一个默认值“hello”。反编译为Java：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ScalaConstructorExample</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">double</span> x;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">x</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.x;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ScalaConstructorExample</span><span class="params">(<span class="keyword">double</span> x, String y)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  	Predef..MODULE$.println(<span class="keyword">new</span> StringBuilder().append(x).append(y).toString());</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ScalaConstructorExample</span><span class="params">(<span class="keyword">double</span> x)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>(x, <span class="string">"hello"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>对应的C#代码：</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">ScalaConstructorExample</span> : <span class="title">ScalaObject</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="keyword">private</span> <span class="keyword">double</span> x = x;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">double</span> <span class="title">x</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>.x;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">ScalaConstructorExample</span>(<span class="params"><span class="keyword">double</span> x, <span class="keyword">string</span> y</span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">		Predef$.MODULE$.println(<span class="keyword">new</span> StringBuilder().Append(x).Append(y).ToString());</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">ScalaConstructorExample</span>(<span class="params"><span class="keyword">double</span> x</span>) : <span class="title">this</span>(<span class="params">x, <span class="string">"hello"</span></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>	&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">	&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>构造函数重载这个特性就显得平淡无奇了，不过还是比较一下行数。定义两个构造函数，打印出构造函数的参数，声明一个getter，这三件事Scala只用7行代码就完成了，Java和C#都需要将近20行。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala中的语言特性是如何实现的(1)</title>
    <url>/blog/2013/05/05/how-are-scala-language-features-implemented/</url>
    <content><![CDATA[<p>Scala可以编译为Java bytecode和CIL，从而在JVM和CLI之上运行。Scala有很多在Java和C#的世界中显得陌生的语言特性，本文将分析这些语言特性是如何实现的。</p>
<h2 id="object"><a href="#object" class="headerlink" title="object"></a>object</h2><p>Scala中可以像这样创建object：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">HowIsObjectImplementedInScala</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">printSomething</span></span>() &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(<span class="string">"printSomething"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>然后在代码的其他地方调用printSomething，一个object究竟是什么东西呢？<br>我们将这段Scala编译为Java bytecode，然后反编译为Java，会发现编译器为HowIsObjectImplementedInScala这个object生成了两个类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">HowIsObjectImplementedInScala</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">printSomething</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    HowIsObjectImplementedInScala..MODULE$.printSomething();</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">HowIsObjectImplementedInScala</span>$</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span>  MODULE$;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">static</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">new</span> ();</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">printSomething</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(<span class="string">"printSomething"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> HowIsObjectImplementedInScala$()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    MODULE$ = <span class="keyword">this</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>第一个类只包含一个静态方法，其实现依赖于第二个叫做HowIsObjectImplementedInScala$的类。</p>
<p>HowIsObjectImplementedInScala$是一个单例，其静态块实例化自己并把this赋值给MODULE$这个public static的成员，从而可以被外界访问。</p>
<p>同样，我们可以把这段代码编译为CIL，然后反编译为C#:</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">sealed</span> <span class="keyword">class</span> <span class="title">HowIsObjectImplementedInScala</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">printSomething</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    HowIsObjectImplementedInScala$.MODULE$.printSomething();</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">public sealed class HowIsObjectImplementedInScala$ : ScalaObject</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> HowIsObjectImplementedInScala$ MODULE$;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">void</span> <span class="title">printSomething</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef$.MODULE$.println(<span class="string">"printSomething"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> HowIsObjectImplementedInScala$()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    HowIsObjectImplementedInScala$.MODULE$ = <span class="keyword">this</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">static</span> HowIsObjectImplementedInScala$()</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">new</span> HowIsObjectImplementedInScala$();</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>和Java代码大同小异，除了静态构造和某几个关键字外，基本一样。一个object就是一个Scala编译器帮我们实现的singleton。</p>
<h2 id="var和val"><a href="#var和val" class="headerlink" title="var和val"></a>var和val</h2><p>var：可变。val：不可变。关于这两个关键字何时该使用哪一个，这里不做讨论，我们只是观察这二者在编译后是如何被表示的。</p>
<p>这段Scala代码：</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HowAreVarAndValImplementedInScala</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">var</span> v1 = <span class="number">123</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">val</span> v2 = <span class="number">456</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">method1</span></span>() = &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">var</span> v3 = <span class="number">123</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">val</span> v4 = <span class="number">456</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    println(v3 + v4)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>定义了两个字段一个var，一个val，方法中定义了两个局部变量，一个var，一个val。</p>
<p>编译为Java bytecode并反编译之后：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HowAreVarAndValImplementedInScala</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="class"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> v1 = <span class="number">123</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> v2 = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">v1</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function">  </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.v1;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> v1_$eq(<span class="keyword">int</span> x$<span class="number">1</span>) &#123; <span class="keyword">this</span>.v1 = x$<span class="number">1</span>; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">v2</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="keyword">this</span>.v2; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">method1</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> v3 = <span class="number">123</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> v4 = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef..MODULE$.println(BoxesRunTime.boxToInteger(v3 + v4));</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>声明为字段的v1和v2，一个是普通字段，另一个则被标记为final。编译器为v1生成了getter和setter，为v2则只有getter，因为v2作为immutable的字段是不可以被重新赋值的。</p>
<p>有趣的是方法中的局部变量都是普通的变量，没有被final修饰。</p>
<p>再来看这段Scala编译为CIL再反编译为C#之后的样子：</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">HowAreVarAndValImplementedInScala</span> : <span class="title">ScalaObject</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> v1;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> v2;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">int</span> <span class="title">v1</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.v1;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">void</span> v1_$eq(<span class="keyword">int</span> x$<span class="number">1</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.v1 = x$<span class="number">1</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">int</span> <span class="title">v2</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.v2;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">void</span> <span class="title">method1</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> v3 = <span class="number">123</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> v4 = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Predef$.MODULE$.println(v3 + v4);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">HowAreVarAndValImplementedInScala</span>(<span class="params"></span>)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>  &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.v1 = <span class="number">123</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">this</span>.v2 = <span class="number">456</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>有一个明显的问题，v2没有标为readonly（C#世界中用于声明变量不可以重新赋值的关键字），这是compiler的bug吗？</p>
<p>除此之外，和Java代码一致。但是有趣的是代码中的所有public方法（包括上一段演示object的代码）都被标为了override，原因不明。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本来以为研究这么简单的两个语言特性不会有啥收获，仅仅是反编译一下，看看compiler都做了啥，满足下好奇心罢了。</p>
<p>结果还是有意外收获，我在反编译后的代码中发现了三个有趣的问题：</p>
<ul>
<li>在Scala中被声明为val的v4为什么在反编译的Java中不是final的呢？</li>
<li>在Scala中被声明为val的v2为什么在反编译的C#中不是readonly的呢？</li>
<li>为什么反编译出来的C#代码中的实例级公开方法都是标有override的呢？</li>
</ul>
<p>为什么呢？为什么呢？为什么呢？答案下期揭晓。</p>
]]></content>
      <tags>
        <tag>Desugar_Scala</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>如何一步一步推导出Y Combinator</title>
    <url>/blog/2013/04/09/make-y/</url>
    <content><![CDATA[<h2 id="本文讲什么？"><a href="#本文讲什么？" class="headerlink" title="本文讲什么？"></a>本文讲什么？</h2><p>本文用Scheme（Racket）代码为例，一步一步的推出Y Combinator的实现。</p>
<h2 id="本文不讲什么？"><a href="#本文不讲什么？" class="headerlink" title="本文不讲什么？"></a>本文不讲什么？</h2><p>Y Combinator是什么，干什么用的，它为什么能够work，它的数学含义以及实际应用场景，这些话题由于篇幅所限（咳咳，楼主的无知）不在本文论述范围之内。</p>
<p>如果有兴趣，请参考维基： <a href="http://en.wikipedia.org/wiki/Fixed-point_combinator#Y_combinator" target="_blank" rel="noopener">http://en.wikipedia.org/wiki/Fixed-point_combinator#Y_combinator</a></p>
<h2 id="鸣谢"><a href="#鸣谢" class="headerlink" title="鸣谢"></a>鸣谢</h2><p>感谢Jojo同学的<br><a href="https://github.com/zhewuzhou/js-y-combinator/blob/master/y-combinator.js" target="_blank" rel="noopener">这段JavaScript代码</a>的启发，我写了<a href="https://github.com/cuipengfei/lambda-calculus-impl/blob/master/racket/%E4%B8%80%E6%AD%A5%E4%B8%80%E6%AD%A5%E6%8E%A8%E5%87%BAY.rkt" target="_blank" rel="noopener">对应的Scheme实现</a>。然后才有了本文。</p>
<h2 id="正文开始"><a href="#正文开始" class="headerlink" title="正文开始"></a>正文开始</h2><p>我们知道Y Combinator可以帮匿名函数实现递归。那就从一个广为人知的递归函数-阶乘开始吧。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(define (fac1 n)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">      (* n (fac1 (- n 1)))))</span></pre></td></tr></table></figure>

<p>如果n小于2，则返回1，否则开始递归，简单明了。如果像这样调用它一下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(fac1 5)</span></pre></td></tr></table></figure>

<p>会返回120，结果无误。</p>
<p>上面是阶乘的递归实现，它有一个名字叫做fac1，但是如果用匿名函数实现阶乘呢？</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">  (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">    (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">        (* n (f (- n 1))))))</span></pre></td></tr></table></figure>

<p>这个匿名函数“梦想着”其调用者会把该函数自己的实现作为参数传递进去。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(((lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">    (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">          (* n (f (- n 1)))))) </span></pre></td></tr><tr><td class="code"><pre><span class="line">  (lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">    (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">          (* n (f (- n 1))))))) 1)</span></pre></td></tr></table></figure>

<p>我们把匿名函数重复写一遍，就可以计算1或者是0的阶乘，但是要计算3的阶乘呢？那就得这么写：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(((lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">    (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">          (* n (f (- n 1)))))) </span></pre></td></tr><tr><td class="code"><pre><span class="line">  ((lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">     (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">       (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">           (* n (f (- n 1)))))) </span></pre></td></tr><tr><td class="code"><pre><span class="line">   ((lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">        (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">            (* n (f (- n 1)))))) </span></pre></td></tr><tr><td class="code"><pre><span class="line">    (lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">        (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">            (* n (f (- n 1))))))))) 3)</span></pre></td></tr></table></figure>

<p>想要计算一个大于2的n的阶乘，就得把这个匿名函数重复写n+1次。这么多的重复代码，这么多的括号。。。</p>
<p>所以我们需要一个神奇的函数，Y，它可以接受一个匿名的伪递归函数作为参数，产出一个真递归的函数。<br>这个神奇的Y作用在上面的匿名函数上之后产出的结果就可以用来计算任何n的阶乘。下面的代码会输出120（如果Y已经实现了的话）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((Y (lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">       (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">         (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">             (* n (f (- n 1))))))) 5)</span></pre></td></tr></table></figure>

<p>下面就开始一步步的构造这个神奇的Y吧。</p>
<p>为了便于推导，暂时给这个匿名函数一个名字，叫做fake_fac。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(define fake_fac </span></pre></td></tr><tr><td class="code"><pre><span class="line">  (lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">    (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">          (* n (f (- n 1)))))))</span></pre></td></tr></table></figure>

<p>有了这个名字之后，再要计算3的阶乘就容易了一些。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((fake_fac (fake_fac (fake_fac fake_fac))) 3)</span></pre></td></tr></table></figure>

<p>观察上面的代码，我们把fake_fac传递给它自己，得到一个返回值，把这个返回的值再次传递给fake_fac，再得到一个新的返回值，又把新的返回值传递给fake_fac，得到最终的返回值，最后把3传递给这个返回值。</p>
<p>可以看到，我们在不停的把fake_rec传给它自己，所以定义一个helper吧：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(define (callself f) (f f))</span></pre></td></tr></table></figure>

<p>这个helper一会儿会派上用场。</p>
<p>现在看看fake_fac中的f是什么呢？对于((fake_fac (fake_fac (fake_fac fake_fac))) 3)这行代码中的最右侧的fake_fac来说，f没有用，因为这个fake_fac自己都没有被调到，它只是起个占位符的作用，实际上这行代码((fake_fac (fake_fac (fake_fac 1))) 3)和上面的那行是等价的。</p>
<p>对于右侧第二个fake_fac来说，f就是fake_fac。对于左侧第二个fake_fac来说，f是(fake_fac fake_fac)的返回值。</p>
<p>对于左侧第一个fake_fac来说，f是(fake_fac (fake_fac fake_fac))的返回值。</p>
<p>由此可见，f是fake_fac对自己反复调用的返回值。而且从fake_fac的定义可见，我们总是给f传递一个数字n，这样的话，我们再写一个helper：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(lambda (n) ((f f) n))</span></pre></td></tr></table></figure>

<p>再把这个helper传递给fake_fac。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(fake_fac (lambda (n) ((f f) n)))</span></pre></td></tr></table></figure>

<p>但是上面这两段代码是有问题的，因为f的值无法确定。</p>
<p>有句话说的好：<br>if you don’t know exactly what you want to put somewhere in a piece of code, just abstract it out and make it a parameter of a function.<br>所以我们就把f抽成参数吧。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(define (callselfWithN f)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  (fake_fac (lambda (n) ((f f) n))))</span></pre></td></tr></table></figure>

<p>我们希望这个helper可以帮fake_fac无限次的调用自己。</p>
<p>现在，我们该怎么调用callselfWithN呢？不能把fake_fac传给它，因为那样的话(f f)就只是fake_fac对自己的调用，它只能计算0或者1的阶乘。所以要把callselfWithN这个我们希望可以帮fake_fac实现无限次自调用的函数传给callselfWithN它自己。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((callselfWithN callselfWithN) 5)</span></pre></td></tr></table></figure>

<p>这行代码可以返回120，结果正确了！</p>
<p>记得前面定义的第一个helper吗？现在用的上了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((callself callselfWithN) 5)</span></pre></td></tr></table></figure>

<p>现在把callselfWithN带入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((callself  (lambda (f)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  (fake_fac (lambda (n) ((f f) n))))) 5)</span></pre></td></tr></table></figure>

<p>可以看出，这段代码和fake_fac是紧耦合的，把它抽到参数上去：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(define (Y3 fake_recur)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  (callself  (lambda (f)</span></pre></td></tr><tr><td class="code"><pre><span class="line">               (fake_recur (lambda (n) ((f f) n))))))</span></pre></td></tr></table></figure>

<p>然后再把callself也带入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(define Y (lambda (fake_recur) </span></pre></td></tr><tr><td class="code"><pre><span class="line">            ((lambda (f) (f f)) </span></pre></td></tr><tr><td class="code"><pre><span class="line">             (lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">               (fake_recur </span></pre></td></tr><tr><td class="code"><pre><span class="line">                (lambda (n) ((f f) n)))))))</span></pre></td></tr></table></figure>

<p>现在Y不依赖于任何其他函数了，测试一下Y，把前面的计算阶乘的匿名函数传给它：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((Y (lambda (f) </span></pre></td></tr><tr><td class="code"><pre><span class="line">      (lambda (n) </span></pre></td></tr><tr><td class="code"><pre><span class="line">        (if (&lt; n 2) 1</span></pre></td></tr><tr><td class="code"><pre><span class="line">            (* n (f (- n 1))))))) 5)</span></pre></td></tr></table></figure>

<p>能够返回120，正确！Y Combinator构造完成！</p>
]]></content>
  </entry>
  <entry>
    <title>用豆瓣读书Chrome插件，简单购买电子书</title>
    <url>/blog/2013/04/01/doubanebookprice/</url>
    <content><![CDATA[<h2 id="为什么要做这款插件？"><a href="#为什么要做这款插件？" class="headerlink" title="为什么要做这款插件？"></a>为什么要做这款插件？</h2><p>在豆瓣上查看一本书的时候，页面的右侧会显示哪些网站可以购买该书以及各自的价格。<br>比如这本《乡关何处》，页面右侧显示了亚马逊，京东，当当等网站的购买链接。</p>
<p><img src="http://farm9.staticflickr.com/8264/8609485540_026e013035_b.jpg" alt="乡关何处"></p>
<p>但是豆瓣只会提供纸质书的购买链接，不提供电子书的。除非该书豆瓣自己有售。<br>所以我写了个Chrome的插件来解决这个问题。</p>
<h2 id="这款插件怎么用？"><a href="#这款插件怎么用？" class="headerlink" title="这款插件怎么用？"></a>这款插件怎么用？</h2><p>这款插件会在每个图书页面上添加正版电子书的购买链接及其价格。您只需点击链接去购买就好了。</p>
<p>在Chrome中加载这个插件之后，再访问豆瓣的《乡关何处》的页面，右侧会多出三个链接：</p>
<p><img src="http://farm9.staticflickr.com/8532/8609505800_fbd41bdc60_b.jpg" alt="乡关何处"></p>
<p>淘宝，多看和亚马逊中文站都卖这本书的电子版。<br>看，纸质书要卖19.2，豆瓣电子书要卖11，多看只要6块钱，噢耶！节省纸张又省钱。</p>
<p>很多英文原版书在国内卖得很贵，所以这款插件也支持一些国外网站。<br>比如这本《The Pragmatic Programmer》：</p>
<p><img src="http://farm9.staticflickr.com/8247/8608418739_e4fe0b1ebb_b.jpg" alt="The Pragmatic Programmer"></p>
<p>原版纸质书亚马逊要卖351，澜瑞外文要卖487，吃人啊！亚马逊英文站的纸质版的售价折合成人民币也要180多，再加运费……</p>
<p>而电子版则要便宜很多，看截图上的红圈，最便宜的折合人民币只要130左右，嗯，还是有点贵，不过如果想读原版的话，这个kindle版是最好的选择了。</p>
<p>这款插件现在支持以下的电子书销售商：</p>
<ul>
<li>多看</li>
<li>唐茶</li>
<li>亚马逊kindle中文站</li>
<li>淘宝</li>
<li>京东</li>
<li>亚马逊kindle美国站</li>
<li>kobo</li>
<li>nook</li>
</ul>
<h2 id="如何实现的？"><a href="#如何实现的？" class="headerlink" title="如何实现的？"></a>如何实现的？</h2><p>是用JavaScript写的。</p>
<p>基本实现思路是这样的：</p>
<ul>
<li>获取当前图书的ISBN号码</li>
<li>根据ISBN去上面罗列的几家网站上搜索是否有这本书</li>
<li>如果有的话，显示在右侧</li>
</ul>
<p>例外情况：</p>
<ul>
<li>有些网站不支持ISBN搜索，对于这些网站直接用书名和作者名搜索的，所以结果有时不太准。</li>
<li>有时一本书的纸质版的ISBN和电子版的ISBN不同，所以实现中用到了google books的API来获取电子版的ISBN，然后再搜索。</li>
</ul>
<p>主要技术：<br>knockoutjs和jQuery。</p>
<h2 id="插件下载和源码"><a href="#插件下载和源码" class="headerlink" title="插件下载和源码"></a>插件下载和源码</h2><p>插件下载地址：<a href="https://chrome.google.com/webstore/detail/ebook-price-for-douban/ppbnlfplpcjhdphaejdfhbojmjifdjgd?hl=zh-CN" target="_blank" rel="noopener">chrome store</a></p>
<p>源码：<a href="https://github.com/cuipengfei/JavaScript-Practice-Code/tree/master/EBookPriceForDouBan" target="_blank" rel="noopener">github</a></p>
<h2 id="Disclaimer"><a href="#Disclaimer" class="headerlink" title="Disclaimer"></a>Disclaimer</h2><p>插件的实现中违反了豆瓣API的协议（豆瓣不允许其API的使用者利用豆瓣的数据产生其他网站的购买信息）。如果您对这点很敏感，请慎用。</p>
]]></content>
  </entry>
  <entry>
    <title>测试</title>
    <url>/blog/2013/03/04/ce-shi/</url>
    <content><![CDATA[<p>测试发布文章</p>
<p>测试修改</p>
<p>再次修改</p>
<p>好久没用了，还能更新吗？test</p>
<p>is it fixed 10?</p>
]]></content>
  </entry>
</search>
